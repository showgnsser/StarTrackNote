---
title: 当前跟踪方案总表
aliases: [八信号当前方案, 跟踪方案速查]
tags: [GNSS, 跟踪, 方案]
version: V1.17
date: 2026-09-14
cssclasses: [startrack-spec]
---

# 当前跟踪方案总表

本文长期维护八种信号实际使用的积分、鉴别方法和重要门限。正文只保留当前值；每次采用新方法，原位更新对应表格，并在末尾记录修改原因、收益、代价和验证依据。实验候选单列，不混入已采用配置；本表不代替灵敏度资格报告。

## 1 实现基线与使用约定

| 范围 | 当前依据 |
|---|---|
| 参考完整引擎 | StarTrack的`codex/sdr-engine-modes`，提交`cac4896`；极弱、辅助、动态状态及BOC粗码／同步开放流程仍据此记录，不表示已在重构通道采用 |
| 当前重构实现 | `codex/tracking-rebuild`，提交`d58d132`；L1CA/B1I D1/G1已接通PDI到码环、载波环、同步及六态的完整浮点通道。首个载波观察后开放同步，牵引与确认总时限20 s；同步参考换算及DLL换带宽偏置已修正。B1I仍为平方asin，不枚举NH；L1CA/G1仍为11点、±62.5 Hz及同步前±25 Hz鉴频限幅 |
| 旧完整引擎与重构的差异 | 旧完整引擎默认载波仍为独立FLL/PLL，不随重构提交自动改变；未接入项目见第6节 |

信号简称为GPS L1CA、BDS B1I/B2a/B1C、GLO G1、GAL E5a/E1及GPS L5。L5/B2a/E5a/E1/B1C默认以导频闭环。强档捕获进入`ShortFast`，中、弱档进入`LongFast`，不根据单次载噪比重新选择捕获入口。

以下普通六态的方法选择已由`track_method`集中实现，状态切换由`track_state`负责；Sensitivity、bit辅助和动态状态单独核对其专用观察器后再实现，当前没有这些状态的占位出口。

硬件始终输出1 ms相关结果。下文“10 ms×16功率”表示每10 ms相干一次，再将16块功率相加；“连续160 ms”才表示整窗相干。DFT均指频率候选旋转、相干累加、取功率及寻峰；普通DFT采用三点插值，不与FMS串联。复平方为复数自乘，不是取功率。

## 2 同步前牵引与同步

### 2.1 载波牵引

| 信号 | 强入口：ShortFast，20 ms更新 | 中弱入口：LongFast，160 ms更新 |
|---|---|---|
| L1CA | 相邻1 ms相关值做asin鉴频，窗内平均 | 10 ms×16功率；11点DFT，间隔12.5 Hz、范围±62.5 Hz；插值后输出限于±25 Hz |
| G1 | 相邻1 ms相关值做asin鉴频，窗内平均 | 5 ms×32功率；11点DFT，间隔12.5 Hz、范围±62.5 Hz；插值后输出限于±25 Hz |
| B1I | 1 ms相关值复平方后做asin鉴频，结果除以2，窗内平均 | 同一鉴频方法，延长平均至160 ms；不使用载波DFT |
| L5、B2a、E5a | 保持温启动移交频率，等待二次码同步；不产生FLL校正 | 同左；160 ms为入口码环周期，不表示此时已有载波鉴频 |
| E1 | 4 ms主码相干、复平方；累加相邻共轭积后做atan2，结果除以2 | 4 ms主码相干、复平方；逐相邻主码间隔做atan2，再平均39个频差 |
| B1C | 10 ms主码相干、复平方；累加相邻共轭积后做atan2，结果除以2 | 10 ms主码相干、复平方；逐相邻主码间隔做atan2，再平均15个频差 |

BOC两条中弱路径也需将二倍相位换算回实际频差；五抽头在每个间隔内联合使用，20/160 ms载波窗口不重叠、不跨窗配对。E1调码期间暂停这一路载波观察，B1C未采用相同的整段暂停；重构选择器保留这一差异，具体调码执行尚未接通。同步前均不使用PLL；所有信号LongFast同步前统一只校频，新频差不写入高阶变化率积分，E1/B1C不例外；BOC强入口及同步后保留原趋势许可。

数据型相邻Prompt平均按20/160个连续间隔计算：首次需要21/161条PDI，随后保留末Prompt端点，每20/160条新PDI更新一次。公共端点不代表重复统计间隔；观察结果的相关范围包含端点，不能把21/161条当成环路更新周期。DFT和BOC主码整窗合同不变。

L1CA/G1的±25 Hz限幅只作用于未同步LongFast的DFT鉴频结果，限制单次大噪声观察进入FLL的幅度；不拒绝观察，不限制真实频差，也不是FCW每次允许变化的幅度。同步后解除此限幅；同步器的一次性校频仍独立执行，不套用该限幅。

量化相关恰好为零时，该间隔频差贡献为0，分母仍为本组全部20/160个间隔，避免放大其余观察；整组均无可计算相角时不发布有效频差。B1I牵引鉴频禁止枚举NH码起点；位同步器自身的边界搜索仍按2.2节独立执行。

### 2.2 同步搜索与确认

| 信号 | 开放条件 | 搜索方法 | 独立确认 |
|---|---|---|---|
| L1CA、B1I D1、G1 | 首个完整载波观察后开放，不再等待15.625 Hz居中门 | 11个频率×20个位边界；频率间隔12.5 Hz、范围±62.5 Hz；按20 ms模板相干、跨位累加功率 | 1/2/4 s分级搜索，独立2/4 s确认；各窗加19 ms边界余量，质量判据不变 |
| L5 | 温启动移交频差在±12.5 Hz内 | 20个导频二次码位置，不设频率候选；20 ms×50功率 | 1 s搜索＋独立1 s确认 |
| B2a、E5a | 同L5 | 100个导频二次码位置，其余同L5 | 1 s搜索＋独立1 s确认 |
| E1 | 载波观察已建立、相关峰选择及细码居中完成 | 25个二次码位置，不设频率候选；20 ms×50功率 | 1 s搜索＋独立1 s确认 |
| B1C | 同E1，并保持宽峰载波资格 | 1800个二次码位置，不设频率候选；20 ms×900功率 | 18 s搜索＋独立18 s确认 |

数据型搜索可提供一次候选频率校正，经控制路径调整FCW并重建观察；确认阶段不重复校频。五种导频同步不提供校频量。同步失败后按原入口重试，中弱牵引不因此退成强入口。搜索与成功候选的确认数据不复用；导频失败搜索允许滑动，不等于载波窗口滑动。

数据型搜索和确认期间FLL/DLL持续运行。只有同步工作副本换算到各窗首条PDI的实际NCO参考，普通相干积分不再旋转；控制端将同步结果换算为“窗参考频率＋同步频差－当前控制频率”，避免重复叠加FLL已经完成的校正。校频只改FCW、不重装相位，确认窗从校频生效后的新PDI开始。独立确认后才擦NH/meander、对齐20 ms位内观察并切换NWPR。

L1CA/B1I D1/G1从捕获移交至独立确认总计20 s，搜索失败、扩窗及确认均不重置。跨过时限的首个完整PDI仍未确认时，`DataTracker`进入Timeout并停止新观察和控制，由上层停止硬件及重新安排捕获；边界上已经完成确认时按成功处理。同步器内部Failed仍允许重试，但不能绕过通道时限。该20 s规则不外推到B1C的18 s搜索＋18 s确认。

## 3 同步后的载波与相位处理

### 3.1 各状态的鉴频器

| 状态与职责 | L1CA、B1I、G1 | L5、B2a、E5a、E1、B1C导频 | 载波更新 |
|---|---|---|---:|
| ShortFast：快速收敛 | 若已同步仍处于本状态，使用20 ms×1功率、11点DFT、间隔10 Hz；未同步见2.1节 | 连续20 ms，11点DFT，间隔10 Hz | 20 ms |
| ShortNormal：正常收敛 | 20 ms×1功率，11点DFT，间隔10 Hz | 相邻20 ms擦码块atan2；DFT并行保留作回退及中心信噪比统计 | 20 ms |
| ShortSlow：强信号精跟踪 | 同上 | 同ShortNormal | 20 ms |
| LongFast：中弱牵引／恢复 | 20 ms×8功率，11点DFT，间隔10 Hz | 连续160 ms，11点DFT，间隔3.125 Hz | 160 ms |
| LongNormal：中弱收敛 | 同LongFast | 同LongFast | 160 ms |
| LongSlow：弱信号保持 | 20 ms×16功率，11点DFT，间隔10 Hz | 连续320 ms，11点DFT，间隔1.5625 Hz | 320 ms |
| MidDynamic／HighDynamic：动态恢复／跟随 | 同LongSlow，使用各自环路系数 | 同LongSlow，使用各自环路系数 | 320 ms |
| Sensitivity：自主极弱保持 | 20 ms×16功率，固定0及±5 Hz三支路FMS | 连续320 ms，0及±1.5625 Hz三支路FMS | 320 ms |
| BitAiding：已知bit低动态辅助 | 擦除已知bit，120 ms×5功率，0及±5 Hz三支路FMS | 导频已有已知符号，通常使用自主长相干路径，不据此推定辅助资格 | 600 ms |
| OutdoorBitAiding：已知bit动态辅助 | 擦除已知bit，100 ms×3功率，11点DFT，间隔5 Hz | 同上 | 300 ms |

11点对称网格的范围为±5个间隔，例如10 Hz间隔对应±50 Hz。导频相邻20 ms块鉴频首次需要40 ms，随后每20 ms更新。数据型先擦G1的meander或B1I的NH码，仍不能跨未知导航bit连续相加；导频擦除二次码后才采用上述长相干。

### 3.2 鉴相器与环路控制

| 信号／条件 | 鉴相器 | 是否用于控制 |
|---|---|---|
| L1CA、B1I、G1，导航bit未知 | 20 ms位内相关值的Costas鉴相，半周周期 | 仅ShortSlow且PLL进入条件满足时参与 |
| 五种已同步导频 | 20 ms擦码相关值的四象限atan2鉴相，一周周期 | 同上；长窗相位诊断不等于开放长周期PLL |
| 已知bit辅助 | 擦bit后的atan2可形成相位观察 | 辅助状态仍只使用FLL，不注入相位 |

当前采用SDR式共享状态FLL辅助PLL，已写入重构模块6。未开放PLL时只使用FLL；开放且有新相差时，默认以0.25 Hz辅助FLL共同更新频率和变化率；辅助带宽设为0可使用纯PLL。无新相差但有新频差时使用纯FLL，两路均无新结果则保持。模式选择由上层提供，不在滤波器中新增门限或固定过渡时间。

共享状态在退出PLL、切换带宽时保留；删除旧逐PDI频率预测，避免重复推进趋势。`update_rate`保留“频差只校频／同时写入变化率”的已有选择，不按频差大小另设门限。同步校频同时修改频率积分和输出。旧完整引擎的独立环仍仅用于运行基线与历史对照。

PLL带宽按当前分量NWPR报告选择：小于36 dB-Hz为2.4 Hz，36～<38为4.8 Hz，38～<42为7.2 Hz，42及以上为9.6 Hz；不越过ShortSlow及PLL进入条件。重构模块7.2的自动选择现已接入三种数据型完整PDI通道，五种导频通道尚未接通。芯片提供C/N0分档，四个浮点噪声带宽沿用本地映射；实验中的“数据固定4.8 Hz、导频固定2.4 Hz”不是当前调度方案。

### 3.3 普通状态时序与带宽

| 状态 | 载波周期 | 本链递进 | FLL参数／Hz | DLL带宽／Hz |
|---|---:|---|---:|---:|
| ShortFast | 20 ms | 数据型可在同步前累计400 ms；导频先等同步再累计400 ms，之后进入ShortNormal | 6.0 | 2.0 |
| ShortNormal | 20 ms | 独立同步确认后累计1000 ms，进入ShortSlow | 4.0 | 0.8 |
| ShortSlow | 20 ms | 按新中心SNR计数切LongSlow | 2.0 | 0.1 |
| LongFast | 160 ms | 同步后累计800 ms且细码观察已建立，进入LongNormal | 3.2 | 1.0 |
| LongNormal | 160 ms | 累计1600 ms，进入LongSlow | 1.6 | 0.5 |
| LongSlow | 320 ms | 按当前NWPR计数恢复ShortSlow | 0.7 | 0.1 |

FLL参数已按共享递推使用旧标称带宽的2倍；BOC同步前20 ms主码路径例外，保留3 Hz。DLL采用第4节实际更新周期，BOC ShortFast为160 ms。G1的800/1600 ms沿用当前完整工程统一值，不是芯片专属1250/3000 ms表的直接移植。

状态只在完整观察结束后推进，当前观察先按旧状态控制，下一完整块使用新方法；切换保留共享频率和变化率。Search、Confirm及Failed重试不等于真正失步，重复未锁定通知不重置状态。只有已锁定后失步才按当前短／长链回ShortFast／LongFast，同步重新成立后重新计时；不保留旧框架的PDI异常、命令确认和通用候选配置。

上述失步回落是状态机已有接口；当前`DataTracker`尚未接入同步后自动失锁监视和重捕获调度。Locked表示边界曾经确认，不代表此后始终保持相关；20 s初始同步时限也不代替同步后失锁检测。

## 4 码鉴别器与积分

| 信号／阶段 | 码鉴别方法与积分 | DLL更新 |
|---|---|---:|
| L1CA、B1I、G1、L5、B2a、E5a，同步前强入口 | 每1 ms先做E/L幅度鉴别，再平均20次 | 20 ms |
| L1CA，同步前中弱入口 | 10 ms相干后取幅度，累加16块，归一化E/L鉴别 | 160 ms |
| G1，同步前中弱入口 | 5 ms相干后取幅度，累加32块，归一化E/L鉴别 | 160 ms |
| B1I、L5、B2a、E5a，同步前中弱入口 | 每1 ms相关功率先开根号，再累加160项幅度，最后做E/L鉴别 | 160 ms |
| 前述六信号，同步后普通状态 | 20 ms块幅度累加；Short为1块、LongFast/Normal为8块、LongSlow及动态档为16块；E/L或五抽头双差 | 20／160／320 ms |
| E1、B1C，同步前后细码 | ASPeCT双副本点积鉴别；分别按4 ms、10 ms主码形成点积与功率，再跨块累加 | ShortFast及LongFast/Normal为160 ms；ShortNormal/Slow为20 ms；LongSlow及动态档为320 ms |
| L1CA、B1I、G1，Sensitivity | 20 ms块幅度，最近600 ms统计；E/L或双差 | 320 ms |
| L5、B2a、E5a，Sensitivity | 专用5 s相位模型补偿长相干，E/L幅度鉴别；不是普通20 ms分块路径 | 320 ms |
| E1、B1C，Sensitivity | 继续主码分块ASPeCT，最近600 ms统计 | 320 ms |
| 数据型BitAiding／OutdoorBitAiding | 擦bit后120 ms×5／100 ms×3块幅度，E/L或适用状态的双差 | 600／300 ms |

普通六信号在ShortSlow、LongSlow、Sensitivity、OutdoorBitAiding且环路C/N0>13.5 dB-Hz时选择五抽头双差，其余使用E/L；L5类Sensitivity专用码路径固定E/L。BOC不套用这项选择，抽头始终为−0.50、−0.25、0、0.25、0.50 chip。

已接通的三种数据型码环独立于同步运行，当前IQ验证使用内抽头±1/3、外抽头±2/3 chip。DLL换带宽只改系数，保留积分偏置及当前输出；下一新观察使用新比例增益。不再将最后一次比例误差差额并入积分，避免窄带阶段长期保留换档时的一次噪声；不重装码相位。

BOC粗码先进行三方向比较、独立确认及有限调码，再进入细码。E1累加8个160 ms子窗后判断方向，B1C每160 ms判断方向，两者均需两次独立同向确认；这部分仍为完整引擎参考，尚未接入重构通道。普通六态的方法选择已统一为重构幅度法，不再把旧完整引擎同步前的功率路径列为当前选择。

## 5 重要门限

本节PLL条件、普通六态切换及数据型同步开放已在重构实现；BOC粗细码及极弱／辅助条件仍列完整引擎参考。C/N0判据使用环路分量估计，不可直接拿复合信号总功率代入；L5类单导频与复合总量相差约3 dB。

重构载噪比统一采用下表规则。C/N0始终指当前闭环分量，不补偿码偏或残余频差造成的相关损失；1 s／5 s表示功率统计时长，不表示相干积分时长。同步确认和擦码由上层提供，新方法从下一完整块开始，切换时清除历史。

| 载噪比阶段 | 方法与窗口 | 报告规则 |
|---|---|---|
| 同步前，全部八信号 | M2M4：逐1 ms功率及功率平方形成二阶／四阶矩；首报后独立1 s统计 | 首次200 ms、满1 s及以后每1 s；首报仍参加首个完整秒；无法分离正信号和正噪声时为NaN，不套用NWPR固定值 |
| 同步后，全部八信号 | NWPR：逐块求窄宽功率比再平均；块内先擦已知符号，不跨未知导航bit；本次20 ms为主、10 ms另验 | 同样首次200 ms、随后每1 s；当前1 s报告≤18 dB-Hz转长窗、≥22转短窗，中间保持；初始长窗，使用已有1～5个完整秒 |
| NWPR低值输出 | 平均功率比R<1报10 dB-Hz；R=1或可计算结果低于8时报8 dB-Hz | 同时保留原始估计；R≥块内点数等不可计算情况仍为NaN；8/10不表示实际精度或信号存在 |
| NWPR衰落滞后处理 | 长窗报告≥20 dB-Hz，且高于当前1 s至少4 dB时，报告两者dB均值 | 不改相干块长；原始字段仍保存所选长窗估计 |

首报、五秒历史、固定输出及长短窗处理机制已核对芯片`evalu_cn0`；20±2 dB-Hz数值沿用完整工程映射，本次未解析到芯片对应宏的数值定义，不宣称18/22是已确认的芯片宏展开值。M2M4是本次授权的同步前方法，不是芯片该函数已有实现。

芯片同步前也有载噪比来源：捕获移交按档位填写参考值，位同步搜索完成后利用最佳候选相干功率与宽带功率估计C/N0，并在判断同步是否通过之前更新通道值。同步成功后才启动常规NWPR持续更新。此项源码核对不自动替换当前重构已采用的同步前M2M4。

`raw_db_hz`为所选窗口原始估计，`db_hz`为应用上述规则后的报告，线性`snr`与报告一致。22 dB-Hz的1 s M2M4在32种子中仅21个有定义且中位偏高3.16 dB，同步后NWPR为32/32且中位偏差−0.41 dB；不能直接把同步前M2M4高值作为强信号或PLL切换依据。3/4 dB-Hz的固定报告更不能用于灵敏度资格判断。

频差按新观察时长求绝对值均值；相差按新观察时长求未限幅RMS，不减均值。两者使用独立窗口，完整发布后清空；只输入新数据，不重复累计旧结果。模块7.2已接入相差折返、限幅和累计判决，不新增单次频差、相差或有效率拦截。

| 用途 | 当前条件与含义 |
|---|---|
| 数据型同步开放 | 首个完整载波观察完成；相邻鉴频首次21/161条PDI、后续20/160条更新，DFT首次160条；不设置数值居中门 |
| 数据型牵引与确认总时限 | 捕获移交起20 s；重试不重置，跨过时限的首个完整PDI仍未确认则退出；确认已完成优先于超时 |
| L1CA/G1中弱同步前鉴频限幅 | DFT寻峰及插值后输出限于±25 Hz；频率搜索仍为11点、±62.5 Hz，不用于同步校频或同步后观察 |
| 全部同步峰值比 | ≥1.18。数据型比较获胜频点上的主边界与相隔10 ms的边界；导频比较主位置与其余位置最大值，分母定义不同 |
| 数据型同步统计量 | 线性C/N0（Hz）×“窗口长度／500 ms”≥370，不是370 dB-Hz；L1CA/G1另要求峰两侧±1 ms处功率分别高于±2 ms处，B1I不做此峰形检查 |
| BOC同步前鉴频 | 已停用0.65集中度及0.14主码相干比拦截；不因此删除码峰和同步判决 |
| 已同步导频块间鉴频 | 归一化共轭积合成幅度≥0.10；未通过时使用有效DFT回退。单对已知符号相关的该比值为1，不是单次相差门限 |
| BOC粗码 | 三方向对比度≥0.10；E1另要求去噪后的主组功率≥其噪声标准差的4倍；保留独立确认 |
| BOC细码 | 首次试探仅将绝对码差≤0.20 chip的观察送DLL；至少400 ms监测的均值绝对值及RMS均≤0.10 chip后开放同步 |
| BOC有限调码 | 每次≤0.10 chip，160 ms完成；累计同向完成E1 4次／B1C 3次可请求细码试探，不代替居中检查 |
| PLL进入 | 同步锁定且处于ShortSlow；每400 ms新频差绝对值均值<10 Hz，连续3批通过；当前NWPR满足G1>30 dB-Hz、其余>28 dB-Hz；第三批完成后，下一次控制开放PLL |
| PLL限幅与回退 | 按鉴相周期处理折返，先统计原相差，再将反馈限幅至±67.5°；200 ms相差RMS>45°时，当前末次相差仍限幅反馈，下一次转FLL并重新确认；不使用单次频差<1 Hz入口限制 |
| 强弱状态切换 | ShortSlow每个新20 ms中心频点的1 ms等效SNR<0.631时加1，否则减1，达到4转LongSlow；LongSlow每个320 ms更新检查最新NWPR，≥32 dB-Hz加1，否则减1，达到21回ShortSlow；不是捕获档位切换 |
| 极弱／辅助切换 | 普通长／动态档≤18 dB-Hz累计6次进入Sensitivity；Sensitivity>24累计26次回LongSlow、≤13累计16次且辅助有效进入BitAiding；BitAiding>15累计6次回LongSlow |

状态切换还受同步、码稳定、动态及驻留条件约束，不能仅由C/N0推算必处于哪个状态；上表累计次数不全部要求连续，不满足时按代码递减或清零。PLL的28/30 dB-Hz来自芯片条件，现代导频统一28 dB-Hz是本地映射，不宣称为芯片专属参数。0.631是线性信噪比：按1 ms换算约28 dB-Hz，不是新增的0.631 dB门限。

ShortSlow没有新中心SNR时保持计数，不能用1 s NWPR的`snr`重复计四次；LongSlow允许保持同一份最新NWPR，在21个320 ms更新后恢复，不要求21次独立载噪比报告。M2M4不用于长环强恢复；极弱／辅助出口暂未接入普通六态。

PLL工作后，C/N0下降或单次频差超过10 Hz本身不立即截断相差，累计相差及上层状态许可决定退出。状态不允许时立即禁PLL并清除质量统计；新NWPR未定义时保持带宽，但不能用于新的进入确认。进入、回退或换带宽均保留共享环路积分，不搬入芯片独立积分清零；无新相差但有新频差时按模块6使用纯FLL，观察空档不重复累计统计。

## 6 重构采用进度与待接入项

| 项目 | 当前进度与证据边界 |
|---|---|
| SDR式共享状态FLL辅助PLL | 已采用，重构`2efc360`完成核心及模块验证；前期完整引擎170/185、最终6条IQ补测6/6保留为历史依据，不算作重构新结果 |
| 载噪比与频相统计 | 重构`bf3b389`已完成同步前M2M4、同步后NWPR及芯片式发布机制；保留`f6a83cf`频相统计，不改鉴别器、同步器或控制状态 |
| PLL质量与带宽调度 | 模块7.2已实现，三种数据型完整通道已接入ShortSlow许可、累计判决及NWPR带宽调度；导频通道待接入 |
| 普通六态与八信号选择 | `5e26b7a`完成`track_state`／`track_method`，覆盖时序、强弱边、失步恢复及方法选择；没有Sensitivity、bit辅助或动态占位出口 |
| 数据型完整流程 | `d58d132`接通L1CA/B1I D1/G1；合范围量化IQ 288/288正确同步并达到末段频码指标；PDI基础模型2304/2304正确同步、2302/2304末段稳定，2例G1失稳保留；192例强弱阶梯切换通过 |
| 普通载波观察器 | `49a1819`完成两处移植修复：LongFast同步前统一禁止新频差写入趋势，相邻Prompt恢复20/160个连续间隔。E1中弱组频差RMS中位数196.925→5.873 Hz；带噪声仍654/1088达到固定状态精度判据，434例超限保留，不能宣称全部未同步保持已通过 |
| B1I历史证据与零相关平均 | `4f62a5a`修复零复积出现时按有效间隔重新归一化的差异；历史“strict-nosync”16/16实际上包含1/2/4 s联合校频，撤回其作为160 ms asin独立保持的依据。正常完整流程通过记录保留，未同步长期保持问题尚未解决；NH枚举候选已否决 |
| B1I二象限atan实验解释 | 未采用。按完整芯片源码中两参数CORDIC的反正切运算，将相邻点积/叉积接入atan作隔离验证；补接点积仍是转录恢复假设，不冒充已核实原调用。两种方法正常同步均4/4通过，但关闭全部同步校频后atan四例频差RMS均更大；正式平方asin和全部门限不变 |
| L1CA/G1芯片参考独立保持 | 08e显示±25 Hz输出限幅降低抖动；08f已采用这一项。芯片不同频率网格、带宽及高阶积分候选未采用，正式11点×12.5 Hz及FLL递推保持不变；未将同步成功等同长期独立保持资格 |
| L1CA/G1限幅及同步频差容忍度 | 08f采用限幅，旧完整IQ双臂各64/64正确同步；08g进一步验证并取消15.625 Hz开放门，增加同步窗参考换算及20 s总时限。同步峰值比、质量和独立确认不变 |
| BOC同步前1 ms鉴频 | 历史提交`5f654f1`确实使用过；当前普通六态选择明确保留E1 4 ms／B1C 10 ms主码，未切回1 ms |
| BOC同步前载波候选对照 | `5a37365`完成七候选隔离实验；后续`4063551`已接真实细码闭环和同步验证，正式选型仍未改变。1 ms方法噪声代价明显，主码分块DFT及25 Hz限幅继续作为组合候选 |
| BOC细码闭环及相干接管候选 | `4063551`支持同步前主码分块DFT、强入口160 ms码观察使用1 Hz DLL、同步后LongFast/Normal先20 ms×8功率DFT，再由LongSlow恢复320 ms长相干；基础与独立种子模型各288例完整通过，弱入口IQ分阶段接管20/20。只完成主峰附近细码验证，粗码、旁峰消歧及生产退出仍未实现，候选尚未写入正式方法表 |
| BOC强入口统一20 ms候选 | `9e2659e`补齐码环周期对照：优先使用ShortFast载波20 ms、码环20 ms、DLL 1 Hz，替代上一轮候选的强入口160 ms码环；弱入口160 ms及其余载波／同步方法不变。强入口模型672/672、量化IQ 20/20通过；20 ms／2 Hz另有1例IQ未确认。仅完成实验，正式方法表仍未修改 |
| 六信号码环统一幅度法 | 已进入普通六态选择，三种数据型已接入CodeObserver；修复换带宽的比例噪声转存后，1152例隔离模型完整通过从950增至1152，不改变积分时长、鉴别器及带宽；其他信号完整通道待验证 |

## 7 更新记录与依据

BOC最新周期验证见[08j强入口20 ms码环对照](https://github.com/showgnsser/StarTrack/blob/9e2659e/docs/experiments/08j-boc-code-period.md)。当前推荐候选为ShortFast载波环20 ms、码环20 ms、DLL带宽1 Hz；LongFast载码160 ms不变，其他状态及08i载波／同步方案不变。E1每20 ms累加5个4 ms主码块，B1C累加2个10 ms主码块的ASPeCT点积与功率，再鉴别和更新DLL；不是连续20 ms跨未知二次码相干。1 Hz适用证据仍限于本轮主峰附近、0/±1 Hz/s范围。

20 ms／1 Hz的强入口模型672/672、量化IQ 20/20正确确认且达到末段指标；真实IQ末段码差RMS不超过0.00417 chip、频差RMS不超过0.194 Hz。20 ms／2 Hz的模型同样通过，但IQ为19/20，另有82.4 s迟同步案例，因此不把两种带宽合并宣称通过。现有0.10 chip码居中条件对短观察的测量噪声更敏感：E1 32 dB-Hz两例同步由对照的2.48/2.96 s延后至4.40/5.60 s，真实码环仍稳定。本次不降低该门限、不增加滤波或确认规则。正式表格尚未采用此候选；实验结果不替代粗码、旁峰消歧及生产退出验证。

BOC上一轮闭环验证见[08i细码闭环、同步与长相干接管](https://github.com/showgnsser/StarTrack/blob/4063551/docs/experiments/08i-boc-sync-flow.md)。该轮组合尚未正式采用：同步前E1 4 ms、B1C 10 ms分块功率DFT，强20 ms、弱160 ms更新，11点×12.5 Hz、范围±62.5 Hz、插值输出限于±25 Hz；强入口160 ms DLL复用1 Hz，后续20 ms码环证据见08j。同步后LongFast/LongNormal先以20 ms×8功率DFT、11点×10 Hz、范围±50 Hz完成原800/1600 ms收敛，再在LongSlow采用320 ms连续相干、11点×1.5625 Hz。峰值比1.18、二次码窗口、PLL条件及状态时间不变，没有新增同步校频或频差门限。

新增真实IQ案例在正确确认E1二次码时仍有26.53 Hz频差，超出原160 ms长相干DFT的±15.625 Hz接管范围。相同输入和同步结果下，分阶段接管使末段频差RMS由约879 Hz降至0.129 Hz，码差RMS由16.14 chip降至0.00937 chip；20个弱入口IQ由19/20改善到20/20。强入口1 Hz DLL另解决了B1C等待确认期间码门反复重置的问题。基础及独立种子模型各288/288正确同步并达到末段指标，强弱变化48/48，纯噪声48例无锁定；±0.5 chip细码直入48例未通过，必须保留粗码和旁峰消歧为独立待完成项。90 s仅为实验预算，不是新生产超时，所有原始失败保留。

BOC观察隔离依据见[08h同步前载波对照](https://github.com/showgnsser/StarTrack/blob/5a37365/docs/experiments/08h-boc-pull-compare.md)。正式方案仍为前表中的4/10 ms主码平方atan；候选尚未采用。历史1 ms方案是Prompt平方共轭积合并后atan2，原开发证据还使用不同环路时标和同步校频，不能直接外推到当前32/35 dB-Hz及160 ms弱入口。新实验中1 ms方法虽扩大频差主值范围，噪声代价明显；主码分块DFT保持20/160 ms更新、11点×12.5 Hz，不枚举二次码边界。

08h量化IQ的12个输入各广播五臂，共60次有效隔离运行。B1C限幅DFT末段频差RMS为2.29～3.41 Hz，20 ms理想擦码信号功率保留98.5%以上；E1弱入口两例RMS仍为17.95/20.24 Hz，功率保留约70.6%/67.6%。该轮码轨迹由测试侧预设保持，没有运行DLL或同步器；不能称为完整码载牵引通过。首次60次运行因主码启动位置及固定码率夹具问题被单列，全部输入修正后重跑。08i已经取消预设码轨迹，以实际ASPeCT和DLL闭环形成后续证据，不能混用两轮指标。

最新方案见[三种数据型牵引及同步](https://github.com/showgnsser/StarTrack/blob/d58d132/docs/data-tracking.md)，验证见[08g完整流程实验](https://github.com/showgnsser/StarTrack/blob/d58d132/docs/experiments/08g-data-pull-flow.md)。288例合范围量化IQ覆盖32/35 dB-Hz及22/24/22 dB-Hz牵引灵敏度、初差±62.5 Hz、非零码差至±0.25 chip、0/±1 Hz/s，均在20 s内正确确认并达到末段指标；强入口同步约3.060 s，弱L1CA/G1约7.218 s，弱B1I中位3.200 s、最长7.219 s。不能将本批100%当成任意场景零失败的保证。

基础PDI模型2304例均正确同步，但G1种子2026094636、2026094811同步后失稳；±5 Hz/s另有L1CA/G1各1例20 s超时，均保留。独立禁止同步时，B1I真实IQ两例频差RMS仍约31～35 Hz，不能宣称长期纯asin精确保持已解决。首次适配器位宽错误6例，以及误生成D2的20例输入均独立记录；20例D2按PRN+24系统更正为D1，未按结果筛种子。当前不覆盖TCXO、多径、极弱辅助、新版FPGA RTL及同步后失锁调度，机器摘要`qualification_complete=false`。

历史接口验证见[同步判决到状态的接口实验与PLL复测](https://github.com/showgnsser/StarTrack/blob/373d428/docs/experiments/07e-validation.md)，其中保留原失败清单和当时未验证范围。

实际鉴频接线见[普通载波观察器实验](https://github.com/showgnsser/StarTrack/blob/1d20575/docs/experiments/08a-carrier-observer.md)。固定Fast噪声响应和导频atan模糊仍需处理；同步后20 ms相邻atan只有±25 Hz主值范围，现有0.10合成比不能判断频率混叠，并行DFT也不等于自动解除模糊。本次未新增切换门限，未改变频率中心补偿决定；观察器保留参考信息，是否外推由后续控制接线明确。

当前修复与不利结果见[同步前趋势许可及相邻间隔修复](https://github.com/showgnsser/StarTrack/blob/49a1819/docs/experiments/08b-carrier-import-fix.md)。未同步也应稳住环路的原要求保持不变；B1I中弱组中位频差RMS17.415→23.136 Hz，L1CA/G1中弱组不变，仍需继续定位，不能只用计数修复或无噪声通过代替保持验证。

B1I后续核对见[历史证据与零相关平均修复](https://github.com/showgnsser/StarTrack/blob/4f62a5a/docs/experiments/08c-b1i-history-audit.md)。旧完整流程B1I 16/16、全输入NH起点40/40的摘要和runner指纹与历史记录一致，但这些试验运行了位同步。另16例“strict-nosync”全程未锁定，却仍有1/2/4 s校频；不能作为纯160 ms asin的证据。本次旧完整引擎正常同步10/10通过；两例接入重构asin后的完整结果摘要与旧实现相同。关闭全部同步搜索和校频后仍有大频差，不能用固定Fast的2 Hz精度超限概括为完整牵引失败，也不能将独立保持问题宣称已经解决。

后续每次更新同时修改正文和本节，只记录实际变化：日期、信号／状态、旧方法→新方法、原因、量化收益或无性能变化、代价／未验证范围、代码提交与实验链接。未采用的实验只更新第6节；不把探索结果自动写入主表。

芯片跟踪源码按完整源码处理，局部转录矛盾不再解释为代码缺失。[B1I二象限atan上下文解释实验](https://github.com/showgnsser/StarTrack/blob/2373e8b/docs/experiments/08d-b1i-atan-context.md)保留推断与已证实内容的边界：180窗数学检查通过，1280含噪声/320无噪声模型在Release与Sanitizer下一致；两例24 dB-Hz关闭同步后的频差RMS由31.21/102.50 Hz增至41.16/179.65 Hz，因此未替换现有平方asin。

[L1CA/G1芯片参考独立保持实验](https://github.com/showgnsser/StarTrack/blob/47c7a26/docs/experiments/08e-l1ca-g1-chip-hold.md)按普通多系统DFT路径验证，不把FMS条件分支混入。24组60 s及两条300 s共104次接收机运行，同IQ、全程无同步和PLL、固定LongFast/160 ms。仅加±25 Hz限幅后，L1CA/G1频差RMS中位数从14.33/33.57 Hz降至11.41/21.69 Hz；芯片参考组合为16.43/22.80 Hz，各信号每臂均0/12达到历史RMS≤5 Hz/P95≤10 Hz参考指标。300 s未出现持续单向发散，但芯片参考臂最大频差达85.51/132.11 Hz，不能恢复独立保持通过声明，也不外推为强入口或所有芯片模式失败。

[限幅采用与同步频差实验](https://github.com/showgnsser/StarTrack/blob/1d8e1c1/docs/experiments/08f-pull-clip-sync.md)保留统一11点、±62.5 Hz。模块测试的±3/±12.5/±25/±31.25/±50/±62.5 Hz各100例均正确确认，搜索后的校频保持启用；两次错误搜索候选被独立确认挡住。完整IQ每信号32例、每例60 s，限幅前后均无误锁，G1同步时间中位数15.48→12.75 s、最大59.59→41.16 s，并非每例更快。限幅后有效搜索结束前1 s的真实频差RMS中位数为L1CA 9.83 Hz、G1 18.07 Hz，随后均正确确认，不能再把5 Hz RMS当成所有位同步成功的必要前提。该证据不等于可以取消居中开放条件，也不证明任意快速频率摆动或范围外残差可同步。完整IQ每档仅8例且跨频差复用种子，不将8/8解释为总体成功率的保证。

| 日期／版本 | 修改内容 | 原因与收益 | 验证与边界 |
|---|---|---|---|
| 2026-09-13／V1.0 | 建立八信号、十一状态的当前总表；区分默认完整实现、共享环路候选和空分支重构 | 统一方案查询入口，消除“已测试即已采用”的歧义；未改变算法，无新增性能收益声明 | 逐项核对`cac4896`的运行路由、状态表与质量模块，以及`cd0c2dc`重构进度；纯文档检查，不重跑仿真 |
| 2026-09-13／V1.1 | 重构载波由独立FLL/PLL改为共享状态联合，默认辅助带宽0.25 Hz；移除独立预测及清零交接 | 落实已确认的统一结构，持久double字段12→9；不宣称所有带宽性能更优 | `2efc360`：12000次数学调用对照通过，Release及ASan/UBSan各24/24；2.4/4.8 Hz测量级1007/1024（旧1012），新增13失败、恢复8例；1.2 Hz压力组明显退化并保留记录；未接入质量策略和完整通道 |
| 2026-09-13／V1.2 | 新增统一C/N0、频差绝对值均值及未限幅相差RMS统计，只在新窗口完成时发布 | 为质量判决提供明确测量，避免缓存重复累计及固定载噪比下限混淆；未改变闭环策略，无新增闭环收益声明 | `f6a83cf`：Release及ASan/UBSan各26/26；正常强弱相关模型32组、2048窗口，最大中位偏差0.25 dB、绝对误差P95 0.94 dB；低C/N0、纯噪声与频差偏差原样保留 |
| 2026-09-13／V1.3 | 同步前采用M2M4，同步后NWPR；接入200 ms首报、每1 s发布、最近1～5 s选择及8/10固定输出，原始值独立保留 | 统一八信号同步前后统计流程，复用芯片报告机制；不把固定输出当作精度收益，也不新增PLL门限 | `bf3b389`：Release及ASan/UBSan各28/28；672例、6720份报告，两种构建一致，旧原始NWPR矩阵输出不变；22 dB-Hz同步前M2M4偏差与未定义问题保留，未验证完整通道控制收益 |
| 2026-09-13／V1.4 | 接入PLL累计进入、相差折返／限幅、累计回退和C/N0带宽调度；进入／回退从下一次更新生效 | 将已有芯片式条件统一交给策略模块，保持共享递推与0.25 Hz辅助；不增加门限，不用M2M4高值开放PLL | `2230ad8`：Release及ASan/UBSan各30/30，测量级733/768通过；静态和强度阶梯全部通过，35例动态／恢复失败均因PLL占比不足，其中18例相差也超实验判据；频差RMS均<2 Hz，未证明完整接收机资格 |
| 2026-09-13／V1.5 | 文件`freq_spectrum`→`dft_freq`、`carrier_stats`→`carrier_error`、`pll_policy`→`pll_switch`；头文件、测试及引用同步调整 | 名称直接表达鉴频、误差统计和PLL切换职责；类名、接口、算法及门限不变，无性能收益声明 | `a487fd4`：Release 30/30，风格及依赖检查通过；768例完整PLL矩阵与改名前逐字节一致，原35例失败不变；历史记录保留当时文件名与指纹 |
| 2026-09-13／V1.6 | 集中普通六态及八信号方法选择；区分搜索失败与真正失步，长链恢复LongFast；统一幅度码路径写入当前选择表 | 按用户确认范围先实现普通状态，去掉旧异常／命令确认框架，避免状态条件散落；未增加门限或宣称性能提升 | `5e26b7a`：Release及ASan/UBSan各33/33；48个同步后组合、19个同步前组合、24个码边界组合、128条六态轨迹通过；轨迹为显式事件测试，不是新的射频或灵敏度资格 |
| 2026-09-13／V1.7 | 补充普通状态验证证据，方法、积分长度、带宽及门限均不变 | 用真实同步判决验证失败保持LongFast及确认后切换；未伪造码就绪或同步成功，无性能提升声明 | `373d428`：Release及ASan/UBSan各34/34；有信号480/512正确同步，32例B1C在无常规FLL反馈的±1 Hz/s残余漂移下42 s未锁定；64例纯噪声产生56次错误候选、零最终锁定；PLL复测仍733/768，与原输出逐字节一致；保留原35例失败，未验证完整PDI闭环 |
| 2026-09-13／V1.8 | 新增普通载波观察器，将已选方法接到真实PDI；不修改现有选型、积分长度、环路或质量条件 | 不再用合成频差代替实际鉴频来验证这条测量链；暴露固定Fast噪声响应和导频atan模糊，无性能提升声明 | `1d20575`：两种构建各36/36；67种方法组合及参考／边界解析回归通过；无噪声272个鉴频案例最大频差RMS 0.0022 Hz，48例Hold保持；带噪声654/1088达到固定状态判据、192例Hold未评估，434例超限中50例频差RMS>100 Hz。夹具给定同步、固定状态、无码环，未通过完整通道验收 |
| 2026-09-13／V1.9 | 修复BOC中弱同步前趋势许可漏接；相邻Prompt从19/159对恢复连续20/160对，更新NCO参考和首发时刻 | 恢复旧已验证控制分工及原生相邻时序；E1中弱组RMS>100 Hz由19例降为2例，未改带宽或门限 | `49a1819`：两种构建各37/37，192窗连续时序及4800窗原生旧／新观察器比较通过；1280例带噪声中192例轨迹改变，1088例不变，总精度通过数仍654/1088；无噪声272例鉴频通过，48例Hold保持。B1I中弱中位误差增大，剩余失败保留 |
| 2026-09-13／V1.10 | B1I保持平方asin，禁止牵引鉴频枚举NH；修复零复积平均分母，纠正历史未同步证据归属 | 一次零Prompt导致的鉴频回放差异从0.043 Hz降至约0.0001 Hz；撤回秒级联合校频记录作为纯asin保持证据的解释，不改写原始日志 | `4f62a5a`：两种构建各38/38，原1280含噪声与320无噪声矩阵逐字节不变；两例完整IQ接入重构asin后结果摘要与旧实现一致。没有宣称独立保持问题已经修复 |
| 2026-09-13／V1.11 | 记录完整芯片上下文下的二象限atan隔离实验，不改变正式方案 | 数学符号不变性成立，但模型及同IQ对照未显示独立保持改善，候选不采用；不枚举NH | 实验证据`2373e8b`：180数学窗口、两构建各1280含噪声/320无噪声模型、4场景×4臂同IQ；正常同步两种方法均4/4通过，独立保持误差增大。生产代码基线仍为`4f62a5a` |
| 2026-09-13／V1.12 | 记录L1CA/G1芯片参考DFT、输出限幅及FLL递推的四臂独立保持实验；正式参数不变 | ±25 Hz限幅有明确改善，但换网格及高阶积分不能自动满足保持要求；不把观察输出限幅当成真实频差范围或质量拒绝门 | `47c7a26`：104次同IQ运行及全程隔离审计通过；16种谱峰、12个解析PDI窗、24000次FLL递推核对通过。22 dB-Hz与300 s结果仍未达原频差参考指标，不宣称资格通过 |
| 2026-09-13／V1.13 | L1CA/G1未同步LongFast的DFT插值输出限于±25 Hz；搜索网格、环路系数及同步确认不变 | 降低大噪声观察反馈；完整IQ的G1同步中位时间15.48→12.75 s，长尾59.59→41.16 s；以真实同步概率区分同步能力与同步前精度，不新增放宽门限 | `1d8e1c1`：两种构建各39/39及2600例同步矩阵一致；128次同IQ运行均正确同步，限幅后原完整指标64/64；原1280含噪声回归仅目标64例变化，320无噪声不变。重构完整通道、强入口及TCXO等资格未覆盖 |
| 2026-09-14／V1.14 | 接通三种数据型完整浮点通道；首观察开放同步、同步窗NCO参考及校频换算、DLL换带宽保留积分偏置、20 s总时限；B1I仍为平方asin | 公共衔接代替新增信号专用分支；1152例DLL隔离对照完整通过950→1152，G1等待长尾在本次完整矩阵中收敛到20 s内；未降低同步质量条件，未采用B1I平方DFT | `d58d132`：Release及ASan/UBSan各40/40、每构建3264例输出一致；合范围IQ 288/288，最终同输入6例日志逐字节一致；模型2例G1同步后失稳及压力2例超时保留，长期无同步保持未宣称通过 |
| 2026-09-14／V1.15 | 记录BOC七候选同步前隔离对照，正式方法、门限、码环及状态不变 | 确认B1C 10 ms平方atan的±25 Hz模糊与1 ms平方损失；DFT候选拓展范围并改善B1C稳定性，E1弱组仍有抖动，未用统一形式掩盖性能差异 | `5a37365`：两构建各41/41，7392例模型逐项一致，12输入×5臂量化IQ；168个解析配置2268窗通过。无同步器、无DLL、无状态切换，不宣称BOC同步资格；所有失败及无效夹具记录保留 |
| 2026-09-14／V1.16 | 记录BOC细码闭环及分阶段相干候选，正式方法表不变 | 1 Hz DLL减少强入口码门重置；同步后先宽范围收敛，避免正确同步但残差超出长相干DFT范围；不降低同步门限、不新增状态或校频旁路 | `4063551`：两构建各44/44、各3888次模型运行一致；基础及独立种子组合各288/288，弱入口IQ分阶段接管20/20，另12个强入口已通过且分阶段逻辑不影响其路径；共128次IQ运行，失败、误锁及±0.5 chip压力保留。粗码、生产超时和失锁监视未完成 |
| 2026-09-14／V1.17 | 优先候选改为BOC强入口载码均20 ms、DLL 1 Hz，正式方法表不变 | 保持统一周期，前2 s码收敛改善，末段精度相近；保留短观察触发现有码居中条件的延时，以及2 Hz失败，不为统一周期降低门限 | `9e2659e`：两构建各46/46、最终模型各4608次一致；强入口672/672及IQ 20/20，另20个弱入口四臂逐PDI一致。40输入四臂共160次IQ运行；32个历史输入对照及720条历史模型结果不变，±0.5 chip范围外压力仍不通过 |

实现依据：[鉴别路由与配置](https://github.com/showgnsser/StarTrack/blob/cac48965887405b9601d4076c2b6eeb93495c750/src/tracking/track_engine.cpp)、[十一状态及PLL带宽](https://github.com/showgnsser/StarTrack/blob/cac48965887405b9601d4076c2b6eeb93495c750/src/tracking/track_fsm.cpp)、[累计PLL质量条件](https://github.com/showgnsser/StarTrack/blob/cac48965887405b9601d4076c2b6eeb93495c750/src/tracking/carrier_quality.cpp)、[数据位同步](https://github.com/showgnsser/StarTrack/blob/cac48965887405b9601d4076c2b6eeb93495c750/src/sync/data_bit_sync.cpp)、[重构共享环路](https://github.com/showgnsser/StarTrack/blob/2efc360a58d242cd0102a571926bb750d0634a8d/src/loop/carrier_loop.cpp)、[本次重构实验](https://github.com/showgnsser/StarTrack/blob/2efc360a58d242cd0102a571926bb750d0634a8d/docs/experiments/06a-shared-carrier.md)。

统计模块：[同步前后载噪比接口](https://github.com/showgnsser/StarTrack/blob/bf3b389819a74c8423c584277ac16afec2aad978/include/quality/cn0_estimator.h)、[频相误差统计接口](https://github.com/showgnsser/StarTrack/blob/a487fd4bebc94dbebb23239b5b82f276ba347dc7/include/quality/carrier_error.h)、[本次方法与报告机制实验](https://github.com/showgnsser/StarTrack/blob/bf3b389819a74c8423c584277ac16afec2aad978/docs/experiments/07b-cn0-methods.md)、[原始NWPR统计实验](https://github.com/showgnsser/StarTrack/blob/f6a83cfaba944f3216b430cd90452d3431c15ad0/docs/experiments/07a-quality-stats.md)。

PLL切换：[当前接口与调用顺序](https://github.com/showgnsser/StarTrack/blob/a487fd4bebc94dbebb23239b5b82f276ba347dc7/include/policy/pll_switch.h)、[模块7.2实验及失败边界](https://github.com/showgnsser/StarTrack/blob/2230ad8b3941db3c1a9acf82af9bf18c47c0ec90/docs/experiments/07c-pll-policy.md)。DFT鉴频：[当前接口](https://github.com/showgnsser/StarTrack/blob/a487fd4bebc94dbebb23239b5b82f276ba347dc7/include/discriminator/dft_freq.h)；[新旧文件名对照](https://github.com/showgnsser/StarTrack/blob/a487fd4bebc94dbebb23239b5b82f276ba347dc7/docs/file-names.md)。

普通六态：[状态切换接口](https://github.com/showgnsser/StarTrack/blob/5e26b7a8df58d586766de092497f06196a2bbb1c/include/policy/track_state.h)、[八信号方法选择](https://github.com/showgnsser/StarTrack/blob/5e26b7a8df58d586766de092497f06196a2bbb1c/include/policy/track_method.h)、[本次实验与未验证范围](https://github.com/showgnsser/StarTrack/blob/5e26b7a8df58d586766de092497f06196a2bbb1c/docs/experiments/07d-normal-state.md)。

实验与设计：[[10-逐模块软件设计/实验记录-20260913-芯片质量条件与BOC强入口|芯片质量条件与BOC强入口实验]]、[[10-逐模块软件设计/20-跟踪算法设计方案-载波环更新|共享载波环结构与验证]]。旧章节用于查原理与历史，不覆盖本表标明的代码基线。
