# 当前会话反馈

## 当前代码的真实情况

当前正式代码中，强信号入口实际使用的是`asin`，不是`atan`。

强入口由`PromptFllObs::addPair()`完成。它先计算相邻1 ms Prompt的点积、叉积和幅度积：

$$
\mathrm{Pdot}=I_{n-1}I_n+Q_{n-1}Q_n,
$$

$$
\mathrm{Pcross}=I_{n-1}Q_n-Q_{n-1}I_n,
$$

$$
s=\frac{\mathrm{Pcross}}{|P_{n-1}|\,|P_n|},
$$

然后执行：

$$
\widehat{\Delta f}=
\frac{\arcsin(s)}{2\pi\alpha\times1\ \mathrm{ms}}.
$$

其中L1 C/A和G1的$\alpha=1$；B1I会先将相邻Prompt分别平方，并令$\alpha=2$。每个相邻1 ms结果先独立计算，再在20 ms内平均。

路由条件是数据型信号尚未完成位同步，并处于`ShortFast`或`ShortNormal`。因此这正是当前强捕获移交入口实际运行的鉴频器。

## 其他路径不要混淆

- 同步后的频率观察器中存在`atan2(cross,dot)`。
- 相位观察器根据信号符号是否已知使用`atan(Q/I)`或`atan2(Q,I)`。
- 这些都不是当前强入口的`PromptFllObs`。

## 当前不一致

我们已经讨论并确定的目标方案是强入口改用`atan(Pcross/Pdot)`，但该修改尚未落入正式跟踪代码。上一条回答把“目标方案”误说成“当前代码”，这是错误的。

目前应明确区分：

- 当前代码：强入口是`asin`；B1I还会先平方Prompt。
- 目标方案：强入口统一改成`atan(Pcross/Pdot)`。
- 当前软件方案文档已经写成目标方案，因此它与现有代码暂时存在待实现差异。

本次仅核对事实，没有修改正式跟踪代码。
