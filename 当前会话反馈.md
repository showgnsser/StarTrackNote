# 状态频率门限整改完成

## 1. 整改结论

已经按照A/B测试结果删除ShortFast、LongSlow和Sensitivity的状态专属频率残差拒绝门。现在的正式控制链为：

```text
频率观察器
    ↓ 功率、峰形、有限性和物理范围判决
观察中心频率重定时到当前NCO控制边界
    ↓
有效频率观察直接进入FLL
    ↓
FLL增益和趋势创新限制约束单次影响
    ↓
独立频率监测器形成均值、RMS和变化率
    ↓
FSM依据持续证据决定状态切换
```

不再使用5 Hz、10 Hz、候选差3 Hz、三次确认或`allow_recenter=false`组成第二套判断状态机。`freq_used=true`现在表示观察器判为有效的频率结果已经实际进入FLL。

PilotSync中的`freq_guard_bins`没有删除。它表示频谱峰值比较时排除主瓣附近频点的宽度，不是本次删除的状态频率残差门。

## 2. 代码修改

### 2.1 TrackEngine

修改文件：

```text
include/tracking/track_engine.h
src/tracking/track_engine.cpp
```

主要变化：

- 删除`FreqGuard`头文件依赖和`freq_guard_`成员；
- 删除`freqGuardConfig()`及LongSlow 5 Hz、Sensitivity 10 Hz等状态配置；
- `tryCenterPullIn()`在频率重定时后直接使用有效观察推进ShortFast FLL；
- `prepareLoopFreq()`在观察器质量判决和NCO时标重定时后直接设置`freq_used`并送入载波环；
- 删除同步校频、导频牵引、状态切换、策略清窗和E1载波冻结中的FreqGuard参考重置；
- 保留观察器自身范围、FLL、`FreqMonitor`、`ChipDynMonitor`和FSM，不改变其他控制参数。

### 2.2 删除旧门控模块

已经删除：

```text
include/monitor/freq_guard.h
src/monitor/freq_guard.cpp
tests/monitor/test_freq_guard.cpp
```

并从根`CMakeLists.txt`删除对应生产源和测试注册，避免仓库残留一套生产不再使用的死代码。

### 2.3 频率监测语义

修改文件：

```text
include/monitor/freq_monitor.h
src/monitor/freq_monitor.cpp
tests/monitor/test_freq_monitor.cpp
```

`FreqMonitor`仍统计有效率、均值、RMS和变化率，但“有效”现在直接指观察器物理有效，不再指通过FreqGuard。

### 2.4 测试合同

修改文件：

```text
tests/tracking/test_track_engine.cpp
tests/tracking/test_state_outdoor_bit_aiding.cpp
```

旧测试`checkShortFastFreqGuard()`要求人工注入的一次25 Hz观察必须被拒绝。它已经替换为`checkShortFastFreqFeedback()`：只要25 Hz仍处于鉴频器物理范围且观察器判为有效，就必须进入FLL并改变载波控制量。

`CHANGELOG.md`和`TASK_HANDOFF.md`已同步记录本轮设计结论、证据和剩余验证边界。

## 3. 构建与测试

最终Release构建目录：

```text
/tmp/startrack-freq-feedback-build
```

最终二进制：

```text
/tmp/startrack-freq-feedback-build/startrack
SHA-256: b22edc5fbd4e24625490ee18da6ae7c0bbaa9da2737ba4ae712ea2aa4906e6ef
```

### 3.1 定向状态测试

以下8项全部通过：

```text
test_track_engine
test_state_short_fast
test_state_long_slow
test_state_sensitivity
test_state_recovery
test_state_dynamic
test_state_weak_fade
test_source_entry_path
```

其中`test_state_weak_fade`包含约21秒的弱信号渐降闭环。

### 3.2 完整CTest

完整结果：

```text
注册测试: 112
通过: 110
失败: 1
按设计跳过: 1
```

唯一失败为`eight_signal_status`。失败原因不是算法或本轮修改，而是机器状态文件冻结的源码身份已经落后：

```text
docs/roadmap/eight_signal_status.json current.head:
98f54260f05f2176afbe36e8ba4223b6a5aaccec

当前Git HEAD:
b4613278cd900c4ebe9dcc8a2757a8def2c9675d
```

该测试按设计拒绝把旧版本资格状态冒充当前版本。本轮没有擅自改写跨任务资格状态。

`test_state_tcxo`因没有提供外部TCXO文件按设计跳过。其余C++功能、style、style_checker、dependencies和runner测试全部通过。

### 3.3 其他门禁

以下检查通过：

```text
python3 skills/startrack-cpp-style/scripts/check_startrack_style.py .
git diff --check
```

AGENTS.md列出的以下脚本在当前仓库不存在，因此无法执行：

```text
scripts/validate_fixed_catalog.py
scripts/validate_signal_campaign.py
```

## 4. 最终生产二进制真实IQ结果

### 4.1 ShortFast和LongSlow

最终二进制复跑7个代表场景，7/7通过。所有场景的同步率、频率观察有效率和使用率均为1，无PDI、命令或overflow错误。

| 场景 | 首次同步 | Doppler RMS/P95 | 码P95 |
|---|---:|---:|---:|
| L1CA 40 dB-Hz，强入口，+62.5 Hz | 3.159 s | 0.800 / 1.673 Hz | 0.0055 chip |
| B1I 40 dB-Hz，强入口，+62.5 Hz | 3.199 s | 1.303 / 2.477 Hz | 0.0037 chip |
| G1 40 dB-Hz，强入口，+62.5 Hz | 3.199 s | 0.495 / 0.913 Hz | 0.0033 chip |
| L1CA 22 dB-Hz，普通入口，−62.5 Hz | 8.659 s | 1.433 / 2.321 Hz | 0.0169 chip |
| L1CA 22 dB-Hz，普通入口，+62.5 Hz | 8.659 s | 0.945 / 2.012 Hz | 0.0338 chip |
| B1I 24 dB-Hz，普通入口，−62.5 Hz | 5.919 s | 1.810 / 2.864 Hz | 0.0225 chip |
| G1 22 dB-Hz，普通入口，−62 Hz | 8.819 s | 1.258 / 2.457 Hz | 0.0172 chip |

结果文件：

```text
/tmp/startrack-freq-feedback-iq/summary.json
SHA-256: ec9416a7ba7dcb56e2ed5793a142760e2cdfd934e67ef1256188f479d1dc8909
```

### 4.2 Sensitivity正式代表点

场景先以22 dB-Hz跟踪30秒，再在20秒内降至11 dB-Hz，计分70至100秒：

```text
Sensitivity驻留: 29.98 s
sync/freq-valid/freq-used: 1.0 / 1.0 / 1.0
Doppler RMS/P95: 1.809 / 3.125 Hz
code P95: 0.1436 chip
PDI/cmd/overflow错误: 0
```

该结果优于整改前同一场景的2.318/3.884 Hz。

结果文件：

```text
/tmp/startrack-freq-feedback-iq-sens11/analysis/tracking_metrics.json
SHA-256: bd0e907f181f5b22a8ec743154eba0b99f9d3c117d55938e17a37aec65b0b2b7
```

### 4.3 Sensitivity 8 dB-Hz压力点

```text
Sensitivity驻留: 30.00 s
sync/freq-valid/freq-used: 1.0 / 1.0 / 1.0
Doppler RMS/P95: 2.863 / 5.631 Hz
code P95: 0.0279 chip
PDI/cmd/overflow错误: 0
```

整改前基线为7.109/9.889 Hz，说明删除状态残差门后，FLL能够继续利用较大的正确纠偏量并自行恢复。

结果文件：

```text
/tmp/startrack-freq-feedback-iq-sens8/analysis/tracking_metrics.json
SHA-256: ef1e052843fad91a59d3d0f9172b72161aa1e1335ea1f82728d4368b7c65c9d8
```

## 5. 当前边界

### 已验证

- 状态专属频率门已从生产数据链彻底移除；
- 有效频率观察直接进入FLL；
- 8项状态确定性测试通过；
- 全仓算法、style、dependencies和runner测试通过，只有旧HEAD资格状态自检失败；
- 3个强入口、4个普通入口、11 dB-Hz正式代表点和8 dB-Hz压力点真实IQ通过；
- 未观察到失步、重捕、NCO发散或码环失稳。

### 尚未覆盖

- 删除门限后的正负1 Hz/s真实IQ多种子；
- TCXO真实IQ多种子；
- 强入口高动态和短时频率阶跃真实IQ；
- 300秒极弱长期保持。

因此本轮可以声明“状态频率门整改和代表场景验证完成”，但不能据此升级八信号资格状态或宣称全部动态、TCXO和耐久范围已经重跑。

## 6. 代码状态

本轮修改保留在当前StarTrack工作树中，没有提交，避免把仓库原有的大量未提交工作混入一次提交。没有覆盖或回退用户已有修改。
