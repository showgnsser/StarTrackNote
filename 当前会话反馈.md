# 当前会话反馈

## L1 C/A和G1是否使用位同步前长窗DFT

是，但只限普通中弱入口，而且与刚刚否决的B1I“2.56秒平方Prompt DFT”不是完全相同的算法。

当前三种数据型信号的普通入口关系如下：

| 信号 | 位同步前频率牵引 | 总观察窗 | 相干短块 | Prompt平方 | DFT频点 | 位同步频点 |
|---|---|---:|---:|:---:|---:|---:|
| L1 C/A | 长窗DFT反馈FLL | 1.28 s | 10 ms | 否 | 11点，正负125 Hz | 1点 |
| G1 | 长窗DFT反馈FLL | 1.28 s | 5 ms | 否 | 11点，正负125 Hz | 1点 |
| B1I | 位同步内联合搜索 | 不使用独立长窗 | 由位同步窗处理 | 否 | 位同步内11点，正负62.5 Hz | 11点 |

## L1 C/A普通入口

当前处理顺序为：

```text
连续1 ms Prompt
  → 每10 ms相干积分
  → 在1.28 s内对128个短块做功率非相干累加
  → 11点DFT选择残余频率
  → 每160 ms滑动发布频率结果
  → FLL更新载波NCO
  → 频率资格成立后开放单频点bit边界搜索
```

L1 C/A没有每毫秒NH覆盖码，所以不需要平方Prompt。10 ms短块最多只会在导航bit边界附近受到一次符号翻转影响，其余短块可以正常积累功率。

## G1普通入口

当前处理顺序与L1 C/A相同，但相干短块缩短为5 ms：

```text
连续1 ms Prompt
  → 每5 ms相干积分
  → 在1.28 s内对256个短块做功率非相干累加
  → 11点DFT选择残余频率
  → 每160 ms滑动发布频率结果
  → FLL更新载波NCO
  → 频率资格成立后开放单频点meander/bit边界搜索
```

5 ms短块用于降低G1 meander符号翻转造成的相干抵消，不使用Prompt平方。

## 与被否决B1I候选的区别

三者的上层结构都属于“先载波牵引，后位同步”，但B1I候选额外具有三个风险：

1. B1I需要对Prompt复数平方以消除NH20正负号；
2. 总观察窗由1.28 s增加到2.56 s；
3. 平方噪声、码相关损失和高度重叠长窗一起进入FLL反馈。

因此B1I失败不能直接证明L1 C/A和G1也一定失败，但它说明“长窗DFT反复反馈FLL”并不是天然正确的统一架构。L1 C/A和G1仍存在窗口高度重叠、旧样本与新NCO混合以及码频耦合的理论风险，只是没有B1I平方域和2.56 s长窗那么严重。

## 强入口不是这套结构

L1 C/A、B1I和G1的强信号入口进入ShortFast/ShortNormal，使用短周期相邻Prompt FLL完成频率牵引，不走1.28 s LongFast DFT。当前讨论的长窗DFT仅指普通中弱捕获入口。

## 当前真实状态

- B1I普通入口已经恢复为11个频率和20个NH20相位的联合位同步，不再使用位同步前长窗DFT；
- L1 C/A和G1普通入口仍保留1.28 s位同步前DFT；
- L1 C/A和G1位同步器当前都只使用1个中心频点；
- 如果后续追求三种数据型信号完全统一，需要单独A/B比较L1 C/A、G1的当前两阶段结构与“频率候选统一放入位同步”的结构，不能仅凭B1I结果直接修改。
