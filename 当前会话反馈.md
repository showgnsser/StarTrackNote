# 当前会话反馈

## DFT应当作用在哪里

你指出得对。DFT的输入应该始终是硬件输出的连续1 ms复相关结果，不应该重新接入一条1.28 s或2.56 s的“长窗粗频DFT”链。

设第\(k\)条1 ms Prompt复相关为\(P[k]\)，五个候选频率为

\[
f_m\in\{-62.5,-31.25,0,+31.25,+62.5\}\text{ Hz}.
\]

每个候选频率都先逐条旋转1 ms结果：

\[
P_m[k]=P[k]\left(\cos\theta_{m,k}-j\sin\theta_{m,k}\right),
\qquad
\theta_{m,k}=2\pi f_m t_k .
\]

这里没有把原始IQ重新做一次DFT，也没有把1.28 s或2.56 s数据线性相干。软件只对已经形成的1 ms复相关进行五种候选旋转。

## 旋转后的积累方式

对每个候选频率：

- L1 C/A：枚举20种数据bit边界，在每个完整20 ms bit内相干相加；不同bit之间只累加功率。
- B1I：先按候选NH20相位恢复公开NH符号，再在完整20 ms导航bit内相干相加；不同bit之间累加功率。
- G1：先按候选meander相位恢复公开符号，再在完整20 ms导航bit内相干相加；不同bit之间累加功率。

所以当前位同步的1 s搜索不是“1 s相干积分”。它由许多20 ms相干结果做非相干功率积累；独立确认阶段使用另一批2 s数据，同样不跨未知导航bit做线性相干。

## 刚才错误的方向

为了修复一个L1 C/A合成失败角，我刚才准备把旧的\(1.28\text{ s}\) PullInFreqObs重新接回L1 C/A。该补丁由于上下文不匹配没有应用，但这个设计方向本身也应当废弃。

旧观察器虽然内部同样从1 ms Prompt开始，并按10 ms短块相干、跨块非相干积累，但它仍然是位同步之前另一套长时间频率搜索。这样会产生两个问题：

1. 与位同步中的五点频率搜索重复；
2. B1I和G1真实IQ已经证明，前级长窗错误校频会破坏后续同步。

严格的“两层5点搜索”实测为：

- B1I代表矩阵26/27通过；
- G1最坏边界52/60通过。

因此不能冻结，更不能通过重新接回长窗来修补。

## 当前有效证据

只保留“逐1 ms旋转、频率与边界联合搜索”的五点候选时，真实IQ结果为：

- L1 C/A：3频差×3码差×3种子，27/27通过；20个bit边界，20/20通过；
- B1I：3频差×3码差×3代表边界，27/27通过；最坏角20边界×3种子，60/60通过；
- G1：同样27/27和最坏角60/60通过。

但完整CTest又发现一个L1 C/A 22 dB-Hz合成角失败：初始码差位于\(+0.5\) chip边缘时，从第一个PDI立即启动二维搜索，可能在Prompt尚未居中时接受错误频率和bit边界。这个问题不能靠恢复长窗DFT解决。

## 当前代码状态

- 实际工作树中已经放入五点联合搜索候选；
- 完整CTest为112项，其中107项通过、1项设计跳过、4项失败；
- 与本方案直接相关的真实失败是L1 C/A边缘合成角；
- 另外两项失败是旧测试仍要求普通入口先使用预同步长窗FLL；
- eight_signal_status失败来自当前脏工作树的HEAD记录不一致；
- 当前没有提交，也没有推送StarTrack代码。

## 下一步

下一步只沿正确数据链继续：

1. 保持五个候选逐条旋转1 ms Prompt；
2. 保持20 ms位内相干、位间非相干；
3. 定位L1 C/A码峰边缘时为何会接受错误二维峰；
4. 改进联合搜索的启动或失败重试逻辑，但不恢复1.28 s/2.56 s预同步长窗；
5. 三种信号定向测试和完整CTest全部通过后，才冻结并提交。
