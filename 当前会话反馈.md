# 数据型信号章节重构与5点DFT审查

## 本次保留的方案

- 文档标题统一为《GNSS跟踪软件算法设计》。
- GPS L1 C/A、BDS B1I D1和GLONASS G1合并到“数据型信号牵引与位同步”一个章节，不再拆成三个独立章节。
- 章节只保留公共处理结构、三种信号差异和公共位同步等必要层级；删除重复背景说明，重点描述输入、处理、参数和输出。
- 每个公式先说明计算目的，公式后紧跟首次出现参数的物理含义、单位和取值。
- 新流程图同时展示强入口、普通入口、码牵引、频率资格和20边界位同步。

## 5点DFT审查结论

普通入口建议由11点改为5点：L1 C/A和G1使用$[-100,-50,0,50,100]$ Hz，B1I使用$[-125,-62.5,0,62.5,125]$ Hz。

- 5点版本的13个代表场景都完成同步并通过物理精度检查。G1有一例只超过15秒测试脚本设置的12秒首次同步门，但11点同输入的同步时刻完全相同，不是5点引入的退化。
- B1I最不利的$\pm93.75$ Hz网格中点，5点在3个种子、2个方向共6例中全部通过；最晚首次同步15.079秒，最坏频率RMS约1.35 Hz、P95约2.55 Hz。
- 同一B1I $+93.75$ Hz、seed 20260901输入下，11点版本选中错误频率分支，20秒内未同步，频率RMS约318 Hz；5点版本9.939秒同步，频率RMS约0.633 Hz。
- 现有证据不支持继续保留11点。更多频点没有扩大必要的接受范围，反而增加了低载噪比下从噪声峰中选出最大值的机会。

当前生产实现仍为11点。本次只完成方案文档和A/B审查，没有修改生产代码；正式切换到5点后仍需重跑完整接受移交矩阵。

## 验证与边界

- 5点代表场景汇总：`/tmp/startrack-data-grid5-iq/summary.json`，SHA-256 `9c2f85e4392cd30b16e79469179a58dd6bbb7aed0bf089ad11f83fa211e46f1b`。
- 5点B1I网格中点汇总：`/tmp/startrack-data-grid5-holdout/summary.json`，SHA-256 `b15ec86f3e6643d6fe57c4d0599ff817e0a8d7ea0bd473f506447bdc7795db78`。
- 11点同输入对照汇总：`/tmp/startrack-data-grid11-holdout/summary.json`，SHA-256 `ea6117067f2def1e2ceadb7763b31ab5b5dd5b4f6335e590864f42af8a851ab7`。
- 文档公式围栏、链接、标题层级和diff格式已检查；新SVG已实际渲染检查，无文字裁切、重叠和连线穿字。
- 本次未修改跟踪生产代码，也未改动用户的`.obsidian`配置。

## 下一步

请先评审合并后的数据型信号章节。确认方案后，再单独把普通入口频率候选从11点改为5点，并执行三种数据型信号的完整接受移交回归。
