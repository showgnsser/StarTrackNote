# 当前会话反馈

软件方案第四章已经整体重写。

本轮保留的最终方案：

- 强捕获等级进入20 ms相邻Prompt鉴频；中、弱捕获等级进入5点功率DFT；
- L1 C/A和G1使用单位复积求和后一次四象限$\operatorname{atan2}$；
- B1I使用二倍角单位复积求和后一次四象限$\operatorname{atan2}$；
- 普通入口固定写明候选旋转、短相干、功率非相干累加和三点插值；
- 载波牵引、码牵引和位同步三条处理链并行运行；
- 位同步只搜索20种数据边界，频率候选固定为0 Hz，并使用独立数据确认。

本轮修复的错误描述：

- 点积和叉积统一改为$P_{\mathrm{dot}}$与$P_{\mathrm{cross}}$，并从相邻Prompt共轭复积开始推导；
- 删除旧11点状态表、试验过程、ASIN、主簇、中值选支和角度重映射等不属于最终方案的内容；
- 明确B1I平方使NH码和导航bit正负号消失，同时使相位变化加倍，所以鉴频分母使用$4\pi T$；
- 明确L1 C/A、B1I和G1的普通入口短相干长度、观察窗、5个物理频点和160 ms发布周期；
- 补全同步前码牵引、载波辅助DLL、三次绝对频率一致性资格、20边界ML搜索、质量检查和1/2/4 s搜索与2/4 s独立确认；
- 更新数据型信号牵引与位同步流程图，使图中的强入口与正文一致。

检查结果：

- 第四章显示公式围栏成对；
- 流程图SVG通过XML解析并完成实际渲染检查，无文字溢出、裁切或遮挡；
- git diff --check通过；
- 本轮没有修改生产跟踪代码。
