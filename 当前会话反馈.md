# 当前会话反馈

## 结论

已按本次决定完成候选验证：B1I中弱信号牵引继续使用11点DFT，只把频率覆盖范围从正负125 Hz收窄为正负62.5 Hz。

结果是：

> 收窄后的11点DFT明显优于上一轮正负125 Hz方案，但还不能作为正式方案冻结。

24 dB-Hz、27个捕获移交角点中通过21例，失败6例。所有失败都集中在负0.5 chip码偏差，说明频率网格收窄有效，但没有解决负码偏差下的码频耦合问题。

因此当前只保留该候选和测试证据，不写回正式工作区。

## 候选配置

B1I LongFast保持以下结构不变：

- 11个DFT频点；
- 2560 ms功率观察窗；
- 160 ms滑动更新；
- Prompt平方域处理，用于消除B1I NH20正负号；
- 位同步阶段不再搜索额外频率，只搜索NH20相位。

唯一算法变化是将11点频率表改为：

```text
-62.5, -50, -37.5, -25, -12.5, 0,
+12.5, +25, +37.5, +50, +62.5 Hz
```

相邻频点间隔为12.5 Hz。

## 测试范围

统一条件：

- 信号：BDS B1I，PRN 6；
- 复合C/N0：24 dB-Hz；
- 运行时间：20秒；
- 评分区间：15至20秒；
- 信号种子：20260831；
- 频率偏差：负62.5、0、正62.5 Hz；
- 码偏差：负0.5、0、正0.5 chip；
- NH20起始位置：0、10、19 ms。

总计27例。

通过条件为：完成位同步，评分段同步比例、频率观察有效率和使用率均不低于0.95，频率RMS不超过5 Hz、P95不超过10 Hz，码误差P95不超过0.2 chip，并且没有PDI或命令时序错误。

## 测试结果

| 频率偏差 | 码偏差 | NH=0 | NH=10 | NH=19 | 小计 |
|---:|---:|:---:|:---:|:---:|---:|
| -62.5 Hz | -0.5 chip | 失败 | 失败 | 失败 | 0/3 |
| -62.5 Hz | 0 chip | 通过 | 通过 | 通过 | 3/3 |
| -62.5 Hz | +0.5 chip | 通过 | 通过 | 通过 | 3/3 |
| 0 Hz | -0.5 chip | 通过 | 失败 | 失败 | 1/3 |
| 0 Hz | 0 chip | 通过 | 通过 | 通过 | 3/3 |
| 0 Hz | +0.5 chip | 通过 | 通过 | 通过 | 3/3 |
| +62.5 Hz | -0.5 chip | 通过 | 通过 | 失败 | 2/3 |
| +62.5 Hz | 0 chip | 通过 | 通过 | 通过 | 3/3 |
| +62.5 Hz | +0.5 chip | 通过 | 通过 | 通过 | 3/3 |

总结果：21/27通过。

21个通过案例的最坏指标为：

- 首次同步最晚13.958秒；
- 频率RMS最大1.505 Hz；
- 频率P95最大4.056 Hz；
- 码误差P95最大0.0141 chip；
- 同步比例、频率观察有效率和使用率均为1.0。

## 六个失败案例的共同特征

六个失败案例全部为负0.5 chip码偏差，而且全部满足：

```text
freq_obs_valid_ratio = 1.0
freq_obs_used_ratio  = 1.0
```

也就是说，DFT观察器持续输出了被判为有效的结果，载波环也持续使用了这些结果，但闭环仍没有收敛。失败案例的频率RMS范围为17.15至983.18 Hz，五例码误差也超过0.2 chip，六例均未完成位同步。

这不是“频率观察不够”造成的，而是负码偏差下相关峰形使频率观察失真，错误观察又被持续送入FLL，最终形成码频耦合发散。该判断目前是由失败分布和日志形成的工程推断；相关器抽头功率、DFT峰位置和首批FLL命令的逐历元因果关系仍需进一步确认。

## 当前决策

- 不恢复正负125 Hz的正式口径；收窄到正负62.5 Hz确实是有效方向。
- 不把当前正负62.5 Hz候选写回生产工作区，因为正式的负0.5 chip边界没有通过。
- 不通过降低评分门限掩盖失败；这些案例同时没有同步并出现明显频率发散，属于真实闭环失败。
- 下一步只需围绕负0.5 chip做最小诊断：保存首个2560 ms窗内各DFT频点功率、E/P/L相关功率、DFT峰值频率和FLL控制字，判断是相关峰选错、平方DFT旁峰，还是码环在频率居中前推动了错误码方向。
- 若该诊断能在不增加场景门限和特殊分支的前提下修复，重新跑同一27例；只有27/27通过后，才把B1I正式范围冻结为11点覆盖正负62.5 Hz。

## 证据位置

```text
候选源码：/tmp/startrack-b1i-grid62p5-src
候选构建：/tmp/startrack-b1i-grid62p5-make
27例结果：/tmp/b1i-grid62p5-matrix
临时测试清单：/tmp/b1i-grid62p5-manifest.json
```

二进制SHA-256：

```text
StarTrack：33f37de9393a47b37d04dcd826f5392fd5b961d006a9f1edcceb7ed97d9ea882
StarGen：08897ec9fb96a202e6cd05b5e84a7f59865f874ed9da18dda86d1006a62eb5c2
```

正式生产工作区没有因本候选新增修改；测试runner只在临时副本中增加了小数频差案例ID格式支持。
