# 当前会话反馈

## 最终原则

数据型信号强入口固定使用atan2系列鉴频，不再保留ASIN作为目标方案。ASIN低信噪比有界投影的现象只作为一次有价值的机理认识，不再影响正式算法结构。

正式方案只保留两条透明公式：

1. L1 C/A和G1使用单位相邻复积整窗求和atan2；
2. B1I使用二倍角单位相邻复积整窗求和atan2。

算法正文不再出现主簇、中值选支、角度聚类或逐角重映射。

## 两条强入口公式

三种信号首先形成相邻1 ms Prompt复积并归一化：

$$
Z_n=P_{n-1}^{*}P_n,
\qquad
u_n=\frac{Z_n}{|Z_n|}.
$$

### L1 C/A和G1

$$
U=\sum_{n=1}^{N}u_n,
\qquad
\widehat{\Delta f}=
\frac{\operatorname{atan2}(\Im U,\Re U)}{2\pi T}.
$$

式中，$N$表示20 ms观察窗内连续有效的相邻Prompt对数；$T=1$ ms。少数bit或meander边界只会反向抵消部分合向量。

### B1I

$$
V=\sum_{n=1}^{N}u_n^2,
\qquad
\widehat{\Delta f}=
\frac{\operatorname{atan2}(\Im V,\Re V)}{4\pi T}.
$$

平方使NH码和导航bit的正负号代数消失。分母使用$4\pi T$，因为平方使相位变化放大两倍。该方法不需要知道NH相位，也不需要任何角度分组。

## B1I最简方案补测

上一版B1I实验候选还包含二倍角角度重映射。为保证最终方案没有隐藏处理，本轮改为最简单的“二倍角单位相量直接求和，再做一次atan2”。

测试覆盖：

- 3个新噪声种子；
- NH起点`0/5/19 ms`；
- 静态$\pm62.5/\pm125$ Hz；
- 40和32 dB-Hz；
- 32 dB-Hz下$\pm125$ Hz初始频差与$\pm5$ Hz/s动态；
- 共36个物理场景、72条ASIN/atan2同输入A/B真实IQ管道。

结果：

| B1I强入口 | 静态 | 动态 | 合计 |
|---|---:|---:|---:|
| 当前ASIN基线 | 24/24 | 12/12 | 36/36 |
| 最简二倍角atan2 | 24/24 | 12/12 | 36/36 |

atan2候选动态首次同步中位数为4.39 s，基线为7.32 s；候选最晚10.50 s，基线最晚11.42 s。候选动态频率P95最坏值为2.236 Hz，码误差P95最坏值为0.165 chip，全部满足强入口门限。

## G1主簇算法是什么

G1主簇算法只是实验阶段尝试过的临时方法，不属于最终算法。它曾执行以下处理：

1. 对每一对Prompt分别计算四象限相位角；
2. 把20个角排序，以中值猜测主要半周；
3. 将与中值相差约半周的角度翻回同一半周；
4. 对重新映射的角度求平均。

该方法试图消除meander边界的$180^\circ$反号，但它引入了新的数据驱动分支选择。低信噪比下，一次错误的中值选择可能让整窗角度一起映射错误。真实IQ已经否决该方案。

正式G1算法不包含以上任何步骤，只使用$U=\sum u_n$和一次`atan2`。

## 强入口与中弱入口

| 捕获等级 | 跟踪入口 | 载波牵引 |
|---|---|---|
| 强捕获 | strong | 本文atan2强入口 |
| 中弱捕获 | normal | 5点功率DFT与块间非相干积累 |

28 dB-Hz G1不会经过强入口atan2，因此不再用它决定强入口方案。normal入口和同步后的DFT/FMS路径均不因本轮结论改变。

## 文档呈现约束

以后结果表中的算法名称必须能由正文中的完整公式直接还原。实验中增加的聚类、候选选择、门限、重映射或确认步骤，只要影响结果，就必须在同一处明确写出；未进入最终方案的实验处理不得混入算法正文。

本轮只更新算法方案，生产StarTrack源码没有修改。

## 证据

- B1I最简atan2脚本：`/tmp/run_b1i_vector_strong_holdout.py`；
- B1I机器摘要：`/tmp/startrack-b1i-vector-strong-holdout/summary_full.json`；
- B1I摘要SHA-256：`70b81d40d78d4fd58386781f09777d9a9e06daef725dd5ffab87d482812ced80`；
- G1强入口摘要：`/tmp/startrack-g1-vector-strong-holdout/summary_full.json`；
- G1摘要SHA-256：`e34fdce8d42b22ce13ab47377abce0d4d8183d3cfa873772ada6a0502c575fc7`；
- 最简atan2候选二进制SHA-256：`79d2bf5f742b2aefc67ec5082220bc08b868ee1561a24d8a7c879ee40cbeef5a`。
