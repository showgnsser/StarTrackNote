# 什么是频率观察使用率

## 1. 先说结论

频率观察使用率不是“每毫秒做了多少次鉴频”，也不是“接收机有多少时间在跟踪”。它表示：频率观察器已经生成的测量结果中，有多少条最终真正送进了载波环路滤波器。

当前分析器的定义是：

$$
R_{used}=\frac{N_{used}}{N_{ready}}
$$

式中，$N_{ready}$是计分时间内频率观察器发布结果的次数，$N_{used}$是通过全部质量和连续性门控、真正送入FLL的次数。

## 2. 一条频率观察要经过三层状态

### 2.1 ready：频率结果已经形成

频率观察器有自己的观察长度和更新周期。例如LongSlow可能每320 ms才形成一次频率结果。在没有形成完整窗口的其他PDI上，`freq_ready=0`，这些PDI不进入使用率的分母。

### 2.2 valid：观察器认为结果具有基本物理意义

当完整观察窗形成后，观察器还会检查功率、峰值质量、有限性等条件。通过后为`freq_valid=1`。这只说明观察器自身认可该测量，不代表控制环一定采用。

### 2.3 used：控制环实际采用了这条结果

有效观察还要经过当前状态的频率连续性门。例如LongSlow只允许残差不超过5 Hz的观察直接进入FLL；更大的变化需要多次独立且一致的结果确认。通过后`freq_used=1`，这条频差才会真正修正载波NCO。

因此三者关系是：

```text
完整观察窗形成
      ↓
freq_ready = 1
      ↓ 观察器质量检查
freq_valid = 1
      ↓ 状态范围、粗差和连续性检查
freq_used = 1
      ↓
送入FLL并修正载波NCO
```

## 3. 当前0.556是怎样算出来的

以L1CA普通入口、22 dB-Hz、-62.5 Hz场景为例。最后3秒处于LongSlow，频率观察更新周期为320 ms，共形成9条观察：

| 观察残差 | valid | used | 处理结果 |
|---:|---:|---:|---|
| +1.85 Hz | 1 | 1 | 直接采用 |
| -6.40 Hz | 1 | 0 | 超过LongSlow的5 Hz直接门，拒绝 |
| +2.68 Hz | 1 | 1 | 直接采用 |
| -9.48 Hz | 1 | 0 | 拒绝 |
| -7.18 Hz | 1 | 0 | 拒绝 |
| -1.56 Hz | 1 | 1 | 直接采用 |
| +1.75 Hz | 1 | 1 | 直接采用 |
| +6.29 Hz | 1 | 0 | 拒绝 |
| +1.36 Hz | 1 | 1 | 直接采用 |

9条观察全部`valid`，所以频率观察有效率为：

$$
R_{valid}=\frac{9}{9}=1.0
$$

真正进入FLL的是5条，所以频率观察使用率为：

$$
R_{used}=\frac{5}{9}=0.556
$$

## 4. 使用率低是否等于跟踪不稳定

不等于。

当前这个场景虽然只使用5/9条频率观察，但最后3秒的Doppler RMS为2.444 Hz、P95为2.778 Hz，码误差P95为0.0166 chip，位同步保持正常。被拒绝的观察主要是低信噪比下偶发的6至9 Hz跳动；粗差门拒绝它们，反而避免NCO被单次噪声拉走。两次频率更新之间，接收机仍由已经建立的FLL状态、载波趋势和PLL连续维持NCO，不是停止跟踪。

不过，如果长时间几乎没有观察被采用，也说明环路只能依靠旧状态预测，遇到真实动态变化时可能无法及时校正。因此使用率是“频率校正证据是否持续补充”的健康指标，而不是单独的稳定性结论。

## 5. 为什么当前短测试很容易低于0.95

最后3秒只有约9条LongSlow观察。`>=0.95`实际上要求9条全部采用，因为8/9只有0.889。低信噪比下只要拒绝一条，就无法过0.95门。

所以目前需要继续确认两件事：

1. 正式计分窗是否应该更长，使320 ms低更新率状态有足够统计样本；
2. `freq_used>=0.95`是否应该按状态职责设置，而不是对所有状态统一要求。LongSlow主动拒绝偶发粗差是设计行为，把这种拒绝全部算作失败可能与稳健门控的目的冲突。

不能为了提高使用率直接放宽5 Hz门限。应先统计更长时间和更多种子下被拒绝观察的误差分布，并确认拒绝是否改善了最终NCO精度。

## 6. 与本轮三信号结果的关系

本轮10个场景全部完成位同步，频率和码精度也全部通过。四个普通入口短场景没有通过“严格整段门”的唯一原因，就是频率观察使用率分别为0.556、0.667、0.556和0.778。

因此当前准确表述是：

- 三种信号的牵引和位同步方案第一轮验证通过；
- 低使用率没有造成当前样本中的同步或精度失败；
- 频率观察使用率的验收口径仍需单独收口，不能把它直接解释为跟踪失败，也不能直接删除这个健康指标。
