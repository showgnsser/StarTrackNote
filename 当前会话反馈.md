# 是否应该保留5 Hz频率门

## 1. 审查结论

你的判断方向基本正确：正常反馈环应该允许后续测量逐渐修正偶发误差，而不是不断增加固定门限。当前5 Hz门缺少独立、可审计的必要性证据，应当重新做A/B；在证明它不可缺少之前，不能把它视为最终设计合同。

更准确地说：

- 过去确实存在弱信号频率环发散的失败；
- 但没有证据证明失败的原因是“缺少5 Hz门”；
- 5 Hz门加入后，旧弱信号失败仍然存在；
- 后来真正改善来自频率观察器、观察时长、环路更新时标和趋势积分保护的共同整改；
- 当前环路已经具备比门控刚加入时更强的自恢复能力，因此这道门可能已经冗余。

## 2. 历史上没有5 Hz门时是否失败过

`FreqGuard`是在提交`51b555e`中加入的。该提交同时大幅修改了频率观察器、位同步、FSM、FLL和测试，因此它不是一次只比较5 Hz门开关的实验。

加入前后的历史问题集中在L1CA 11 dB-Hz弱跟踪：

- 320 ms局部FMS在部分种子中失去频率响应；
- 宽范围相位差观察产生较多粗差；
- 二阶/三阶FLL的频率变化率状态被噪声持续推动；
- 局部FMS越过单调区后会折返成看似较小的错误量。

但5 Hz门加入后，seed 20260810仍然得到：

- 11 dB-Hz段Doppler RMS 5.521 Hz；
- Doppler P95 11.848 Hz；
- 恢复阶段NCO偏离约31.6 Hz。

所以“以前有失败”是真的，但“失败是缺少5 Hz门造成的”没有被证明。后来三种子改善主要来自600 ms观察、0.2 Hz FLL和局部FMS整改，而不是一次独立的5 Hz门验证。

历史上还尝试过把残差硬限制到±2 Hz，结果连续同号噪声仍进入趋势积分，NCO甚至漂移到百Hz量级。这说明简单限幅和简单拒绝都不能替代正确的观察器与反馈环。

## 3. 当前环路能否自行恢复粗差

可以，前提是粗差是偶发的、鉴频器仍处于有效范围、后续测量继续提供正确方向。

LongSlow当前使用320 ms更新。其三阶FLL主要增益约为：

$$
K_1\approx0.299,\qquad K_2\approx0.0447
$$

同时高阶趋势支路的单次输入被限制到0.1 Hz，并带有0.999的有限记忆。假设偶发一条10 Hz误差：

- 比例支路不会把NCO直接跳10 Hz，而是约修正3 Hz；
- 趋势支路只增加约0.0045 Hz/update，即约0.014 Hz/s；
- 后续方向正确的频率观察会逐步把这3 Hz拉回来。

即使鉴频器输出到±25 Hz边界，单次比例变化也大约为7.5 Hz，而不是25 Hz。它可能造成短暂扰动，但不必然导致永久发散。

因此当前环路与当年失败版本已经不同：趋势创新受限、状态连续、后续观察会负反馈纠正。继续使用旧5 Hz硬门，需要新的证据。

## 4. 硬门可能破坏正确反馈

如果一条超过5 Hz的结果是噪声，拒绝它有利；但如果它来自真实动态或NCO确实偏离，拒绝它会产生相反效果：

```text
真实频差已经变大
        ↓
观察器正确测出8 Hz
        ↓
5 Hz门拒绝
        ↓
FLL得不到纠偏
        ↓
残差继续存在或扩大
```

当前逻辑虽然允许三次一致后重新接纳，但这会增加至少一个完整观察跨度的延迟。LongSlow每次更新320 ms，真实动态可能在等待期间继续变化。

这也是硬门的根本矛盾：它无法只凭“残差较大”区分噪声粗差和真实动态。

## 5. 现有数据不支持统一5 Hz

按已有真实IQ日志做近似真值比较：

- L1CA和G1中，被拒绝组整体误差更大，门控有一定保护作用；
- B1I负频差场景中，被拒绝组与采用组误差接近，门控没有分离能力；
- 用相同观察序列做简化环路反事实回放时，L1CA两例不设门的平均NCO误差更小，G1和B1I各有一例保留门稍好。

这说明当前数据没有支持“所有信号、所有LongSlow场景统一使用5 Hz”的证据。不同结果也说明不能只挑一个场景删除门，必须使用相同PDI做完整闭环A/B。

## 6. 建议的简单方案

应优先验证接近芯片的简单闭环：

```text
观察器完成物理有效性判断
        ↓
鉴频输出限制在物理范围±25 Hz
        ↓
所有有效结果进入三阶FLL
        ↓
环路带宽抑制噪声
        ↓
频差均值/RMS只负责动态状态切换
```

这个方案只保留三类必要限制：

1. 观察器自身的功率、峰值和有限性判决；
2. 鉴频器物理无模糊范围；
3. 环路和状态本身的动态职责。

不再额外使用5 Hz、3 Hz和三次确认去判断一条观察是否“像粗差”。

## 7. 下一步A/B

下一步不应继续调整5 Hz数值，而应只比较两个版本：

- A：当前5 Hz门；
- B：删除LongSlow频率门，所有物理有效观察直接进入FLL，保留±25 Hz鉴频范围和当前环路参数。

使用同一PDI、同一种子、同一初始状态，覆盖：

- L1CA、B1I、G1静态弱信号；
- ±1 Hz/s动态；
- TCXO；
- 短时频率阶跃；
- 166 Hz/s强入口高动态；
- 每项至少三个种子。

比较顺序应是：失步和重捕、频率RMS/P95、码误差、动态恢复时间、NCO最大偏移，最后才是观察使用率。

如果B不比A差，应删除5 Hz门，而不是保留一个没有必要性的参数。如果B只在明确的重复粗差场景失败，应优先修正观察器或使用简单的环路影响限制，不再增加候选计数、独立跨度等状态机。

## 8. 当前建议

当前设计门应标记为REWORK：5 Hz门不是芯片合同，历史失败也没有证明它必需。最合理的下一步是实现一个仅供测试的门控旁路，完成相同PDI的闭环A/B。若简单反馈环通过，应回到“有效观察全部进入环路”的芯片式结构。
