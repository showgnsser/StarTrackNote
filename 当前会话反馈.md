# 当前会话反馈

## 本次目标

尝试以atan2为核心重新设计L1CA、G1和B1I位同步前强入口鉴频器，并验证asin能够工作的场景是否可以由更合理的atan结构覆盖。

## 最终候选结构

三个信号共用相邻1 ms Prompt复积：

$$
Z_k=P_{k-1}^{*}P_k=dot_k+j\,cross_k.
$$

先归一化为单位向量：

$$
u_k=\frac{Z_k}{|Z_k|}.
$$

后续按信号符号结构分别聚合，不再统一套用逐对`atan(cross/dot)`。

### L1CA

20个单位复积直接求和：

$$
U=\sum_{k=1}^{20}u_k,
\qquad
\widehat{\Delta f}=\frac{\operatorname{atan2}(\Im U,\Re U)}{2\pi T}.
$$

正常相邻对同向相加；一个稀疏bit边界只产生一个反向向量，降低合向量幅度但不改变多数方向。全窗只做一次四象限atan2，不逐对判dot符号。

### G1

G1具有固定meander边界，20 ms内反向向量比L1CA更多。先计算每一对完整四象限角度，使用窗口中值确定主簇，再把相差半周的少数边界角度映射回主簇，最后平均并换算频率。

该处理显式利用G1 meander边界稀疏且有固定结构的特点。

### B1I

B1I的NH符号逐毫秒变化，不能使用稀疏边界假设。对单位复积做二倍角：

$$
v_k=u_k^2.
$$

NH和导航bit正负号在二倍角中代数消失。随后在二倍角域用窗口主簇进行圆周重映射和平均：

$$
\widehat{\Delta f}=
\frac{\overline{\arg(v_k)}}{4\pi T}.
$$

这里使用四象限atan2，因此1 ms基线的物理无模糊范围仍为`(-250,+250) Hz`，正式`±125 Hz`位于范围内部，不再是模糊边界。

## 确定性验证

以下测试全部通过：

- 任意单次bit翻转；
- 每毫秒交替的最坏NH符号；
- 频差`±62.5、±125、±249 Hz`的正确符号和Hz尺度；
- PDI缺口清窗；
- L1CA无噪声随机bit、166 Hz/s动态强入口闭环。

## 真实IQ测试矩阵

仍使用与前几轮完全相同的48个物理场景，每例15秒、2个信号种子：

- L1CA：`±62.5 Hz`，40、32、22 dB-Hz；
- B1I：`±62.5/±125 Hz`，40、32、24 dB-Hz；
- G1：`±62.5 Hz`，40、32、28 dB-Hz。

候选二进制所有48条运行均无PDI错误和命令错误。

## 强入口正式范围结果

32和40 dB-Hz对应强捕获入口范围。候选在该范围内全部成功位同步：

| 信号 | 场景数 | 当前asin | 信号专用atan2 |
|---|---:|---:|---:|
| L1CA | 8 | 8/8 | 8/8 |
| B1I | 16 | 16/16 | 16/16 |
| G1 | 8 | 8/8 | 8/8 |
| 合计 | 32 | 32/32 | 32/32 |

10～15秒频率绝对误差P95的每组最坏值如下：

| 信号 | C/N0 | 当前asin | 信号专用atan2 |
|---|---:|---:|---:|
| L1CA | 40 | 2.177 Hz | 2.332 Hz |
| L1CA | 32 | 1.588 Hz | 1.481 Hz |
| B1I | 40 | 2.576 Hz | 2.199 Hz |
| B1I | 32 | 1.782 Hz | 1.757 Hz |
| G1 | 40 | 2.558 Hz | 2.296 Hz |
| G1 | 32 | 2.045 Hz | 1.998 Hz |

L1CA高C/N0两者基本相当；B1I和G1的atan2候选在40、32 dB-Hz下均保持或改善频率P95。

首次同步时间主要由固定搜索和确认窗决定，因此强信号下差异不大。32 dB-Hz时atan2候选全部同步，但部分种子比当前方案慢，仍需更多种子评估时延分布。

## G1 28 dB-Hz压力结果

G1即使在28 dB-Hz压力场景也保持4/4同步：

| 指标 | 当前asin | G1主簇atan2 |
|---|---:|---:|
| 首次同步中位数 | 7.54 s | 6.41 s |
| 最慢首次同步 | 11.64 s | 8.72 s |
| 10～15秒频率P95最坏值 | 4.244 Hz | 3.190 Hz |
| 10～15秒最小同步率 | 0.673 | 1.000 |

这是本轮最明确的性能提升，说明G1确实需要利用meander边界结构，而不是继续让asin把边界频率反号后平均。

## B1I单独方案结论

B1I二倍角主簇atan2解决了前一轮两个核心问题：

1. 不需要对每个dot单独判NH符号；
2. 正式`±125 Hz`不再位于模糊边界。

结果为40 dB-Hz 8/8、32 dB-Hz 8/8同步。40 dB-Hz首次同步中位数由3.21秒缩短到3.19秒；32 dB-Hz中位数由3.18秒变为4.33秒，但全部案例仍在8.10秒内同步。频率P95在两档均略有改善。

因此B1I已经找到不依赖asin的可行强入口鉴频方案，但32 dB-Hz同步时延方差仍需更多种子确认。

## 低信号入口边界

L1CA 22和B1I 24 dB-Hz不属于强入口范围。强制使用任一1 ms短周期鉴频器时都容易进入相位门限区。它们应使用normal入口的长观察功率搜索；当前基线按正确入口复测为16/16同步。

这不表示atan2性能不足，而是1 ms单对相干SNR已经不足以支撑强入口职责。不能要求更换反函数来替代处理增益。

## 当前结论

1. asin能够工作的强入口场景，信号专用atan2候选同样全部通过。
2. B1I不需要退回asin；二倍角主簇atan2已覆盖`±125 Hz`并通过32/40 dB-Hz矩阵。
3. G1的atan2方案已有明确性能提升。
4. L1CA在当前矩阵中总体相当，32 dB-Hz频率P95略有改善。
5. 当前证据支持继续推进atan2强入口设计，但只有2个种子和静态频差，尚不足以直接修改生产代码。
6. 下一门应补充至少3个新种子、符号边界偏移和真实动态；通过后再替换生产`PromptFllObs`。

## 证据位置

- 候选源码快照：`/tmp/startrack-atan-signal-src`
- 候选Release二进制：`/tmp/startrack-atan-signal-build/startrack`
- 候选二进制SHA-256：`5cc96f92aacc0bf2c22d588e1d08c88dc788b5c3267963e6a7fb31468f5d9905`
- 完整机器摘要：`/tmp/startrack-prompt-fll-atan-ab/summary.json`
- 摘要SHA-256：`63992723ffbdd2f5e709bb26686e0909571bdee9dc6eab8029bdbf24ea82170e`
- 生产仓库未修改。
