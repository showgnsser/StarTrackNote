# 当前会话反馈

软件方案第四章已经按完整信号流程重新组织。

新的章节结构为：

1. 强信号牵引；
   - GPS L1 C/A；
   - BDS B1I D1；
   - GLONASS G1；
   - 强信号公共控制时序。
2. 中弱信号牵引；
   - GPS L1 C/A；
   - BDS B1I D1；
   - GLONASS G1；
   - 中弱信号公共控制时序。
3. 数据位同步；
   - GPS L1 C/A；
   - BDS B1I D1；
   - GLONASS G1；
   - 公共搜索、确认与复位。

每个信号小节现在都明确给出了：

- 载波鉴频使用的1 ms输入和符号处理方法；
- 短相干长度和总观察长度；
- FLL真正更新控制状态的周期；
- 五抽头码观察的组织方法；
- DLL真正更新控制状态的周期；
- 载波辅助码率的作用；
- 进入位同步前需要满足的频率资格。

强信号部分明确为20 ms FLL和20 ms DLL。中弱信号部分明确为160 ms FLL和160 ms DLL，同时单独区分了1280/2560 ms载波功率观察窗，避免把长观察窗误解为环路更新周期。

数据位同步被移到牵引方案之后单独说明，按L1 C/A全+1模板、B1I NH20和G1 meander分别展开，再统一说明20边界最大似然搜索、质量检查、1/2/4 s搜索和2/4 s独立确认。

点积、叉积继续使用$P_{\mathrm{dot}}$和$P_{\mathrm{cross}}$。强入口不包含ASIN、主簇、中值选支或角度重映射；中弱入口固定使用5点功率DFT。

流程图同步增加了“强入口FLL 20 ms更新”“中弱入口FLL 160 ms更新”和“DLL强20 ms、中弱160 ms更新”的明确标注。公式围栏、SVG解析、实际渲染和git diff检查均通过。本轮没有修改生产跟踪代码。
