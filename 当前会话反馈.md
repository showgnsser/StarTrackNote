# 当前会话反馈

## 160毫秒更新周期与1.28秒观察窗的区别

160 ms和1.28 s描述的是两件不同的事情：

- 160 ms是控制更新周期：FLL每隔160 ms最多接收一次新频率结果，并生成一次新的载波控制量；
- 1.28 s是观察窗长度：每个频率结果都使用最近1.28 s的Prompt历史计算。

因此当前L1 C/A和G1普通入口实际执行的是：

```text
时刻1.28 s：使用0～1.28 s数据形成第一个频率结果
时刻1.44 s：使用0.16～1.44 s数据形成第二个频率结果
时刻1.60 s：使用0.32～1.60 s数据形成第三个频率结果
……
```

它不是每1.28 s才更新一次，而是每160 ms输出一次高度重叠的长窗估计。

## 重叠程度

1.28 s窗口每160 ms滑动一次，相邻两次结果共享：

$$
1.28-0.16=1.12\ \text{s}
$$

共享比例为：

$$
\frac{1.12}{1.28}=87.5\%.
$$

也就是说，一段Prompt数据会被连续用于约8次频率结果。

被否决的B1I 2.56 s方案更严重：

$$
\frac{2.56-0.16}{2.56}=93.75\%,
$$

同一段数据会被连续用于约16次结果。

## 当初为什么拉长观察窗

设计动机是提高弱信号下DFT频点选择的可靠性。160 ms控制周期内的能量可能不足，于是保留160 ms环路更新节拍，同时用更长历史做非相干功率累加，希望兼顾：

1. 长时间积累带来的频率峰稳定性；
2. 每160 ms更新一次控制量的响应速度。

这个想法在统计观察器层面可以成立，但不能把每个高度相关的滑窗结果都当成新的独立证据反复送入FLL。

## 为什么当前看起来是画蛇添足

对于普通中弱数据型入口，位同步器本身已经要积累约1 s数据，并且完全可以同时搜索频率和bit/NH相位。此时在它前面再放一个1.28 s滑窗DFT，功能发生重复：

```text
1.28 s预同步DFT先搜索一次频率
→ 多次反馈FLL
→ 频率资格成立
→ 1 s位同步器再次积累数据搜索边界
```

这会带来四个额外代价：

1. 首次频率结果必须等待1.28 s成熟；
2. 观察中心相对当前时刻天然滞后约0.64 s；
3. 相邻结果共享87.5%的数据，却被FLL连续使用，容易重复计算同一份证据；
4. DLL和载波NCO在长窗内已经变化，窗口前半段和后半段不再对应完全相同的闭环条件。

对2.56 s方案，这些问题进一步变成1.28 s中心迟滞和93.75%相邻重叠。

## 当前判断

从现在明确的入口职责看，B1I已经证明可以删除这一级长窗DFT：捕获NCO保持、160 ms功率DLL修正码偏差、位同步器联合搜索11个频率和20个NH相位，27/27通过。

因此L1 C/A和G1普通入口的1.28 s预同步DFT也很可能属于重复设计。它曾经用于弥补160 ms弱信号频率观察不稳定，但在“频率候选放入位同步器”的方案下，这项职责已经可以由位同步搜索一次完成。

更简洁、职责更清楚的统一候选应是：

```text
强入口：短周期Prompt FLL → 位同步

普通中弱入口：
保持捕获载波NCO
+ 160 ms功率DLL修正码偏差
+ 位同步器联合搜索频率和bit/NH相位
→ 搜索通过后一次性校频
→ 独立确认
```

当前不能仅凭B1I结果直接删除L1 C/A和G1路径，但下一步正确的验证不是继续调整1.28 s窗口，而是对这两种信号做同场景A/B：当前1.28 s预同步DFT与联合位同步方案各跑捕获移交矩阵。若联合方案不弱，就应删除1.28 s长窗反馈，而不是保留两套重复的频率搜索。
