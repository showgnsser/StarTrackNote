# 当前会话反馈

## 160毫秒B1I独立频率牵引测试结论

已按要求完成测试：B1I位同步不搜索频率；删除2.56 s长观察窗；频率观察窗和FLL更新周期都固定为160 ms，不使用滑动重叠。

结果明确失败：

```text
27例中：
完成合格位同步 0/27
满足频率、码和同步全部物理门 0/27
runner通过 0/27
```

因此在24 dB-Hz捕获灵敏度下，“160 ms平方Prompt DFT先牵引，位同步只搜索NH20相位”的方案不可行。

## 候选配置

普通B1I入口使用：

- 1 ms Prompt复数平方，消除未知NH20正负号；
- 每10 ms形成一个相干短块；
- 160 ms内累积16个短块功率；
- 11个频率候选，覆盖正负62.5 Hz，相邻间隔12.5 Hz；
- 观察窗160 ms；
- 更新周期160 ms；
- 相邻频率观察完全不重叠；
- 频率观察进入FLL；
- 频率资格成立后，位同步只搜索20个NH20相位，不搜索频率。

这次没有1.28 s或2.56 s历史，也没有长窗滑动复用。

## 测试范围

- BDS B1I，PRN 6；
- 复合C/N0为24 dB-Hz；
- 频差为负62.5、0和正62.5 Hz；
- 码差为负0.5、0和正0.5 chip；
- NH20起始位置为0、10和19 ms；
- signal seed为20260831；
- 每例20秒，评分区间为15至20秒；
- 共27例。

定向测试首先通过：

```text
test_pull_in_freq_obs   PASS
test_data_bit_sync      PASS
test_data_track_engine  PASS
```

这只证明模块时序和无噪声确定性输入成立，不能替代24 dB-Hz真实IQ结果。

## 真实IQ表现

27例没有一例达到合格同步比例。只有一例在15.578 s短暂同步，但15至20秒同步比例仅0.888，仍未达到0.95；其余26例没有完成同步。

所有案例都记录为：

```text
freq_obs_valid_ratio = 1.0
freq_obs_used_ratio  = 1.0
```

也就是说，问题不是“没有频率观察”，而是错误频率观察被当成有效结果持续送入FLL。

## 零频差、零码差案例的直接证据

场景真值为0 Hz、0 chip，但前几次160 ms频率结果依次为：

| 观察末历元 | DFT输出 | FLL更新后载波控制量 |
|---:|---:|---:|
| 160 ms | +27.94 Hz | +25.60 Hz |
| 320 ms | -17.42 Hz | +9.64 Hz |
| 480 ms | +48.16 Hz | +53.77 Hz |
| 640 ms | -0.21 Hz | +53.58 Hz |
| 800 ms | -27.83 Hz | +28.07 Hz |
| 960 ms | +19.60 Hz | +46.03 Hz |
| 1120 ms | -32.64 Hz | +16.12 Hz |
| 1280 ms | -40.07 Hz | -20.60 Hz |

这些观察的峰值比分别只有约1.02至1.31，却全部被当前无附加质量门的候选接受。之后频率结果继续在多个频点之间跳变，零频差场景最终评分段频率RMS达到498.48 Hz、P95达到712.83 Hz，码误差P95达到3.09 chip。

因此位同步尚未开始以前，载波闭环已经被错误DFT峰推离正确中心。

## 为什么160毫秒不够

平方Prompt虽然消除了NH20正负号，但它也把弱信号噪声通过非线性平方带入观察量。24 dB-Hz下，160 ms只包含16个10 ms平方域短块；这些能量不足以稳定区分11个相邻频率候选，最大功率频点经常只是随机噪声峰。

长到2.56 s能够增加平方域频点分离度，但会产生长迟滞、高度重叠和重复反馈，先前也只通过21/27。缩短到160 ms虽然消除了长窗问题，却直接暴露出平方域灵敏度不足。

联合位同步方案不同：它针对每个NH20相位候选先恢复公开符号，再在线性复数域积累约1 s数据，不需要对Prompt平方，因此弱信号统计效率明显更高。当前联合方案在同一27例中通过27/27。

## 当前决策

- 不写回160 ms平方DFT候选；
- 当前生产WIP继续保留B1I“11个频率×20个NH20相位”的联合位同步；
- B1I普通入口保持捕获载波NCO，160 ms功率DLL继续修正码偏差；
- 不通过增加频率峰门限挽救本方案，因为零频差、零码差基本角已经从第一批观察开始随机选峰；更严门限只会减少错误更新，不能形成可靠牵引；
- 正式生产WIP本轮未修改。

## 证据位置

```text
候选源码：/tmp/startrack-b1i-160ms-src
候选构建：/tmp/startrack-b1i-160ms-build
27例结果：/tmp/b1i-160ms-matrix
```

候选Release二进制SHA-256：

```text
b9bccc9a21d12d04ec8ac10f682f72ac5881ebc338a71cbe964677ce9bf17fd7
```
