**先舍入是对的，但不能统一高位补0。**

$\Delta K_{code}$的处理顺序是：

1. 将乘积$T_a$的18个小数bit按对称四舍五入去掉，得到**有符号整数**。例如：$+3.5\rightarrow+4$，$-3.5\rightarrow-4$。
2. 将舍入结果**符号扩展到37 bit**：非负数高位补0，负数高位补1。
3. 与前补0扩展到37 bit的$K_{code,0}$相加。

所以，$\Delta K_{code}$是“**先舍入，再符号扩展**”；始终高位补0的是无符号的$K_{code,0}$。
