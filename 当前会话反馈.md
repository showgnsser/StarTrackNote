# 当前会话反馈

## 直接结论

对。只针对数据型信号的普通/中弱捕获入口`LongFast`，当前正式工作区的未提交WIP在首次约1.019秒内，载波环处于：

> 有NCO连续运行和已有趋势预测，但没有新的频率测量用于校正。

从反馈控制角度看，这一段就是载波测量开环。码环不是开环，DLL仍按160 ms更新码NCO。

这不是因为“160 ms更新出来的频率控制字不准”。更准确地说：

- 频率控制字只是载波NCO当前执行的命令；
- 160 ms是载波环希望获得新观察并更新命令的节拍；
- 真正可能不可靠的是低信噪比、未知数据符号条件下，由160 ms数据算出的频率误差观察；
- 当前WIP把这条160 ms观察直接删掉了，所以160 ms控制时刻虽然还存在，却没有测量可用。

## 两种入口不要混淆

### 强信号入口

强捕获结果进入`ShortFast/ShortNormal`。该路径位同步前仍使用相邻1 ms Prompt形成频率误差，并按20 ms节拍更新载波环。因此强入口最初1秒不是载波开环。

### 普通/中弱信号入口

普通或中弱捕获结果进入`LongFast`。当前未提交WIP的时序是：

```text
捕获频率装入载波NCO
        │
        ├─ 0～约1.019 s：NCO继续运行，但没有新频率观察校正
        │                  DLL仍每160 ms更新
        │
        ├─ 位同步器收满1000 ms数据和19 ms边界余量
        │
        ├─ 搜索5个频率×20种边界
        │
        ├─ 搜索通过：把估计频差一次性加到载波NCO控制量
        │
        └─ 从后续数据开始：正常160 ms频率观察和FLL更新
```

如果第一次1秒搜索未通过，搜索窗会扩展到2秒，最多4秒。在这期间仍没有新的载波频率测量校正，所以风险比“固定开环1秒”更严重。

## 1秒结果和160 ms更新的关系

1秒结果不是每1秒运行一次的正式FLL，也不是把环路更新周期从160 ms改成1000 ms。

它是位同步联合搜索给出的一次性粗频率结果。通过时执行：

```text
新载波NCO控制量 = 当前载波NCO控制量 + 1秒搜索得到的残余频率
```

然后才允许正常160 ms观察器接管。当前正式WIP通常只在首次搜索通过时执行这一次修正。

“每1秒重复修正一次”只存在于`/tmp/startrack-joint-nosync-src`压力测试副本中，不是正式代码。

## 为什么会删除160 ms观察

当时需要解决两个问题：

1. 位同步前独立频率搜索和位同步内部频率搜索重复计算；
2. 在捕获灵敏度、未知符号条件下，若160 ms观察器偶发输出大误差，FLL会把粗差写入NCO。

但这只能说明“原160 ms观察器需要重新审查”，不能推出“载波可以先开环1秒”。我把删除重复长窗DFT和删除全部160 ms频率反馈放在了一起，这是不正确的设计跳跃。

已经做过的临时试验也只说明：

- 简单相邻Prompt反正切方案在L1 C/A牵引边界失败；
- 独立160 ms五点DFT在B1I初测中没有形成统一可靠方案；
- 1秒联合搜索在已测场景中能够校准频率。

这些结果不证明160 ms节拍本身有问题，也不证明1秒测量开环是合理设计。

## 正确的问题应该怎样描述

现在需要解决的问题不是：

> 160 ms控制字准不准？

而是：

> 在位同步前、未知数据符号和捕获灵敏度下，怎样每160 ms形成一个足够可靠、符号不敏感的频率误差观察？

目标结构应当是：

```text
160 ms频率观察 ──> FLL ──> 载波NCO
        与
1～4 s位同步搜索 ──> bit/NH/meander边界确认
```

两条路径并行。位同步慢或失败不应使载波环停止获得测量；位同步的1秒频率结果可以作为辅助粗校准，但不应替代持续的160 ms载波反馈。

## 当前状态

- 已提交基线`b461327`仍保留`LongFast`位同步前的160 ms频率观察；
- 当前工作区删除该观察的改动尚未提交；
- 每1秒重复校频只在`/tmp`测试副本；
- 当前没有后台测试进程；
- 在重新确认160 ms预同步鉴频方案前，不继续修改正式算法。
