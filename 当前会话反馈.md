# 强入口鉴频与普通入口公式简化

## 强入口使用的鉴频器

当前强入口使用的是**归一化叉积反正弦鉴频器**，不是DFT鉴频器，也不是`atan2`鉴频器。

它的物理过程只有三步：

1. 把相邻两条1 ms Prompt看成I/Q平面中的两根向量；
2. 用归一化叉积得到两根向量夹角的正弦值；
3. 用反正弦恢复相位变化，再除以1 ms得到频率。

L1 C/A和G1直接比较相邻Prompt。B1I先把Prompt平方，以消除NH码和导航bit的正负号；平方后相位变化加倍，所以频率换算分母使用$4\pi\times1$ ms，而不是引入难理解的“相位倍数”参数。

反正弦的主值范围决定了单对唯一鉴频范围：L1 C/A和G1为$\pm250$ Hz，B1I平方鉴频为$\pm125$ Hz。

观察器先对每一对Prompt独立鉴频，再平均连续20次结果，每20 ms更新一次FLL。

## 普通入口公式整改

普通入口仍是5点功率DFT，但文档不再使用复指数公式。新的描述直接写成：

1. 对每个候选频率补偿1 ms Prompt；
2. L1 C/A和B1I把10条补偿结果直接复数相加，G1把5条直接复数相加；
3. 每个短相干块结束后计算一次功率；
4. 将观察窗内的短块功率相加，选择总功率最大的频点；
5. 主峰不在边缘时，用相邻三个功率作插值。

文档中的短相干公式现在只表达“补偿后的复数逐项相加”，不再出现`exp`。B1I平方后旋转速度加倍，因此候选补偿角也加倍，但候选表和最终结果仍使用物理Hz。

## 本次修改

- 软件方案版本更新到V0.12；
- 明确写出鉴频器名称和物理步骤；
- 删除通用“相位倍数”记号及其残余引用；
- 把复杂DFT相干和改成补偿后Prompt直接相加；
- 每个公式后保留参数、单位和信号差异说明；
- 本次没有修改生产代码，也没有改动用户的`.obsidian`配置。

## 下一步

请先检查强入口鉴频和普通入口频率观察是否已经足够直观。确认后，再继续整理数据型信号的码牵引和公共位同步部分。
