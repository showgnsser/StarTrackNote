# 当前会话反馈

## 结论

不是把位同步前的频率牵引直接取消，而是取消独立的1.28秒/2.56秒滑动长窗DFT。位同步前必须继续闭合载波环和码环。

当前测试支持的最简统一方案是：连续1 ms Prompt进入同一张“五个频率×20个bit/NH/meander边界”功率表；该功率表同时产生两个相互独立的输出：频率输出固定每1秒发布并更新载波NCO，边界输出只有通过位同步峰形、C/N0和独立确认后才允许擦除符号。码NCO在位同步前继续由160 ms DLL与载波辅助更新。

因此，频率估计和位同步可以共享计算，但必须分开判断：bit边界不合格不能阻止频率更新；频率可用也不能提前把候选bit边界用于符号擦除。

## 逐项回答

### 1. 位同步前载波NCO和码NCO是否停止更新

不会。

- 载波NCO：每1秒使用五频点联合搜索给出的频率误差校正；
- 码NCO：每160 ms使用DLL残余码误差更新，并持续加入载波多普勒辅助码率；
- 位同步：与上述两个闭环并行运行，只负责确认导航bit、NH或meander边界。

若删除长窗DFT后也删除频率反馈，载波确实会在动态多普勒下逐渐离开相关峰，这种方案已经被否决。

### 2. B1I与L1 C/A、G1能否统一

可以统一大流程：三者都使用连续1 ms Prompt、五个频率、20个边界、每1秒频率更新、独立边界确认和160 ms DLL。

物理差异只进入公开覆盖码：L1 C/A覆盖码全为+1，B1I使用NH20，G1使用meander。后面的频率输出、载波控制、码环和同步确认时序保持一致。

### 3. 去掉独立前置DFT后的测试

已经比较三种候选：

1. 160 ms相邻Prompt逐对`asin`或整窗`atan2`：L1 C/A 22 dB-Hz确定性移交角发生失控，否决；
2. 独立160 ms五点DFT：L1 C/A确定性27/27通过，但B1I真实IQ首批4例仅1例通过，G1在旧22 dB-Hz压力口径也出现失败，不能作为统一方案；
3. 位同步联合搜索共享功率表、频率与边界解耦：严格禁止最终位同步锁定、禁止使用候选边界擦符号时，48/48真实IQ通过。

48例包括每种信号12条静态场景和4条动态场景：静态覆盖3个初始频差、2个码差、2个种子；动态覆盖正负1 Hz/s和正负62.5 Hz初始频差。L1 C/A、B1I、G1分别使用22、24、28 dB-Hz。

| 信号 | 静态最坏RMS/P95 | 动态最坏RMS/P95 | 最坏最终码误差 |
|---|---:|---:|---:|
| L1 C/A | 1.53/2.96 Hz | 3.21/5.35 Hz | 0.058 chip |
| B1I | 1.13/2.60 Hz | 2.22/3.31 Hz | 0.113 chip |
| G1 | 0.47/0.95 Hz | 2.12/2.77 Hz | 0.138 chip |

每例10秒计分尾段均有10次频率更新；`sync`始终为0，状态保持LongFast，PDI错误和命令错误均为0。

另外，正常允许位同步的三条`+1 Hz/s`真实IQ代表场景全部完成同步；尾段L1 C/A、B1I、G1的RMS/P95分别为1.30/2.48、1.07/2.18、1.09/1.75 Hz。

### 4. 问题是否来自鉴频器性能

部分是。

22 dB-Hz时单个1 ms Prompt的信噪比很低。直接对相邻1 ms结果做反正切，即使积累160 ms，也会偶发接近整个模糊范围的大角度误差。三阶FLL若把这种结果当作频率变化率，误差会被持续放大。

DFT并非性能差；它的优点正是把输出限制在已知搜索范围并提供非相干处理增益。旧方案的问题是使用了1.28/2.56秒高度重叠长窗，并在位同步中再次执行同一频率搜索，形成两套重复频率反馈。

新方案保留DFT的稳健性，但只计算一张五频点×20边界功率表，不再叠加独立长窗DFT或同步前FMS。FMS继续用于同步后的相应弱状态，不承担本阶段粗牵引。

### 5. 不完成位同步能否锁住环路

可以。48条正式压力实验故意使最终位同步永远不能锁定，也禁止候选边界参与符号擦除。三种信号的频率和码误差仍全部满足5 Hz RMS、10 Hz P95和0.2 chip门限。这证明载波和码环能够独立于最终位同步保持闭环。

### 6. 当前能冻结什么

可以冻结算法方向：

- 删除独立1.28/2.56秒滑动长窗DFT；
- 三种数据型信号统一使用五个频点，范围为正负62.5 Hz；
- 频率搜索与20种边界使用同一张功率表；
- 频率支路固定1秒更新，不读取bit边界峰形或位同步C/N0门；
- bit边界必须独立确认后才能用于符号擦除；
- 位同步前DLL保持160 ms更新和载波辅助；
- 不使用“残差小于5 Hz才允许更新”之类额外门限。

目前还不能冻结正式代码提交。现有48例来自测试分支，它故意关闭了最终位同步，用于隔离证明频率与码环。下一步需要把同步器正式重构为“共享度量、频率/边界双输出”，然后同时回归正常位同步和严格不同步两条路径，清理测试注入后才能合入正式工作区。

### 7. 最终统一数据链

```text
连续1 ms Prompt
        │
        ▼
五频点 × 20边界联合功率表
        ├── 频率输出：每1秒固定发布 ──> 载波环 ──> 载波NCO
        │
        └── 边界输出：峰形/CN0/独立确认 ──> 确认后才擦符号

E/P/L/VE/VL ──> 160 ms DLL ──> DLL残差 + 载波辅助 ──> 码NCO
```

该结构只有一套频率搜索、一套载波反馈和一套边界确认；L1 C/A、B1I、G1只替换公开覆盖码，不增加三套状态机。

## 证据位置

- 严格不同步48例汇总：`/tmp/presync-freq-decoupled-matrix/summary.json`
- 测试源：`/tmp/startrack-joint-nosync-src`
- 测试二进制：`/tmp/startrack-joint-nosync-build/startrack`
- 正常同步动态日志：`/tmp/presync-normal-l1-rate-logs`、`/tmp/presync-normal-b1i-rate-logs`、`/tmp/presync-normal-g1-rate-logs`

这些是开发证据，不是正式资格结果，也尚未写入正式源码提交。
