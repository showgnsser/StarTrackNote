# 当前会话反馈

## 结论

芯片源码证明，数据型信号在位同步前不应让载波测量开环。合理结构是：

```text
短相干频率观察 → 功率非相干积累 → FLL → 持续更新载波NCO
                         与
频率 × 数据边界二维搜索 → 独立确认 → 位同步
```

两条路径并行。位同步可以使用11点DFT，但它不能代替持续的牵引载波环。

## 芯片源码的真实方案

### L1CA

- 普通捕获入口进入`TRACK_SHORTER_FMS_LOOP`和`LongFast`；
- 位同步前按10 ms形成频点复数和；
- 多个10 ms块只累加各频点功率，不跨块线性相干；
- 观察成熟后经功率峰搜索、三点插值和约±25 Hz输出限幅进入三阶FLL；
- FLL继续更新载波NCO，不等待位同步完成。

源码初始化附近有“320 ms update period”的旧注释，但LongFast系数表明确标注`T=160 ms`，状态初始化又使用`TRK_LOOP_LONG_PERIOD / us_coh_time`，当前可执行路径应按160 ms理解。

### G1

- 大流程与L1CA相同；
- 为避开10 ms数据/meander边界，短相干块改为5 ms；
- 之后仍以功率非相干方式形成LongFast频率观察。

### B1I

- 芯片没有把未经NH剥离的Prompt直接做10 ms线性相干；
- 位同步前进入`TRACK_1MS_LOOP`，对相邻1 ms Prompt形成频率误差，再在LongFast统计期内平均；
- 位同步阶段才枚举NH/bit边界，按候选NH符号恢复20 ms相干块；
- 位同步后使用已确认NH符号剥离每个1 ms PDI，再形成20 ms相干块。

芯片`signal_tracking_1ms()`中没有看到明确NH剥离，因此“原始1 ms鉴频在全部NH相位下都安全”不能只靠源码假定，必须实测。

## 已验证的候选方案

当前只在临时副本中实现，尚未写入正式工作区。

### L1CA牵引

- 10 ms线性相干；
- 16个短块功率非相干积累；
- 160 ms观察、160 ms更新；
- 11点DFT，间隔12.5 Hz，总范围±62.5 Hz；
- 功率峰三点插值；
- 单次送入FLL的频差限制为±25 Hz。

### G1牵引

- 5 ms线性相干；
- 32个短块功率非相干积累；
- 其余11点DFT、160 ms更新和±25 Hz限幅与L1CA相同。

### B1I牵引

- 枚举20种NH起点；
- 每个候选先按NH符号恢复20 ms相干块；
- 不同20 ms块之间累加功率；
- 使用320 ms滑动统计窗，每160 ms发布一次；
- 使用同一11点、12.5 Hz间隔和±25 Hz限幅。

### 三信号位同步

- 统一使用11点DFT，间隔12.5 Hz，总范围±62.5 Hz；
- 同时搜索数据边界；
- 首次搜索与后续确认使用互不复用的数据；
- 仍按1 s搜索、2 s确认、失败时最多扩到4 s的当前时标。

## 真实IQ结果

### 三信号矩阵

场景覆盖L1CA 22 dB-Hz、B1I 24 dB-Hz、G1 28 dB-Hz，残差频率`{-62.5, 0, +62.5}` Hz、码差±0.5 chip、两个静态种子以及±1 Hz/s动态。每例30 s，后10 s评分。

- 结果：48/48通过；
- 最晚首次同步：11.077 s；
- 最坏频率RMS：2.735 Hz；
- 最坏频率绝对误差P95：4.295 Hz；
- 最坏码误差P95：0.1553 chip；
- 结果：`/tmp/joint160-matrix-30s/summary.json`；
- SHA-256：`237881e43eeb376f2f9b9fa66cc835922e4b6b17d2e33c0e72027f57db9c49a0`。

### B1I全部NH起始相位

固定24 dB-Hz、码差+0.5 chip、残差±62.5 Hz、seed 20260831，用0～19 ms输入起点覆盖20种NH相位，共40例。

- 结果：40/40通过；
- 最晚首次同步：7.058 s；
- 最坏频率RMS：1.637 Hz；
- 最坏频率绝对误差P95：4.153 Hz；
- 最坏码误差P95：0.1138 chip；
- 结果：`/tmp/joint160-b1i-nh-phase/summary.json`；
- SHA-256：`098fe6cc862a775a4c84eb600a336f06da5bc48aea6ba6bf1173a377217cfaa1`。

## 已淘汰的B1I方案

- 未经NH修正的原始10 ms线性相干：物理上会随NH相位抵消，禁止采用；
- 1 ms Prompt平方后直接做160 ms频率观察：低信噪比下质量不足，三信号矩阵仅35/48；
- 完全照抄源码、对原始相邻1 ms鉴频结果平均：改善到41/48，但B1I仍有2个真实失控角点，不能冻结；
- 160 ms NH候选DFT：B1I达到15/16，仍有一个边界角点失控；
- 320 ms NH候选统计、160 ms更新消除了该失败，并通过全部NH相位专项。

## 当前代码状态与下一步

- 生产工作区尚未修改；
- 临时源码：`/tmp/startrack-joint160-src.xFLNy1`；
- 临时Release二进制：`/tmp/startrack-joint160-build-make/startrack`；
- 二进制SHA-256：`9de2b570eca06ab0e11a3be4ef26ca90a81bba55d94b139cfb9354f310f2b057`；
- 下一步是把临时逻辑收敛为独立、可测试的正式观察器，补齐确定性单测后再写入当前大面积未提交的StarTrack工作区；
- 正式落地前不修改环路带宽、状态机、DLL或同步时标。
