# 当前会话反馈

你指出的问题是对的：上一版只写了DLL的20 ms或160 ms更新周期，却没有说明码鉴相器怎样从五抽头得到码误差、环路滤波器怎样把误差变成码率，也就没有真正解释DLL。

本轮已经把第四章的码环主链补完整：

1. 明确DLL是Delay Lock Loop（延迟锁定环），由码相位鉴别器、PI环路滤波器和码NCO构成；
2. 解释VE/E/P/L/VL五抽头各自测量相关峰的哪个位置，以及$A_E=A_L$、$A_L>A_E$、$A_E>A_L$分别意味着什么；
3. 给出Prompt固定时的归一化Early–Late鉴别公式，并解释抽头间距$d$、零分母和正负方向；
4. 给出强入口的五抽头宽峰公式，说明先用$(p-2)d$判断相关峰位于Prompt哪一侧，再用相邻抽头细化码误差；
5. 明确Prompt中心模式的输出限制为$\pm0.25$ chip，宽峰牵引负责较大初差，Prompt固定模式负责局部细跟踪；
6. 给出增量式PI公式，解释比例项负责立即纠偏、积分项负责消除长期码率误差；
7. 给出标称码率、载波辅助码率和DLL剩余码率的完整求和关系，并增加L1 C/A载波多普勒$+1000$ Hz对应约$+0.649$ chip/s码率辅助的例子；
8. 分别写清L1 C/A、B1I和G1在强入口及中弱入口怎样形成五抽头观测量：强入口逐1 ms功率鉴相后20 ms平均，L1 C/A/G1中弱入口使用10 ms复相干幅值乘16，B1I中弱入口使用1 ms功率乘160；
9. 新增“DLL码鉴相与PI码环”原理图，并把第四章总览图中的模糊方框改成“Early–Late码鉴相”和“PI型载波辅助DLL”。

本轮按当前跟踪行为核对了宽峰索引、Early–Late符号、PI正负方向、观察无效时保持控制量以及载波辅助关系。重新从当前源码构建并运行`test_chip_dll_disc`、`test_pull_in_code_obs`和`test_code_loop`，3项全部通过；两张SVG均完成XML检查和Chrome实际渲染检查，没有裁切、文字越界或连线交错。本轮没有修改生产跟踪代码，也没有提交用户的Obsidian配置修改。
