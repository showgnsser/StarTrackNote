# 当前会话反馈

## 结论

5点DFT已经具备写入当前算法方案的实测依据，但必须明确是哪一种5点：

- L1 C/A、G1采用$\{-100,-50,0,+50,+100\}$ Hz；
- B1I采用$\{-125,-62.5,0,+62.5,+125\}$ Hz。

这组参数只是把11点减为5点，频率覆盖范围没有缩小。它可以直接作为当前中弱信号LongFast载波牵引的方案参数。

$\{-50,-25,0,+25,+50\}$ Hz是另一种更窄的5点方案。此前对它的验证发生在数据位同步模块，不是LongFast载波DFT，因此目前不能把它写成已经证明的LongFast参数。

## 已有实测证据

5点和11点使用相同真实IQ场景进行了A/B：

| 信号与场景 | 5点结果 | 11点结果 | 结论 |
|---|---:|---:|---|
| L1 C/A，$-62.5$ Hz | 通过 | 通过 | 5点没有退化 |
| L1 C/A，$+62.5$ Hz | 通过 | 通过 | 5点没有退化 |
| G1，$-62$ Hz | 通过 | 通过 | 5点没有退化 |
| G1，$+62$ Hz | 迟同步，短测试总判定失败 | 同样迟同步，短测试总判定失败 | 问题与频点数无关，11点没有改善 |
| B1I，$-62.5/-93.75/-125$ Hz | 三例通过 | 三例通过 | 5点覆盖边缘频差 |
| B1I，$\pm93.75$ Hz，3个种子 | 6/6通过 | 5/6通过 | 已测样本中5点不弱于11点 |

5点B1I六个保留种子的多普勒误差P95为$0.871\sim2.545$ Hz，码误差P95为$0.0054\sim0.0445$ chip，均明显处于现有频率和码精度门内。

## 证据边界

这组结果足以完成“11点还是5点”的工程选型，因为11点没有显示出额外收益，计算量却约为5点的$11/5=2.2$倍。但它还不是完整的八信号Qualification：L1 C/A和G1尚未完成该修改下的多种子、动态和长时耐久矩阵，G1正频差的迟同步问题也仍需单独关闭。

因此准确表述是：**5点全覆盖方案已经通过设计选型A/B，可以写入算法方案；完整发布资格仍待生产代码对齐及正式回归。**

当前生产工作区的LongFast配置仍默认11点，后续实现时需将该状态的频点数明确改为5，再执行定向CTest和正式回归。在生产代码完成对齐前，不能把现有11点二进制称为5点实现。

## 本轮文档处理

软件方案已改为5点全覆盖参数，流程图同步将“11点功率DFT”改为“5点功率DFT”。文档继续明确：若进一步把L1 C/A和G1的覆盖范围缩小为$\pm50$ Hz，必须重新测试，不能复用本轮结论。
