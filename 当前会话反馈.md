# 当前会话反馈

## 方案决定

接受简化方案。B1I牵引阶段不枚举NH/bit边界，不使用320 ms滑动窗，也不把位同步候选提前引入载波牵引。

B1I位同步前固定采用芯片式结构：

```text
连续1 ms Prompt
        ↓
相邻两个1 ms Prompt形成归一化叉积
        ↓
每对独立换算1 ms频率误差
        ↓
160 ms内对逐对频率误差求平均
        ↓
单次频率误差限制到±25 Hz
        ↓
三阶FLL
        ↓
每160 ms更新一次载波NCO
```

该160 ms窗口互不重叠：

- 观察长度160 ms；
- 更新周期160 ms；
- 没有320 ms历史；
- 没有160 ms步长的重叠滑窗；
- 没有NH相位候选；
- 没有牵引阶段的bit边界预测。

## 位同步保持独立

B1I正式位同步继续执行：

```text
11个频率候选 × 20种NH/bit边界
        ↓
候选NH剥离
        ↓
20 ms相干积分
        ↓
多个导航位功率非相干积累
        ↓
1 s搜索
        ↓
独立2 s确认
```

频率候选间隔12.5 Hz，总覆盖范围±62.5 Hz。只有独立确认通过后才发布NH/bit边界，牵引FLL不读取同步器的临时边界候选。

## 三种数据型信号的当前结构

### L1CA

- 10 ms线性相干；
- 16个短块功率非相干积累；
- 160 ms非重叠观察和更新；
- 11点DFT覆盖±62.5 Hz；
- 三点功率插值；
- 单次FLL输入限制到±25 Hz。

### G1

- 5 ms线性相干；
- 32个短块功率非相干积累；
- 其余观察、更新和频率网格与L1CA相同。

### B1I

- 不做未经NH恢复的10 ms线性相干；
- 使用相邻1 ms鉴频；
- 160 ms内平均；
- 每160 ms更新一次FLL；
- 不在牵引阶段使用DFT或NH边界枚举。

三种信号的位同步均使用11点频率搜索，但B1I在同步度量中额外按候选边界剥离NH码。

## 简化方案的真实IQ结果

### 三信号30 s矩阵

场景覆盖L1CA 22 dB-Hz、B1I 24 dB-Hz、G1 28 dB-Hz，残差频率`{-62.5,0,+62.5}` Hz、码差±0.5 chip、两个静态种子和±1 Hz/s动态，共48例。

- 46/48满足预设“16 s内同步＋后10 s精度”门；
- 另1例B1I在18.734 s完成同步，随后频率RMS 0.992 Hz、P95 1.902 Hz，属于迟同步而非失控；
- 因此30 s内最终完成同步且稳定的案例为47/48；
- 1个真实失败角为B1I `+62.5 Hz、+0.5 chip、-1 Hz/s、seed 20260831`，30 s内未同步且载波发散；
- 结果：`/tmp/joint160-matrix-30s/summary.json`；
- SHA-256：`b4b9031b63424e555acf77b9c782c6d2c3fbb8914ffaa996891b71c6fc631205`。

### B1I全部NH起始相位

固定24 dB-Hz、码差+0.5 chip、残差±62.5 Hz、seed 20260831，输入起点覆盖0～19 ms，共40例。

- 37/40满足预设16 s同步门；
- `-62.5 Hz、NH phase 5`在18.854 s完成同步，末段精度合格；
- 因此20 s内最终完成同步且稳定的案例为38/40；
- 两个真实失败角为`-62.5 Hz、NH phase 17`和`+62.5 Hz、NH phase 8`，均未同步且NCO明显发散；
- 结果：`/tmp/joint160-b1i-nh-phase/summary.json`；
- SHA-256：`aa46005def2bd0096c7539a9a62915e2faf4bbba3e54ce02d8a8ec76f78ec381`。

## 如何解释这些数字

上述47/48和38/40只是冻结场景中的通过比例，不是B1I真实失效概率，也没有足够样本计算概率置信区间。

它们能证明：

- 简化方案大多数情况下可以完成牵引和位同步；
- 失败集中在B1I的特定NH相位、频差方向和动态组合；
- 失败时可能出现NCO持续偏离，而不只是同步时间稍慢；
- 选择该方案意味着明确接受这种边界失效风险，以换取更简单的实现和更低计算量。

## 当前代码状态

- 该方案只在临时副本完成验证，正式StarTrack工作区尚未修改；
- 临时源码：`/tmp/startrack-joint160-src.xFLNy1`；
- 临时二进制SHA-256：`9b7ec19c9d1e4538dc8a7cf8b3f6ba86c927024b82364f6ee85efc8d660199ac`；
- 正式实现时不引入320 ms滑窗、NH候选牵引或新的质量门限；
- 位同步仍保留11点DFT和20种边界搜索。
