# 三种数据型信号牵引方案（当前讨论稿）

## 1. 已确认的共同原则

三个数据型信号的牵引方案统一遵守以下原则：

- 捕获等级只选择强入口或中弱入口；
- 载波牵引不枚举数据bit、NH或曼彻斯特相位；
- 所有频率候选都对连续1 ms Prompt逐点旋转后再积分；
- 数据相位候选只在位同步模块中搜索；
- 位同步前以FLL为主，不让未知符号污染PLL；
- 位同步完成后擦除符号，再汇合到统一正常跟踪路径。

## 2. 三种信号的物理差异

| 信号 | 1 ms相关结果中的符号结构 | 连续同号的最长可靠区间 |
|---|---|---:|
| L1CA | 一个导航bit保持20 ms | 最长20 ms，但边界未知 |
| B1I D1 | 导航bit叠加20-chip NH，每1 ms可能变号 | 未知NH相位时只能保证1 ms |
| G1 | 20 ms数据bit叠加10 ms正、10 ms负的meander | 最长10 ms，但边界未知 |

这决定了它们不能直接使用完全相同的原始相干长度，但可以使用同一套总体框架：

```text
连续1 ms Prompt
    ↓
符号不敏感处理或短相干分块
    ↓
频率候选旋转
    ↓
块内相干积分
    ↓
模平方
    ↓
块间非相干积分
    ↓
FLL频率观察
```

## 3. 强捕获入口

强入口的目标不是极限灵敏度，而是快速覆盖捕获残余频差。建议统一使用1 ms基线，在约20 ms内完成一次控制更新。

### 3.1 L1CA强入口

L1CA每20 ms最多出现一次数据bit翻转，因此可以使用相邻1 ms Prompt鉴频：

$$
R_k=P_{k-1}^{*}P_k
$$

$$
\widehat{\Delta f}_k=
\frac{\arg(R_k)}{2\pi\cdot1\text{ ms}}
$$

20 ms内约有19个相邻对，通常只有一个相邻对跨越数据bit边界。该异常对不会主导整个平均值。

```text
1 ms Prompt
    ↓
相邻叉点积鉴频
    ↓
20 ms内平均
    ↓
FLL更新
```

该方法不需要频率网格，无模糊范围宽，没有平方损失，并与芯片`signal_tracking_1ms()`的基本思路一致。数据bit边界由位同步模块独立搜索。

### 3.2 B1I强入口

B1I不能照搬L1CA。NH码每1 ms可能改变符号，普通相邻复积会频繁变号。建议先平方：

$$
Y_k=P_k^2
$$

再使用相邻平方复积：

$$
R_k=Y_{k-1}^{*}Y_k
$$

$$
\widehat{\Delta f}_k=
\frac{\arg(R_k)}{4\pi\cdot1\text{ ms}}
$$

因为：

$$
(+1)^2=(-1)^2=1
$$

NH码和导航bit的符号都会被消除。

```text
1 ms Prompt
    ↓
复数平方
    ↓
相邻平方复积鉴频
    ↓
20 ms内平均
    ↓
FLL更新
```

此时不搜索20个NH相位，NH相位只由位同步模块搜索。

物理无模糊范围为：

$$
|\Delta f|<\frac{1}{4T}
$$

当$T=1$ ms时：

$$
|\Delta f|<250\text{ Hz}
$$

±250 Hz是否覆盖强捕获移交的全部残余频差，需要用捕获输出合同和真实移交测试确认。

### 3.3 G1强入口

G1每10 ms发生一次meander翻转，20 ms观察中只有少数相邻对跨越符号边界。存在两种候选方法：

1. 普通相邻FLL：与L1CA相同，依靠20 ms内大多数相邻对没有跨边界；没有平方损失，频率范围约为±500 Hz，也更接近芯片强入口。
2. 平方相邻FLL：与B1I相同，彻底消除meander和数据bit符号；更稳健，但存在平方损失，范围缩小到±250 Hz。

当前优先候选为：

```text
G1强入口 = 普通相邻1 ms FLL + 20 ms平均
```

原因是G1符号翻转只发生在10 ms边界，不像B1I那样每1 ms都可能翻转。若真实测试表明边界污染明显，再与平方FLL比较。

## 4. 中弱捕获入口

中弱入口的目标是提高单次频率观察灵敏度。共同计算顺序必须固定为：

$$
\boxed{\text{1 ms结果先按频率候选旋转，再进行相干积分}}
$$

不能先做原始长相干再旋转。

对频率候选$f_m$，一个相干块为：

$$
Z_b(f_m)=
\sum_{k\in b}
P_k e^{-j2\pi f_m t_k}
$$

如果输入已经平方，则为：

$$
Z_b(f_m)=
\sum_{k\in b}
P_k^2e^{-j4\pi f_m t_k}
$$

然后形成非相干功率：

$$
M(f_m)=\sum_b|Z_b(f_m)|^2
$$

整个载波搜索只需要$N_f$个频率候选，不与位同步相位候选相乘。

### 4.1 L1CA中弱入口

建议使用：

```text
1 ms Prompt
    ↓
逐频点旋转
    ↓
每10 ms相干积分
    ↓
模平方
    ↓
多个10 ms块非相干积分
    ↓
DFT粗频点 + FMS细鉴频
```

L1CA数据bit长20 ms。位边界未知时，固定10 ms块可能跨bit边界，但连续20 ms中的两个10 ms块至少有一个不会跨越bit边界。跨边界块功率会下降，但不会与其他块复数相消，因为块间只累加功率。

初始待测候选为：

$$
10\text{ ms相干}\times16\text{块}=160\text{ ms观察}
$$

这个数值只是测试起点，尚未冻结。频率观察器不枚举20个bit边界。

### 4.2 B1I中弱入口

B1I不能使用原始10 ms相干，因为未知NH码会在10 ms内多次翻转。建议采用平方域DFT：

```text
1 ms Prompt
    ↓
复数平方，消除NH和数据bit符号
    ↓
逐物理频率候选做二倍角旋转
    ↓
平方域短相干积分
    ↓
模平方
    ↓
块间非相干积分
    ↓
DFT粗频点 + FMS细鉴频
```

对应公式为：

$$
Y_k=P_k^2
$$

$$
Z_b(f_m)=
\sum_{k\in b}
Y_k e^{-j4\pi f_m t_k}
$$

$$
M(f_m)=\sum_b|Z_b(f_m)|^2
$$

需要比较两个初始候选：

$$
10\text{ ms平方域相干}\times16\text{块}
$$

以及：

$$
20\text{ ms平方域相干}\times8\text{块}
$$

总观察时间都是160 ms，但动态损失、频率峰宽和噪声分布不同。当前更倾向先测试10 ms×16，因为平方后频率变化也加倍，较短块对残余动态更宽容，并能提供更多非相干样本。NH20相位枚举只在位同步模块中进行。

### 4.3 G1中弱入口

芯片源码使用5 ms短相干，这与G1的10 ms meander半周期相匹配。建议：

```text
1 ms Prompt
    ↓
逐频点旋转
    ↓
每5 ms原始相干积分
    ↓
模平方
    ↓
多个5 ms块非相干积分
    ↓
DFT粗频点 + FMS细鉴频
```

固定5 ms块即使没有与meander边界对齐，也会有一部分块跨越边界、功率下降，另一些块完整位于同号区间。块间只累加功率，不会发生跨块复数抵消。

初始待测候选为：

$$
5\text{ ms相干}\times32\text{块}=160\text{ ms观察}
$$

载波观察器不枚举meander相位，G1位同步模块独立搜索公开meander结构和数据bit边界。

## 5. 码环牵引

三个数据型信号的码环可以高度统一。对每个1 ms结果计算抽头功率：

$$
P_E=I_E^2+Q_E^2
$$

$$
P_L=I_L^2+Q_L^2
$$

基本码鉴别量为：

$$
e_{\mathrm{DLL}}=
\frac{P_E-P_L}{P_E+P_L}
$$

因为使用模平方，数据bit、NH和meander的正负号都会自然消失。因此：

- 强入口积累约20 ms码功率并快速更新；
- 中弱入口积累约160 ms码功率以提高灵敏度；
- 码环不枚举bit、NH或meander相位；
- DLL继续保留载波辅助。

## 6. 位同步与载波牵引的配合

不建议让位同步从移交第一毫秒开始永久积累一个超长窗口。更合理的时序是：

```text
载波FLL开始工作
    ↓
形成第一次可靠频率修正
    ↓
载波NCO发生较大粗调
    ↓
清除之前受频率误差污染的位同步窗口
    ↓
在较稳定载波下启动位同步搜索
    ↓
搜索阶段确认候选
    ↓
使用独立数据完成确认阶段
```

这样位同步通常只需要一个中心频率候选。频率补偿可以保留为配置项，但不应默认形成大频率网格。

| 信号 | 位同步搜索内容 |
|---|---|
| L1CA | 20 ms导航bit边界 |
| B1I | NH20相位及导航bit边界 |
| G1 | meander相位及20 ms数据bit边界 |

频率候选与这些相位候选不形成笛卡尔积。

## 7. 两条完整牵引流程

### 7.1 强捕获流程

```text
强捕获移交
    ↓
1 ms宽范围FLL
    ├─L1CA：普通相邻复积
    ├─B1I：平方相邻复积
    └─G1：普通相邻复积
    ↓
20 ms平均并更新载波
    ↓
20 ms码功率DLL
    ↓
位同步搜索与独立确认
    ↓
擦除数据覆盖码
    ↓
进入正常跟踪
```

### 7.2 中弱捕获流程

```text
中/弱捕获移交
    ↓
1 ms Prompt进入频率候选旋转
    ├─L1CA：原始Prompt，10 ms相干
    ├─B1I：Prompt平方，10 ms候选起步
    └─G1：原始Prompt，5 ms相干
    ↓
块内相干、块间功率非相干
    ↓
DFT粗频 + FMS细频
    ↓
较长功率DLL
    ↓
位同步搜索与独立确认
    ↓
擦除数据覆盖码
    ↓
进入正常跟踪
```

## 8. 运算量边界

按当前方案：

- 强入口没有DFT频率网格；
- 中弱入口只有$N_f$个频率候选；
- 位同步只有$N_{\mathrm{phase}}$个相位候选；
- 不产生$N_f\times N_{\mathrm{phase}}$的二维搜索。

B1I只增加一次平方域变换：

$$
P_k\rightarrow P_k^2
$$

平方后仍进入统一的“频率旋转—块内相干—块间非相干”框架。

## 9. 待测试确认的问题

当前可以固定算法分层，但以下数值仍属于待确认问题：

1. B1I平方域采用10 ms×16还是20 ms×8；
2. G1强入口普通FLL是否需要改成平方FLL；
3. 三种信号中弱入口需要几个频点；
4. 频点间隔和总搜索范围；
5. 160 ms是否足够覆盖各自捕获移交灵敏度；
6. 位同步在首次可靠FLL后延迟多久启动；
7. 切换到同步后正常跟踪所需的频率和码误差门限。

下一步优先测试B1I，比较：

- 当前raw 10 ms方案；
- 1 ms平方相邻FLL；
- 10 ms平方域DFT×16；
- 20 ms平方域DFT×8。

测试通过后，再把相同框架复用到L1CA和G1。
