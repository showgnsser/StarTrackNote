# 当前会话反馈

## 位同步前2.56秒平方Prompt DFT是什么

它是这几轮讨论中临时试验过的一套B1I普通入口载波牵引方案，不是历史540/540方案，也已经从当前B1I正式候选中删除。

“2.56秒”表示观察窗总长度，不表示把B1I信号连续相干积分2.56秒。它的实际结构是：

```text
1 ms Prompt
  → Prompt复数平方
  → 每10 ms相干累加
  → 256个10 ms功率非相干累加
  → 11点DFT选频
  → 每160 ms滑动发布一次频率结果
  → FLL更新载波NCO
```

因此总观察长度为：

$$
256\times 10\ \text{ms}=2.56\ \text{s}.
$$

## 为什么要平方Prompt

B1I的1 ms Prompt在位同步前仍带有NH20正负号。可把第$k$个Prompt简化写成：

$$
P_k=A_kc_k\mathrm{e}^{j\phi_k}+n_k,
$$

式中，$A_k$是相关幅度，$c_k\in\{+1,-1\}$是尚未对齐的NH20符号，$\phi_k$是残余载波相位，$n_k$是噪声。

相邻毫秒直接相加时，$c_k$的正负翻转会造成抵消。平方后：

$$
P_k^2\approx A_k^2\mathrm{e}^{j2\phi_k},
$$

因为$(+1)^2=(-1)^2=1$，未知NH20符号被消除；代价是载波相位和频率都变成二倍角，噪声也经过非线性平方，弱信号下会产生更强的平方噪声。

## 11点DFT怎样形成

对每个候选物理频率$f_i$，先按二倍角旋转平方Prompt，再把连续10个1 ms结果相加：

$$
S_{b,i}=\sum_{m=0}^{9}P_{10b+m}^{2}\,\mathrm{e}^{-j4\pi f_i t_{10b+m}}.
$$

式中，$b$表示第$b$个10 ms短块，$i$表示第$i$个候选频率，$t_k$是第$k$个Prompt的时间。

每个短块只保留功率，再把256个短块相加：

$$
J_i=\sum_{b=0}^{255}|S_{b,i}|^2.
$$

最终选择$J_i$最大的频率候选，并把它附近的频率残差交给FLL。由于短块之间只累加功率，所以这不是2.56秒相干积分，而是“10 ms相干、2.56 s非相干观察”。

## 为什么当时会设计它

当时想把两件事拆开：

1. 位同步前先利用平方Prompt把残余频率牵引到中心；
2. 载波居中后，位同步器只搜索20个NH20相位，不再枚举频率。

这样表面上可以把“11个频率×20个NH相位”的联合搜索拆成两个一维模块。但它改变了原来已经验证过的闭环时序。

## 为什么最终否决

主要问题不在DFT公式本身，而在它被放到了闭环反馈中：

1. 码偏差接近正负0.5 chip时，Prompt相关幅度已经明显下降，频率峰更容易受噪声和旁峰影响；
2. 2.56 s窗口内DLL和载波NCO都可能已经改变，早期样本与当前控制中心不再属于完全相同的闭环状态；
3. 相邻输出每160 ms发布一次，但共享绝大多数历史样本，因此不是独立频率证据；
4. 一次错误频率结果会先修改载波NCO，随后又改变Prompt相位和码环载波辅助，形成码频耦合；
5. 平方虽然消除了NH符号，但在24 dB-Hz下会引入明显的平方噪声。

实测表现正是如此：该候选27例只通过21例，6个失败全部集中在负0.5 chip；失败时频率观察仍被判为有效并持续送入FLL，最终载波和码环一起发散。

## 当前B1I普通入口已经不用该方案

当前恢复为已经验证的联合位同步结构：

```text
捕获载波NCO保持
  + 160 ms功率DLL持续修正码偏差
  + 位同步器联合搜索11个频率和20个NH20相位
  → 搜索成功后一次性校正载波NCO
  → 独立确认并锁定位边界
```

当前11个频点间隔12.5 Hz，覆盖正负62.5 Hz，不使用额外局部细化。同一27例真实IQ结果为27/27通过。

B1I强入口仍可使用短周期预同步FLL；被删除的只是B1I普通中弱入口的2.56 s平方Prompt DFT反馈路径。
