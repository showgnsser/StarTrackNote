# 5 Hz频率门的来源与粗差验证方法

## 1. 结论

当前LongSlow状态的5 Hz门不是芯片源码原有逻辑，而是StarTrack后续增加的稳健性保护。它的出发点是防止低信噪比DFT偶发选错频率峰后，把一次较大的频差直接写进三阶FLL，造成NCO和频率变化率状态游走。

这个门不能直接当作最终合理参数。现有真实IQ数据表明，它对L1CA和G1确实拦住了一批误差更大的观察，但在B1I负频差场景中没有表现出有效的粗差分离能力，存在拒绝正常测量的可能。

## 2. 当前5 Hz门怎样工作

LongSlow的频率观察器可以输出约±25 Hz范围内的结果，但控制入口按以下规则处理：

1. 当前残差绝对值不超过5 Hz：直接送进FLL；
2. 当前残差超过5 Hz：先拒绝；
3. 如果后续至少三条绝对频率候选彼此相差不超过3 Hz，并且证据之间已经替换了一个完整观察跨度，则认为这不是孤立粗差，重新建立频率参考并允许进入FLL。

所以它不是简单的“超过5 Hz永远删除”，而是“小变化立即采用，大变化延迟确认”。

该逻辑位于：

- `src/monitor/freq_guard.cpp`的`FreqGuard::check()`；
- `src/tracking/track_engine.cpp`的`freqGuardConfig()`为LongSlow指定`direct_hz=5.0`。

## 3. 芯片源码怎样处理

芯片参考代码没有逐条5 Hz硬拒绝。

其主要流程为：

1. `get_freq_error()`从DFT频点功率形成频差，并把输出限制在约±25 Hz；
2. 每条频差都进入`ul_freq_rms`和`l_freq_mean`的IIR统计；
3. `update_loop_status()`检查失锁、状态切换和动态；
4. 正常情况下直接调用`carrier_loop_filter()`，把本次频差送入三阶FLL；
5. `dynamic_check()`用频差均值和绝对值IIR的比例判断高动态或低动态，不负责删除单条观察。

可选的高精度误跟踪检测会在频率峰连续约50次停留于边缘频点时触发重捕，但这也不是孤立5 Hz结果剔除。

因此芯片依靠的是鉴频输出限幅、较窄环路系数、长期均值/RMS和状态切换，而不是当前StarTrack这种逐条创新门。

## 4. 现有真实IQ的初步真值审计

对静态场景，可以用日志中的PDI NCO加原始频率残差近似还原观察到的绝对频率，再与场景真实频率比较。下面统计的是绝对误差均值：

| 场景 | 被采用观察 | 被拒绝观察 | 初步判断 |
|---|---:|---:|---|
| L1CA 22 dB-Hz，-62.5 Hz | 3.42 Hz | 6.19 Hz | 拒绝组明显更差 |
| L1CA 22 dB-Hz，+62.5 Hz | 2.30 Hz | 4.73 Hz | 拒绝组更差 |
| G1 22 dB-Hz，-62 Hz | 2.70 Hz | 6.35 Hz | 拒绝组明显更差 |
| B1I 24 dB-Hz，-62.5 Hz | 3.58 Hz | 3.49 Hz | 没有分离，可能误杀 |

这说明5 Hz门不是完全没有价值，但固定门限并不适合直接推广到所有信号和状态。

这组数字仍是初步证据，因为当前CSV只记录当前PDI的硬件NCO，没有记录频率观察窗自己的平均NCO参考。长窗内NCO发生变化时，两者并不完全相同，必须补充只读诊断后才能得到精确结论。

## 5. 怎样精确判断一条结果是不是粗差

### 5.1 补齐只读诊断

每条成熟频率观察需要额外记录：

- 观察窗平均NCO：`result.freq.nco_hz`；
- 原始频率残差：`result.freq.freq_hz`；
- 观察中心时刻：`result.freq.center_s`；
- 重定时到控制边界后的残差；
- 粗差门的接受或拒绝原因；
- 更新前后的载波NCO和Hz/s状态。

仿真真值只由分析器按`center_s`计算，绝不能进入TrackEngine或参与控制。

### 5.2 对每条观察计算真实误差

观察器给出的绝对频率为：

$$
\widehat f_{obs}=f_{NCO,obs}+\Delta f_{obs}
$$

在同一个观察中心时刻取场景真值$f_{true}$，定义：

$$
e_{obs}=\widehat f_{obs}-f_{true}
$$

再比较门控前NCO预测误差：

$$
e_{pred}=f_{NCO,pred}-f_{true}
$$

如果$|e_{obs}|$明显大于$|e_{pred}|$，采用该观察只会让NCO变差，它才有充分理由被称为粗差。仅仅因为残差超过5 Hz，不能证明它是错的；在真实动态中，它可能正是环路急需采用的校正量。

### 5.3 做反事实环路回放

对同一条PDI和同一条频率观察并行运行三个只读环路副本：

1. 当前方案：5 Hz直接门加三次确认；
2. 芯片式方案：不删除单条结果，只保留±25 Hz鉴频限幅和原环路带宽；
3. 软稳健方案：不完全删除，而是按创新大小降低权重或限制单次校正量。

比较下一次观察前的NCO误差、频率RMS/P95、变化率误差、失锁次数和码误差。这样可以直接知道“拒绝本条观察”到底改善还是恶化了后续闭环。

## 6. 建议的A/B场景

至少覆盖：

- L1CA、B1I、G1；
- 静态±62.5 Hz；
- 低信噪比目标档；
- ±1 Hz/s连续频率变化；
- TCXO随机频率变化；
- 166 Hz/s高动态强入口；
- 每种情况至少三个固定种子。

门限选择不能只看频率观察使用率。优先级应为：不失步、最终频率和码精度、真实动态响应、错误观察造成的NCO最大跳变，最后才比较观察使用率。

## 7. 当前建议

在完成上述A/B前，不应直接删除5 Hz门，也不应为了让使用率达到0.95而放宽它。更准确的判断是：

- 5 Hz门是StarTrack的工程扩展，不是芯片源码合同；
- 它已经在L1CA和G1样本中表现出一定粗差抑制作用；
- 它在B1I样本中可能过度拒绝；
- 下一步应补只读诊断并做同一PDI的反事实环路A/B，再决定保留、分信号配置，或改为软稳健校正。
