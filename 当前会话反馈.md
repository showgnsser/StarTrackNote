# 当前会话反馈

## 当前DFT的实际使用方式

当前实际工作树中，数据型普通入口的DFT只在位同步器内部使用。它不是对原始ADC数据做FFT，也不是把1.28 s或2.56 s数据做长相干DFT，而是对硬件已经输出的连续1 ms Prompt复相关执行五个频率假设。

### 1. 输入

硬件每1 ms输出一条Prompt复相关：

\[
P[k]=I[k]+jQ[k].
\]

它已经经过载波NCO混频、本地码相关和1 ms相干积分。DFT不再读取原始IQ。

### 2. 五个频率候选

三种数据型信号当前统一配置：

\[
f_m\in\{-62.5,-31.25,0,+31.25,+62.5\}\text{ Hz}.
\]

也就是5个频点、31.25 Hz间隔、总覆盖范围125 Hz。

### 3. 每条1 ms结果怎样进入候选频点

对于频率候选\(f_m\)，第\(k\)条1 ms Prompt旋转为

\[
\widetilde P_m[k]
=P[k]\left(\cos\theta_{m,k}-j\sin\theta_{m,k}\right),
\qquad
\theta_{m,k}=2\pi f_m t_k .
\]

这一步的含义是：假设当前残余频率就是\(f_m\)，把该频率产生的逐毫秒相位转动消除。如果候选频率正确，旋转后的相邻Prompt相位基本对齐；候选错误时，相干相加会互相抵消。

### 4. 频率和bit边界联合搜索

位同步器同时枚举：

- 5个频率候选；
- 20个bit/NH/meander边界候选。

每个候选边界内部，连续20条1 ms结果形成一个复数和：

\[
S_{m,b,r}
=\sum_{n=0}^{19}c_b[n]\,\widetilde P_m[b+20r+n].
\]

式中：

- \(m\)是频率候选编号；
- \(b\)是边界候选，范围0至19 ms；
- \(r\)是当前统计窗中的第\(r\)个完整20 ms块；
- \(c_b[n]\)是公开覆盖码。L1 C/A固定为+1，B1I使用候选NH20相位，G1使用候选meander相位。

不同20 ms块之间不做复数相加，只累加功率：

\[
J(m,b)=\sum_r\left|S_{m,b,r}\right|^2.
\]

所以当前1 s搜索的真实处理顺序为：

    1 ms Prompt
      → 5个频率候选逐条旋转
      → 每个候选下枚举20种边界
      → 每20 ms内部相干
      → 50个20 ms结果做功率非相干积累
      → 在5×20=100个假设中选择最大值

它不是1 s相干积分。

### 5. 为什么需要1019条PDI

首次搜索需要1000 ms完整数据。为了让边界0至19的每一种候选都能取到连续1000 ms，缓存容量为

\[
1000+20-1=1019\text{ ms}.
\]

这19 ms是边界余量，不是额外相干长度。

### 6. 怎样得到频率结果

二维功率最大的\((m,b)\)给出粗频点和边界。如果最大频点不在频率网格两端，再使用左右相邻频点做三点抛物线插值；如果最大值位于正负62.5 Hz边缘，则保留边缘粗频率，不向范围外插值。

第一次搜索通过后，频率结果不会再送进一条长窗FLL。软件只在这个边界执行一次：

\[
f_{\mathrm{NCO,new}}
=f_{\mathrm{NCO,old}}+\widehat{\Delta f}.
\]

只修改载波NCO频率控制量，不跳载波相位。

### 7. 独立确认

校频后清空第一次搜索数据，再收集独立的2 s数据。确认阶段只比较20个边界，不再搜索5个频点，因为第一次搜索得到的频差已经写入NCO。

如果搜索或确认质量不足，统计长度按1 s、2 s、4 s增长；这里增长的是非相干统计数据量，每个未知导航bit内部仍只相干20 ms。

### 8. 强入口与普通入口的区别

- 强入口：先由相邻Prompt FLL完成短周期牵引，频率确认后才开放上述位同步搜索。
- 普通入口：当前WIP直接开放五频点×二十边界联合搜索，并在搜索期间保持捕获NCO不被另一条预同步DFT重复修改。

### 9. 同步后的DFT是另一套模块

完成位同步后，稳态载波鉴频由FreqBinObserver负责。它仍从连续1 ms Prompt开始，在已知bit边界内形成20 ms相干块，再按状态配置对多个块做功率非相干积累。

当前状态表中的稳态DFT仍主要是11点，例如：

- ShortNormal、ShortSlow：11点、10 Hz间隔、20 ms观察；
- LongFast、LongNormal：11点、10 Hz间隔、8个20 ms块，共160 ms；
- LongSlow：11点、10 Hz间隔、16个20 ms块，共320 ms。

因此本轮“五点”目前只改到了数据位同步阶段，还没有把同步后的全部状态DFT改成五点。

## 当前代码状态

- 位同步五点配置已经存在于实际工作树；
- 普通入口不再调用旧的1.28 s/2.56 s PullInFreqObs；
- PullInFreqObs源文件和独立单元测试仍保留，但不在当前数据型普通入口路径中；
- 当前方案尚未提交；
- 完整CTest仍有L1 C/A码峰边缘合成角失败，因此这还是待修候选，不是已经冻结的生产方案。
