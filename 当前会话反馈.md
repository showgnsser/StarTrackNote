# 三种数据型信号牵引与位同步测试结果

## 1. 本轮结论

当前方案已经完成第一轮实现与多场景验证：L1CA、B1I和G1共10个真实IQ短场景全部完成位同步，强入口、普通入口、正负捕获残差以及B1I的频率搜索边界均有覆盖。

这轮结果证明的是“当前牵引方案能够把频率拉回中心，并让后续位同步正常工作”，还不是三种信号的完整资格结论。尤其普通入口的部分短场景虽然已经同步且频率、码精度合格，但最后3秒计分窗内的频率观察使用率低于0.95，后续正式资格仍需单独处理。

## 2. 已落实的方案

### 2.1 强捕获入口

- L1CA：连续1 ms Prompt做相邻叉积、点积鉴频，在20 ms内平均。每个20 ms数据符号最多只污染一个相邻对，不使用复数平方，避免低信噪比损失。
- B1I：1 ms Prompt先做复数平方，再做相邻二倍角FLL，消除逐毫秒NH码和数据bit正负号。
- G1：与L1CA相同，使用普通相邻1 ms FLL；10 ms meander边界只污染少量相邻对，不使用平方FLL。
- 强入口在ShortFast到ShortNormal之间连续交接三阶FLL已经建立的物理频率变化率，避免高动态换档时重新从零辨识趋势。

### 2.2 普通捕获入口

- L1CA：10 ms短相干块，在1280 ms内做块间非相干频率搜索。
- B1I：10 ms平方域短相干块，在2560 ms内做块间非相干频率搜索。
- G1：5 ms短相干块，在1280 ms内做块间非相干频率搜索。
- 频率候选只属于载波牵引，不枚举bit、NH或meander相位。
- LongFast预同步长窗可以校正频率，但高度重叠的结果不能直接作为独立Hz/s证据；在获得独立变化率资格前，缺测时保持当前NCO，不跨缺口外推内部趋势。

### 2.3 位同步开放条件

- 位同步默认只使用中心频点，不再默认扩展为多频点与相位的笛卡尔搜索。
- 只有载波牵引形成连续、可信且已经进入中心区的频率结果后，才开放位同步窗口。
- 位同步相位候选仍由各信号自己的同步模块处理：L1CA搜索20 ms bit边界，B1I搜索NH/bit边界，G1搜索meander/bit边界。
- 资格判断使用接收机内部的NCO和频率观察，不读取仿真真值。

## 3. 关键修改位置

- `src/carrier_observer/prompt_fll_obs.cpp`：增加可选平方二倍角FLL；生产配置只给B1I启用。
- `src/carrier_observer/pull_in_freq_obs.cpp`：支持5/10 ms短相干块、平方域B1I处理和LongFast专用NCO参考重定时。
- `src/tracking/track_engine.cpp`：配置三种信号的强/普通入口，增加牵引资格门，保持ShortFast到ShortNormal的高动态趋势连续，并在资格成立前阻断位同步。
- `src/sync/signal_sync.cpp`：增加位同步开放门。
- `src/sync/data_bit_sync.cpp`：默认使用单中心频点，同时保留可配置多频点能力。
- `tests/carrier_observer/test_prompt_fll_obs.cpp`、`test_pull_in_freq_obs.cpp`：覆盖平方FLL、短相干块和NCO换字重定时。
- `tests/tracking/test_data_track_engine.cpp`、`test_source_entry_path.cpp`、`test_state_dynamic.cpp`：覆盖三信号强/普通入口、正负频差、位同步时序和166 Hz/s高动态。

## 4. 真实IQ短矩阵

测试二进制：`/tmp/startrack-data-pull-build/startrack`

二进制SHA-256：`5174ee7f2b05b6b053c882015d37bc6ae1ac9b8faac9ed5e36fa0959c5c72793`

汇总：`/tmp/startrack-data-pull-iq/summary.json`

汇总SHA-256：`cc8ec35d60fb8ede1cb85e62489ad05155000714b6db64a30b81b46764d192a0`

| 场景 | 入口 | C/N0 | 捕获残差 | 首次同步 | Doppler RMS/P95 | 码P95 | 结果 |
|---|---|---:|---:|---:|---:|---:|---|
| L1CA strong | 强 | 40 dB-Hz | +62.5 Hz | 3.599 s | 0.887/2.123 Hz | 0.0059 chip | 同步与严格门通过 |
| L1CA normal | 普通 | 22 dB-Hz | -62.5 Hz | 8.659 s | 2.444/2.778 Hz | 0.0166 chip | 同步与精度通过；使用率0.556 |
| L1CA normal | 普通 | 22 dB-Hz | +62.5 Hz | 8.659 s | 1.944/2.455 Hz | 0.0338 chip | 同步与精度通过；使用率0.667 |
| B1I strong | 强 | 40 dB-Hz | +62.5 Hz | 3.519 s | 1.315/2.334 Hz | 0.0020 chip | 同步与严格门通过 |
| B1I normal | 普通 | 24 dB-Hz | +62.5 Hz | 9.939 s | 0.882/1.584 Hz | 0.0302 chip | 同步与严格门通过 |
| B1I normal | 普通 | 24 dB-Hz | -62.5 Hz | 6.399 s | 2.055/3.268 Hz | 0.0121 chip | 同步与精度通过；使用率0.556 |
| B1I normal边界 | 普通 | 24 dB-Hz | -125 Hz | 15.099 s | 0.885/1.818 Hz | 0.0320 chip | 同步与严格门通过 |
| G1 strong | 强 | 40 dB-Hz | +62.5 Hz | 3.599 s | 0.501/0.883 Hz | 0.0014 chip | 同步与严格门通过 |
| G1 normal | 普通 | 22 dB-Hz | -62 Hz | 8.819 s | 0.831/1.752 Hz | 0.0178 chip | 同步与精度通过；使用率0.778 |
| G1 normal | 普通 | 22 dB-Hz | +62 Hz | 12.679 s | 4.259/8.143 Hz | 0.0627 chip | 15 s内完成同步与精度门 |

G1 `+62 Hz`场景另延长到18 s复跑，首次同步仍为12.679 s；最后3秒同步率、频率有效率和使用率均为1.0，Doppler RMS/P95为1.707/2.339 Hz，码P95为0.0166 chip，严格门通过。由此确认原15 s短例只是同步后计分时间不足。

## 5. 确定性与完整回归

定向回归命令：

```bash
ctest --test-dir /tmp/startrack-data-pull-build --output-on-failure \
  -R 'test_(state_normal_chain|state_recovery|state_dynamic|track_engine|source_entry_path|closed_loop_state_rig|data_track_engine)$' -j 1
```

结果：7/7通过。其中166 Hz/s、25 dB-Hz的L1CA高动态场景在9.473 s完成位同步，随后进入HighDynamic，证明强入口换档不再丢失FLL趋势。

完整回归命令：

```bash
cmake --build /tmp/startrack-data-pull-build --parallel 1
ctest --test-dir /tmp/startrack-data-pull-build --output-on-failure -j 1
```

结果：112/113通过，`test_state_tcxo`按设计跳过。唯一失败是`eight_signal_status`：该测试要求当前Git HEAD与冻结机器摘要完全相同；当前仓库存在尚未提交的算法WIP，因此身份检查按设计失败。所有功能、状态、观察器、runner、分析器、style和dependencies测试均通过。

附加检查：

- `python3 skills/startrack-cpp-style/scripts/check_startrack_style.py .`：通过；
- `git diff --check`：通过；
- AGENTS.md中提到的`scripts/validate_fixed_catalog.py`和`scripts/validate_signal_campaign.py`在当前仓库中不存在，无法单独执行；相关知识库、活动runner和依赖检查已经由完整CTest覆盖并通过。

## 6. 仍需处理

1. 普通入口四个短场景的最后3秒频率观察使用率为0.556至0.778，低于正式`>=0.95`门。它不影响本轮位同步结论，但进入完整跟踪资格前必须解释是状态观察节拍的统计口径问题，还是确实存在观察拒绝。
2. 当前只有10个代表场景和一个高动态确定性场景，不构成多种子、全频差、全bit/NH/meander相位资格矩阵。
3. StarTrack源码仍位于大型未提交WIP中，本轮没有提交或推送源码，避免把不相关改动混成一次提交。

## 7. 当前判断

三种数据型信号的架构方向已经得到第一轮闭环支持：强入口使用低延迟符号不敏感FLL，普通入口使用短相干块加长非相干观察，频率候选留在牵引，符号相位候选留在位同步。当前最值得继续做的不是再改位同步算法，而是扩大多种子与相位矩阵，并单独收口普通入口的频率观察使用率。
