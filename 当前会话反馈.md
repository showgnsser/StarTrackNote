# 当前会话反馈

你对第一次粗频观察的判断是正确的。B1I使用$\{-250,-125,0,+125,+250\}$ Hz五点时，$\pm125$ Hz确实与频点完全对齐；如果DFT只执行一次，并把NCO直接跳到正确频点，第一次观察不会产生相干损失。

当前LongFast不是一次性粗频搜索，而是一个连续闭环观察器：2560 ms功率窗每160 ms发布一次结果，FLL每次只完成有限校正，后续观察继续使用滑动窗。因此第一次校正后，残差会经过125 Hz与0 Hz之间的连续数值，不会始终停在某个DFT整点。

B1I先把Prompt平方，候选$f_m$下的10 ms相干幅度近似满足

$$
|C(f_m)|
\propto
\left|
\operatorname{sinc}
\left(
2(\Delta f-f_m)T_c
\right)
\right|,
\qquad
T_c=10\ \mathrm{ms}.
$$

式中，$\Delta f$表示当前物理残余频率；$f_m$表示候选物理频率；$T_c$表示短相干长度。平方使相位转速加倍，因此主瓣第一零点满足$|\Delta f-f_m|=50$ Hz。

五点$\pm250$ Hz网格的间隔为125 Hz，远大于50 Hz主瓣半宽。第一次对齐后，只要FLL把残差移到两个频点之间，三个相邻功率就不再是同一主瓣上的连续采样，抛物线插值失去物理基础，粗频点会在$-125/+125$甚至$-250/+250$之间跳变。

11点网格本身也包含$\pm125$ Hz：

$$
\{-125,-100,-75,-50,-25,0,+25,+50,+75,+100,+125\}\ \mathrm{Hz}.
$$

它的间隔只有25 Hz，小于50 Hz主瓣半宽。FLL校正后，残差经过中间值时仍能由相邻频点连续观测和插值。因此11点的优势不在第一次$\pm125$ Hz对齐，而在闭环校正过程中的连续频率覆盖。

日志也对应这一机理：五点方案的粗频结果在$-125$ Hz和$+125$ Hz之间反复跳变，随后跳到$\pm250$ Hz；11点方案则依次经过约$-100$、$-75$、$-50$、$-10$和$0$ Hz并完成收敛。

所以结论不是“B1I理论上不能使用5点”，而是“当前固定网格、长滑窗和渐进FLL结构不能只把频点数改成5”。若坚持5点，需要改成一次性粗搜后直接重置NCO和观察窗，再建立局部细频网格；那属于另一套两级牵引算法，不能作为本次配置修改处理。
