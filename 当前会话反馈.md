# 当前会话反馈

## 本次问题

asin鉴频器相对atan究竟强在哪里？atan是否也能利用L1CA和G1符号边界稀疏的特点？

## 直接结论

**atan当然可以利用稀疏边界，而且这比“每一对独立做二象限折叠”更合理。**

上一轮测试的逐对atan为每个相邻1 ms复积单独执行`atan(cross/dot)`。它在数学上自动消除正负符号，但等价于每一对都根据dot符号独立选择半周分支。低SNR时dot经常因噪声变号，因此产生20次相互独立的硬判决，没有使用“一个20 ms窗口内只有极少数真实符号边界”这个时间先验。

更好的强入口方案应保留四象限atan2的完整角度，先用窗口内绝大多数正常相邻对确定公共频差，再修正少数半周边界。

## 当前asin方案真正强在哪里

当前逐对asin使用：

$$
\widehat{\Delta f}_k=
\frac{1}{2\pi T}
\operatorname{asin}
\left(
\frac{cross_k}{|P_{k-1}||P_k|}
\right).
$$

它的优势不是asin函数具有更高的理论估计精度，而是当前结构具有三个工程特性：

1. **不使用dot做逐对分支判决。** dot被噪声推过零点时，不会立刻发生半周分支跳变。
2. **输出天然有界。** 当$T=1$ ms时，每对输出被限制在`[-250,+250] Hz`，单个噪声样本不会产生接近`±500 Hz`的四象限异常值。
3. **边界只造成有限偏差。** 无噪声情况下，一个真实bit边界只使一对频率反号。20对平均时，19对为$+f$、1对为$-f$，最终得到$0.9f$，而不是产生一个半周大粗差。

所以当前asin更像一种“有界、忽略dot象限、容忍少量边界污染”的稳健工程鉴频器。它在低SNR闭环中看起来更稳定，主要来自这些结构特性，而不是asin在信息论上优于atan。

其代价也很明确：

- 没有充分使用dot携带的相位信息；
- 高SNR下频率精度通常不如正确的atan2；
- 真实边界仍会产生固定比例偏差；
- 接近无模糊边界时非线性明显。

## 为什么刚才的逐对atan没有利用稀疏边界

两象限atan写为：

$$
\widehat{\Delta f}_k=
\frac{1}{2\pi T}
\operatorname{atan}\left(\frac{cross_k}{dot_k}\right).
$$

它对理想正负符号不敏感，因为bit翻转会让dot和cross同时变号，比值不变。但在低SNR下，这个比值并不知道dot变号是由真实bit边界还是噪声造成。它把两者一律按符号翻转处理。

因此该算法利用的是“BPSK正负号在单对复积中可以约掉”，不是“整个窗口只有一个或两个边界”。这两个先验完全不同。

## 利用稀疏边界的atan方案

L1CA和G1强入口可以采用以下窗口级方案。

### 第一步：保留每对完整四象限角度

$$
\phi_k=\operatorname{atan2}(cross_k,dot_k),
\qquad -\pi<\phi_k\le\pi.
$$

正常相邻对集中在真实相位增量$\Delta\phi$附近；发生符号翻转的少数边界对集中在$\Delta\phi\pm\pi$附近。

### 第二步：用多数正常相邻对确定主簇

在20个角度中使用中值或圆周稳健中心得到$\phi_0$。L1CA和G1只有极少数真实边界，正常相邻对占明显多数，因此主簇位置不依赖单个dot的正负判断。

### 第三步：以主簇为参考修正半周边界

对每个$\phi_k$选择：

$$
\phi'_k\in\{\phi_k-\pi,\phi_k,\phi_k+\pi\},
$$

使$\phi'_k$与$phi_0$的距离最小。最后平均全部$phi'_k$：

$$
\widehat{\Delta f}=
\frac{1}{2\pi T}
\frac{1}{N}
\sum_{k=1}^{N}\phi'_k.
$$

这个结构具有以下特点：

- 使用完整dot和cross信息；
- 真正利用窗口内边界稀疏的事实；
- 不对20个dot分别作互不关联的半周硬判决；
- 边界对修正后仍可参与平均，不必简单丢弃；
- 不需要用真值，也不需要人为设置频率粗差门限。

对于正式残差`±62.5 Hz`，主簇远离1 ms四象限的`±500 Hz`回绕边界，中值初始化具有足够余量。

## L1CA与G1的差异

- L1CA的导航bit边界每20 ms出现一次，但相邻bit可能同号，因此实际半周跳变至多稀疏出现。
- G1还有公开meander结构。当前工程模板为前10 ms正、后10 ms负的20项周期，边界位置具有固定结构；高SNR首窗测试中，普通逐对atan已经把频率RMSE从asin约21.74 Hz降到约1.74 Hz，证明边界处理是G1的主要收益点。

两者可以共享“窗口主簇＋半周重映射”的数学框架，但G1还可进一步利用已知meander相位候选，不能简单照抄L1CA边界数量假设。

## B1I为什么不能直接共用

B1I的NH符号逐毫秒变化，不属于稀疏边界。窗口内大量相邻对都可能带半周翻转，多数主簇假设不成立。因此B1I必须单独设计：

- 当前平方Prompt＋asin在代数上消除NH符号，但二倍角把无模糊范围压到`±125 Hz`；
- 平方Prompt＋二象限atan使正式`±125 Hz`落在模糊边界，不可采用；
- 未平方的两象限atan可保留`±250 Hz`范围，高SNR和32 dB-Hz初测均能同步，但需要单独验证NH相位、符号边界和低SNR门限。

## 芯片源码中的对应提示

芯片重建源码`signal_tracking_1ms()`在cross鉴频位置留有注释：GPS在20 ms内没有bit变化时可以使用atan2。这说明原作者也知道完整atan2在无边界区间更合适，但现有可见路径没有完成这一优化，而且位同步前任意20 ms窗口未必与bit边界对齐。

因此合理的整改不是直接把cross/asin替换成逐对二象限atan，而是实现“完整atan2＋窗口稀疏边界处理”。

## 当前决定

1. 不再把asin描述成理论上比atan更强；asin当前优势是有界和低SNR稳健。
2. L1CA、G1强入口下一候选采用四象限atan2与窗口级稀疏边界处理。
3. 中弱normal入口继续使用长观察功率搜索，不随强入口改动。
4. B1I单独设计，不进入L1CA/G1稀疏边界方案。
5. 该候选尚未写入生产代码，必须先做同一Prompt离线A/B，再做真实IQ闭环。
