# 当前会话反馈

## 直接结论

是的。临时验证中的B1I方案虽然没有正式宣布“位同步成功”，但它在算法本质上已经提前进行了NH相位，也就是20 ms数据边界的候选预测。

B1I的20位NH码与20 ms导航数据位对齐，因此：

```text
枚举20种NH起点
        ≈
枚举20种数据位边界
```

## 临时方案实际做了什么

在每个320 ms频率观察窗内，对20种NH起点和11个频率候选计算功率：

```text
20种NH起点 × 11个频率候选
        ↓
每个候选按NH符号恢复1 ms Prompt
        ↓
形成20 ms相干块
        ↓
不同20 ms块累加功率
        ↓
选择功率最大的“NH起点＋频率”组合
```

这里得到的最佳NH起点只用于生成本次频率观察：

- 不写入正式同步状态；
- 不对后续实时PDI固定剥离NH；
- 不开放长相干跟踪；
- 下一个160 ms更新时重新搜索，候选可以变化。

因此它不是“已经完成位同步”，但确实属于“位同步候选的预估”。

## 为什么会采用这个临时方案

B1I在NH未知时不能直接进行10 ms线性相干。已经测试过的两种纯载波方案均不够可靠：

- 1 ms Prompt平方后鉴频，在低信噪比下噪声过大；
- 完全照抄芯片的原始相邻1 ms鉴频平均，在16个B1I角点中仍有2个失控。

加入NH候选后，等价于先恢复每个候选下的20 ms有效能量，频率观察才足够稳定。320 ms统计窗、160 ms更新通过了16/16 B1I矩阵和40/40全部NH相位专项。

## 这是不是最终推荐结构

不建议在正式实现中保留“一个牵引NH搜索器＋一个独立位同步器”两套重复逻辑。更整洁的最终结构应该是一个共享的B1I二维估计器：

```text
连续1 ms Prompt
        ↓
共享的11频点 × 20边界统计
        ├─ 统计达到320 ms：只发布临时频率观察给FLL
        │                  不宣布位同步
        └─ 统计达到1 s，并由独立2 s数据确认：
                           发布正式NH/bit边界
```

也就是说：

- 牵引和位同步共享同一组二维统计；
- 320 ms输出是暂态频率估计；
- 1 s搜索和2 s独立确认才决定正式位同步；
- 不重复计算两套NH候选；
- 位同步失败或候选变化时，载波FLL仍可继续获得频率观察。

## 必须诚实保留的结论

如果要求“牵引阶段绝对不估计NH相位”，当前测试还没有找到在B1I 24 dB-Hz、±62.5 Hz、±0.5 chip全部角点可靠通过的方案。

所以当前有两种选择：

1. 接受共享二维估计器：牵引阶段允许内部预测NH边界，但只有独立确认后才正式锁定；
2. 坚持牵引与位同步完全分离：需要继续寻找比源码1 ms鉴频和平方鉴频更可靠的符号不敏感鉴频器，当前尚未验证成功。

临时方案属于第一种，但正式生产代码尚未修改。
