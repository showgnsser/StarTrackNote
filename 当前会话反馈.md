# 当前会话反馈

## 本次任务

按照预先约定的测试思路，扩大L1 C/A、B1I和G1同步前强入口鉴频器的同输入A/B验证。只有通过新噪声种子、符号边界相位和动态矩阵的候选才写入算法方案。

## 冻结候选

三种信号都从相邻1 ms Prompt复积开始：

$$
Z_n=P_{n-1}^{*}P_n=d_n+jc_n,
\qquad
u_n=\frac{Z_n}{|Z_n|}.
$$

式中，$d_n$和$c_n$分别为相邻Prompt的点积和叉积；$u_n$为只保留方向的单位复积。

### L1 C/A候选

连续单位复积直接合并：

$$
U=\sum_{n=1}^{N}u_n,
\qquad
\widehat{\Delta f}=\frac{\operatorname{atan2}(\Im U,\Re U)}{2\pi T}.
$$

L1 C/A的bit边界相对稀疏，少数反向单位相量只降低合向量幅度，不改变多数相邻对的方向。$T=1$ ms。

### B1I候选

B1I的NH符号可能每毫秒翻转，因此先做二倍角：

$$
v_n=u_n^2,
\qquad
\widehat{\Delta f}=\frac{\bar\theta_2}{4\pi T}.
$$

NH码和导航bit的正负号在平方后代数消失；$\bar\theta_2$为二倍角域完成圆周重映射后的平均相位差。物理无模糊范围为$(-250,+250)$ Hz，覆盖正式$\pm125$ Hz移交点。

### G1候选

原候选对每个完整四象限角度取窗口主簇，再把相差半周的角度映射回主簇。该候选在新28 dB-Hz holdout中失败，因此不写入算法方案。

## 测试方法

冻结二进制：

- StarGen SHA-256：`c7b91c678959b705a3b4adf82b6ab49d42df4715d539a53f07d13a981fe06aa7`；
- 当前ASIN基线StarTrack SHA-256：`b22edc5fbd4e24625490ee18da6ae7c0bbaa9da2737ba4ae712ea2aa4906e6ef`；
- 信号专用atan2候选SHA-256：`5cc96f92aacc0bf2c22d588e1d08c88dc788b5c3267963e6a7fb31468f5d9905`。

真实IQ矩阵使用3个未参与上一轮选择的新种子`20260902/20260903/20260904`。每个案例15 s，基线与候选使用完全相同的场景：

- L1 C/A：bit起点偏移`0/7/19 ms`，频差$\pm62.5$ Hz，40/32 dB-Hz；
- B1I：NH相位偏移`0/5/19 ms`，频差$\pm62.5/\pm125$ Hz，40/32 dB-Hz；
- G1：meander相位偏移`0/5/9 ms`，频差$\pm62.5$ Hz，40/32/28 dB-Hz；
- 循环电文分别为`01`、`0011`、`001011`；
- 动态项使用$\pm5$ Hz/s，L1 C/A和B1I为32 dB-Hz，G1为28 dB-Hz。

验收要求为：强入口12 s内同步；G1 28 dB-Hz压力项15 s内同步；最后3 s同步率不低于0.95；10～15 s频率绝对误差P95不超过10 Hz；码误差P95不超过0.2 chip；PDI和命令错误为0。

同步后的PLL历元没有短FLL观察，因此不把每20 ms日志行的`freq_used`零值当作失败。若CSV时刻存在有效频率观察，则仍检查它是否被控制使用。

## 测试结果

一共执行78个物理场景、156条真实IQ管道。ASIN基线78/78通过；atan2组合候选75/78通过。所有运行均无PDI错误和命令错误。

### L1 C/A：通过并写入方案

| 场景 | ASIN基线 | 单位相量atan2 |
|---|---:|---:|
| 40 dB-Hz静态 | 6/6 | 6/6 |
| 32 dB-Hz静态 | 6/6 | 6/6 |
| 32 dB-Hz、$\pm5$ Hz/s | 8/8 | 8/8 |
| 合计 | 20/20 | 20/20 |

32 dB-Hz静态频率P95最坏值由1.998 Hz变为1.738 Hz；动态由2.008 Hz变为1.954 Hz。候选首次同步略慢：32 dB-Hz静态中位数3.38 s，基线3.21 s；动态中位数3.43 s，基线3.20 s。全部仍远低于12 s门限。

### B1I：通过并写入方案

| 场景 | ASIN基线 | 二倍角atan2 |
|---|---:|---:|
| 40 dB-Hz静态 | 12/12 | 12/12 |
| 32 dB-Hz静态 | 12/12 | 12/12 |
| 32 dB-Hz、$\pm5$ Hz/s | 8/8 | 8/8 |
| 合计 | 32/32 | 32/32 |

B1I候选完整覆盖$\pm125$ Hz以及三个NH起点。动态首次同步中位数由7.32 s缩短到4.40 s，最慢由7.44 s缩短到5.16 s；动态频率P95最坏值由1.822 Hz改善到1.680 Hz。

32 dB-Hz静态中位同步时间由5.17 s缩短到4.23 s，但单个最慢案例由7.50 s变为7.82 s；频率P95最坏值由1.644 Hz变为1.869 Hz。两者都满足正式门，说明收益主要在同步时延分布，而不是每个统计量都单调改善。

### G1：候选否决

G1主簇atan2在32/40 dB-Hz为12/12通过，但28 dB-Hz静态和动态只有11/14通过，相同输入的ASIN基线为14/14。三个失败分别表现为未同步或同步后频率P95达到22～46 Hz。

进一步把G1改成与B1I相同的二倍角方案，28 dB-Hz前四个候选案例全部发散，频率误差达到数百至数万Hz，因此提前停止。二倍角在G1目标灵敏度下的阈值损失不可接受。

G1当前继续保留ASIN基线。不能仅凭32/40 dB-Hz通过就把低信噪比失败的atan2候选写成正式方案。

## 最终算法决策

1. L1 C/A强入口采用“单位相邻复积整窗合并，最后一次四象限atan2”。
2. B1I强入口采用“单位相邻复积平方，二倍角圆周重映射，再按$4\pi T$换算频率”。
3. G1继续使用当前归一化叉积ASIN窗口平均，等待新的低SNR无阈值损失方案。
4. normal入口的5点功率DFT以及同步后各状态的DFT/FMS路径均不因本轮结果改变。
5. 本轮只更新算法方案；生产StarTrack源码没有修改。正式落地仍需独立代码审查、完整CTest和受影响信号回归。

## 证据位置

- holdout执行脚本：`/tmp/run_atan2_holdout.py`；
- 完整机器摘要：`/tmp/startrack-atan2-holdout/summary_full.json`；
- 摘要SHA-256：`e539a679fed01a87968be1b3f3abf3bf8b4c7cfb9b0932dce28eaf0933a7d190`；
- 候选源码快照：`/tmp/startrack-atan-signal-src`；
- 候选Release二进制：`/tmp/startrack-atan-signal-build/startrack`；
- 生产仓库未修改。
