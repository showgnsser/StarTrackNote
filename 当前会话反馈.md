# 当前会话反馈

## 先纠正上一条结论

你指出得对：我上一条把临时压力测试方法写成了“当前方案”，这是错误的。

**正式工作区没有被改成“每1秒校频一次”的周期载波环。** 每1秒校频只存在于`/tmp/startrack-joint-nosync-src`测试副本中，用于验证“故意不允许位同步成功时，频率和码环能否继续闭环”。它没有合入正式工作区，也没有提交。

但是，正式工作区当前确实存在另一项尚未提交的改动：`LongFast`位同步前原有的160 ms频率观察被移除，改成等待位同步器首次约1秒的联合搜索结果后做一次性校频。这项改动发生在2026-09-02的未提交工作树中。我没有在修改前把算法时序和影响向你说明清楚，这是我的错误。

## 当前需要区分的三个版本

| 版本 | 位同步前载波频率处理 | 状态 |
|---|---|---|
| 当前提交`b461327` | `LongFast`使用独立频率观察器，观察和控制节拍按160 ms配置 | 已提交基线 |
| 正式工作区未提交改动 | `LongFast`位同步前不产生频率观察；首次同步搜索约1秒成熟后做一次性校频，随后才进入正常160 ms观察 | 当前WIP，不应视为已批准方案 |
| `/tmp`压力测试副本 | 故意禁止最终位同步，每1秒重新搜索并校频一次 | 仅测试注入，不是生产方案 |

## 正式工作区当前到底怎样运行

### 1. `LongFast`的策略参数仍然是160 ms

`TrackPolicy`中`LongFast`的载波观察周期、载波环更新周期、码观察周期和码环更新周期仍然都是160 ms。因此“160 ms”没有从状态配置中消失。

但必须区分两件事：

- **环路更新周期**表示有新频率观察时，载波环按什么节拍校正控制量；
- **观察器是否真的输出测量**决定这一时刻是否有东西可以送入环路。

仅把策略写成160 ms，并不能自动产生160 ms频率测量。

### 2. 当前WIP在首次位同步候选之前没有160 ms载波观察

当前`TrackEngine::formFreqObs()`对数据型信号执行：

1. 强入口`ShortFast/ShortNormal`在未同步时使用相邻1 ms Prompt FLL，每20 ms形成一次牵引观察；
2. 普通入口`LongFast`在未得到首次同步候选时直接返回空`FreqObs`；
3. 因为空观察不会进入`CarrierLoop::correct()`，载波NCO只保持已有控制量并按已有趋势预测，没有160 ms测量校正；
4. 码环仍按160 ms形成码误差，并叠加载波辅助更新码NCO。

所以当前WIP不能表述成“位同步前仍有160 ms载波闭环”。准确说法是：**码环有160 ms更新，载波环虽然保留160 ms配置，但在首次同步候选前没有对应频率测量。**

### 3. 首次约1秒结果是什么

数据位同步器的默认时标是：

- 首次搜索数据长度1000 ms；
- 独立确认数据长度至少2000 ms；
- 搜索或确认失败时最多扩展到4000 ms；
- 当前三个数据型信号使用5个频点，间隔31.25 Hz，覆盖正负62.5 Hz。

搜索还要容纳20种可能的bit/NH/meander边界，因此首次计算实际需要`1000+19=1019`条连续1 ms结果。首次搜索通过后，`TrackEngine::applySyncFreq()`把搜索得到的残余频率一次性吸收到载波NCO控制量中。

这不是“每1秒运行一次的FLL”，而是**搜索阶段完成时的一次性NCO重置**。随后当前`syncAligned()`把“首次候选已通过、正在Confirm”视为可对齐状态，正常160 ms频率观察器才开始工作；独立的2秒边界确认仍继续进行。

当前WIP的真实时序是：

```text
进入LongFast
    │
    ├─ 码环：160 ms DLL持续更新
    │
    ├─ 载波：首次候选前没有频率测量校正
    │
    └─ 位同步器：约1.019 s形成首次“频率+边界”候选
            │
            ├─ 一次性修正载波NCO
            ├─ 从下一批数据开始运行正常160 ms载波观察
            └─ 继续使用独立数据确认bit/NH/meander边界
```

这正是你担心的问题：如果动态多普勒在最初1秒内变化明显，载波NCO可能在获得首次候选之前发生漂移。当前WIP并没有用160 ms反馈覆盖这段时间。

## 为什么上一条会出现“每1秒校频一次”

为了回答“即使位同步一直失败，载波环和码环能否独立锁住”这个问题，我在`/tmp`测试副本中做了两项故障注入：

1. 强制`syncAligned()`只在最终`locked=true`时成立，禁止首次候选参与符号擦除；
2. 每次1秒频率搜索完成后重新开始下一批独立1秒搜索，永远不进入最终边界确认。

这使测试副本形成了“每1秒校频一次、始终不同步”的人工路径。48条真实IQ压力用例通过，只能证明这种测试结构能在已测场景中维持频率和码误差，不能证明它就是正式方案，更不能擅自替代160 ms设计。

## 什么时候改的

- 提交`b461327`中仍保留`LongFast`位同步前的独立频率观察；
- 2026-09-02 07:44左右的正式工作区未提交修改，把该分支改成空`FreqObs`，目的是删除重复的位同步前长窗DFT并让位同步联合搜索承担首次粗频校正；
- 随后的“每1秒重复校频”只在`/tmp`测试副本中增加，正式工作区没有这段逻辑；
- 当前没有StarTrack、StarGen或测试脚本在后台运行。

## 现在能得出的结论

### 已验证事实

- `LongFast`策略仍配置为160 ms；
- 当前正式WIP在首次同步候选前不产生160 ms载波频率观察；
- 首次约1秒联合搜索只在通过时执行一次NCO校频；
- `/tmp`的每1秒重复校频是测试注入，不是生产代码；
- 临时160 ms相邻Prompt反正切方案在L1 C/A牵引边界失败；
- 临时独立160 ms五点DFT在B1I真实IQ初测中没有形成统一可行方案；
- 因此不能在没有进一步设计和测试的情况下，直接声称“恢复任意160 ms鉴频器”就能解决问题。

### 当前假设

最符合你此前要求的目标结构应当是：位同步前保持真实的160 ms载波观察与反馈，位同步只负责边界搜索和确认，二者不互相阻塞。这样环路节拍统一，也不会让动态信号在首个1秒搜索窗内开环。

### 待确认问题

必须重新设计一个在L1 C/A、B1I和G1牵引灵敏度下都可靠的160 ms符号不敏感载波观察器。现有两个简单候选已经暴露出L1 C/A或B1I失败，不能未经确认就写入正式方案。

在你确认之前，我不会把“每1秒校频”或其他新频率路径固化为生产方案，也不会继续修改正式算法代码。

## 证据位置

- 正式工作区：`/home/showskills/SN770/Project/StarTrack`
- 当前关键函数：`src/tracking/track_engine.cpp`中的`formFreqObs()`、`holdPreSyncFreq()`、`applySyncFreq()`和`updateLoops()`
- 位同步时标：`src/sync/data_bit_sync.cpp`中的`BitSyncCfg`、`DataBitSync::add()`、`search()`和`confirm()`
- 1秒压力测试副本：`/tmp/startrack-joint-nosync-src`
- 48例压力汇总：`/tmp/presync-freq-decoupled-matrix/summary.json`

这些`/tmp`结果只属于开发诊断，不是正式资格证据。
