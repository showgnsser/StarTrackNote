# 当前会话反馈

这里不存在“我们的方案使用160 ms，而芯片统一使用20 ms”的差异。芯片在同步前本来就有强、普通两套牵引入口。

| 捕获入口 | 芯片初始状态 | 同步前DLL更新周期 |
|---|---|---:|
| 强捕获 | ShortFast | 20 ms |
| 普通或弱捕获 | LongFast | 160 ms |

芯片的入口选择过程如下：

1. 强扫描结果或捕获移交强度达到强信号门限时，初始状态被强制设为ShortFast；
2. 普通、弱捕获结果进入LongFast；
3. ShortFast使用短环路总周期20 ms；
4. LongFast使用长环路总周期160 ms；
5. 位同步是否完成不直接决定DLL周期，捕获入口和当前环路状态才决定周期。

所以“同步前”不是一个固定周期。同步前既可能运行20 ms ShortFast，也可能运行160 ms LongFast。

这样设计的原因是：

- 强信号的1 ms码误差比较可靠，连续20次平均后即可快速更新DLL；
- 普通或弱信号下，20 ms码误差噪声较大，LongFast把码证据积累到160 ms后再更新，以降低DLL抖动；
- 两条入口都保留载波辅助码率，因此160 ms期间码NCO并不是完全停止，主要码率漂移仍由载波多普勒辅助承担。

当前软件方案第四章与芯片一致：

- 强入口：每1 ms形成码误差，连续20次平均，每20 ms更新DLL；
- 普通中弱入口：积累160 ms码证据，每160 ms更新DLL。

因此不应把普通入口的160 ms改成统一20 ms。若未来希望所有同步前入口都用20 ms，必须先在L1 C/A 22 dB-Hz、B1I 24 dB-Hz和G1 28 dB-Hz牵引边界做真实IQ A/B，证明20 ms DLL不会明显增加码抖动或丢失Prompt；芯片源码本身不能为这种修改提供依据。
