# 状态频率门限A/B测试结论

## 1. 结论

这轮测试没有发现状态专属频率门限带来可验证的闭环收益。相反，当前证据显示：

- ShortFast的默认5 Hz门会延迟强信号牵引；
- LongSlow的5 Hz门会把能够被环路利用的频率观察拒绝掉，直接造成`freq_used_ratio`不达标；
- Sensitivity的10 Hz门以及“禁止重定心”在正式11 dB-Hz代表场景中根本没有触发，在8 dB-Hz压力场景中反而拒绝了一条有用的纠偏观察；
- 全部旁路FreqGuard后，已跑的状态确定性测试和三个真实IQ代表层级都没有出现失步、发散或非法状态跳转。

所以，当前最符合证据的判断是：观察器已经完成物理有效性判决后，应让有效频率观察直接进入FLL；鉴频器物理范围、FLL自身增益与趋势创新限制负责约束单次影响，FSM负责状态职责。ShortFast、LongSlow和Sensitivity不应再叠加5 Hz或10 Hz的静默拒绝门。

这还不是生产代码修改结论。本轮只建立了临时A/B二进制，没有修改当前工程源码。正式删除前仍应补充动态、TCXO和多种子真实IQ回归。

## 2. A/B版本

基线使用当前工作树构建的二进制：

```text
/tmp/startrack-data-pull-build/startrack
SHA-256: 5174ee7f2b05b6b053c882015d37bc6ae1ac9b8faac9ed5e36fa0959c5c72793
```

临时版本只改变FreqGuard，不改变观察器、FLL、FSM、同步、码环或测试输入：

| 临时版本 | 改动 | 二进制SHA-256 |
|---|---|---|
| no-short | 默认直接门由5 Hz放宽到250 Hz | `7388e7ad90295ff2a69be843f2985f2568feef739ee7027ff02fa0dcf3137040` |
| no-long | LongSlow直接门由5 Hz放宽到25 Hz | `43534df2781a78ef9ed29d841519dcc7b2fff95b1774b6662fb6fe4b6ebc52ba` |
| no-sens | Sensitivity直接门放宽到46.72 Hz并允许重定心 | `80f997c2909a340eee70ffeb9b3bacaca717a1badb8068023affda5532278a0a` |
| no-all | `FreqGuard::check()`直接返回观察器结果 | `e0093bc53d9ca49aee957029913895529f82c5ede728f2ceb56d8013d156c6d2` |

## 3. 确定性状态测试

五个版本都执行了以下状态测试：

```text
test_state_short_fast
test_state_long_slow
test_state_sensitivity
test_state_recovery
test_state_dynamic
test_state_weak_fade
test_source_entry_path
test_state_tcxo
```

结果均为7项通过，`test_state_tcxo`因缺少外部TCXO文件按设计跳过。`test_state_weak_fade`包含约21秒的弱信号渐降闭环，五个版本全部通过。

Sensitivity确定性测试包含3个静态弱信号种子和6个正负动态斜率案例。去掉Sensitivity门后，8个案例与基线逐指标相同；只有静态seed 11发生一次使用差异：

| 版本 | 使用率 | RMS | P95 |
|---|---:|---:|---:|
| 基线 | 0.998933 | 1.433 Hz | 2.713 Hz |
| no-sens | 1.000000 | 1.571 Hz | 2.799 Hz |

这一次差异很小，不能证明门限必要；其余案例完全一致。

`test_track_engine`基线通过，no-short和no-all在`checkShortFastFreqGuard()`失败。原因是该测试明确断言“人工注入的一次25 Hz跳变必须被拒绝”。这证明旧门能够执行旧合同，但没有证明该人工跳变来自真实观察器，也没有比较接受后环路是否会恢复，因此不能作为门限具有实际价值的证据。

## 4. ShortFast真实IQ

场景为40 dB-Hz、强入口、+62.5 Hz捕获残差，信号分别为L1CA、B1I和G1。

| 信号 | 基线首次同步 | no-short首次同步 | 基线RMS/P95 | no-short RMS/P95 |
|---|---:|---:|---:|---:|
| L1CA | 3.599 s | 3.159 s | 0.887 / 2.123 Hz | 0.800 / 1.673 Hz |
| B1I | 3.519 s | 3.199 s | 1.315 / 2.334 Hz | 1.303 / 2.477 Hz |
| G1 | 3.599 s | 3.199 s | 0.501 / 0.883 Hz | 0.495 / 0.913 Hz |

三例的同步、频率有效率、使用率均为1，无PDI或命令错误。删除门限后首次同步提前0.32至0.44秒，精度没有实质退化。ShortFast的5 Hz门在这些真实IQ场景中没有体现保护价值。

证据目录：

```text
/tmp/startrack-data-pull-iq
/tmp/startrack-guard-iq-no-short
```

## 5. LongSlow真实IQ

场景为普通入口，L1CA/G1使用22 dB-Hz，B1I使用24 dB-Hz，捕获残差约为正负62.5 Hz。计分段均已同步并处于LongSlow。

| 场景 | 基线使用率 | no-long使用率 | 基线RMS/P95 | no-long RMS/P95 |
|---|---:|---:|---:|---:|
| L1CA −62.5 Hz | 0.556 | 1.000 | 2.444 / 2.778 Hz | 1.433 / 2.321 Hz |
| L1CA +62.5 Hz | 0.667 | 1.000 | 1.944 / 2.455 Hz | 0.945 / 2.012 Hz |
| B1I −62.5 Hz | 0.556 | 1.000 | 2.055 / 3.268 Hz | 2.032 / 3.591 Hz |
| G1 −62 Hz | 0.778 | 1.000 | 0.831 / 1.752 Hz | 1.258 / 2.457 Hz |

基线4例的物理精度、同步和时序都通过，但全部因为使用率低于0.95而被runner判失败。no-long的4例全部通过，码误差P95均小于0.034 chip。

去门后L1CA两例精度明显改善；B1I和G1有小幅统计波动，但仍远低于5/10 Hz精度门。没有出现频率发散、失步或重捕。

更关键的是，门限并不能稳定识别粗差。计分段内按`NCO + 频率观察`与真值比较：

| 场景 | 已采用观察平均绝对误差 | 被拒绝观察平均绝对误差 |
|---|---:|---:|
| L1CA −62.5 Hz | 2.907 Hz | 6.192 Hz |
| L1CA +62.5 Hz | 2.535 Hz | 4.729 Hz |
| B1I −62.5 Hz | 3.582 Hz | 3.494 Hz |
| G1 −62 Hz | 2.695 Hz | 6.349 Hz |

L1CA和G1中，被拒绝组平均较差；但B1I中被拒绝组反而略好。5 Hz门判断的是“本次残差相对当前NCO是否超过5 Hz”，不是“绝对频率估计离真值多远”，因此它没有可靠的粗差分类能力。

证据目录：

```text
/tmp/startrack-data-pull-iq
/tmp/startrack-guard-iq-no-long
```

## 6. Sensitivity真实IQ

### 6.1 正式11 dB-Hz代表场景

场景先以22 dB-Hz跟踪30秒，再在20秒内线性降至11 dB-Hz，最后计分70至100秒。基线与no-sens结果逐指标相同：

```text
Sensitivity驻留: 29.98 s
sync/freq-valid/freq-used: 1.0 / 1.0 / 1.0
Doppler RMS/P95: 2.318 / 3.884 Hz
code P95: 0.1415 chip
```

这里10 Hz门没有触发，所以它没有提供可观察的保护。

### 6.2 8 dB-Hz压力场景

使用相同的预跟踪和降功率过程，把目标改为8 dB-Hz：

| 版本 | 使用率 | RMS | P95 | 码P95 |
|---|---:|---:|---:|---:|
| 基线 | 0.989 | 7.109 Hz | 9.889 Hz | 0.178 chip |
| no-sens | 1.000 | 4.335 Hz | 8.440 Hz | 0.147 chip |
| no-all | 1.000 | 2.863 Hz | 5.631 Hz | 0.028 chip |

基线在epoch 74080拒绝的Sensitivity观察，观察器残差为11.571 Hz，因超过10 Hz而被拒绝；但加上当时NCO后，该次绝对频率估计距离真值只有3.371 Hz。也就是说，这不是粗差，而是一条能把已经偏离的NCO拉回来的有效纠偏量。

`allow_recenter=false`因此存在结构性问题：NCO已经偏离时，正确纠偏量天然可能超过10 Hz；门限会因为“纠偏太大”而拒绝它，使弱状态更难自行恢复。

### 6.3 5 dB-Hz额外压力

同一种子降至5 dB-Hz后，基线使用率仍为1.0，门限没有触发，RMS/P95为4.371/7.060 Hz。这进一步说明10 Hz门是否触发并不稳定对应信号强弱，也不能作为可靠的弱信号粗差保护。

证据目录：

```text
/tmp/startrack-guard-iq-sens-baseline
/tmp/startrack-guard-iq-sens-no-guard
/tmp/startrack-guard-iq-sens8-baseline
/tmp/startrack-guard-iq-sens8-no-guard
/tmp/startrack-guard-iq-sens8-no-all
/tmp/startrack-guard-iq-sens5-baseline
```

## 7. 当前证据能证明什么

### 已验证事实

1. 当前短确定性状态矩阵没有依赖这些门限才能稳定。
2. ShortFast去门后3个真实IQ强信号案例同步更快，没有精度退化。
3. LongSlow去门后4个真实IQ案例全部恢复100%使用率并通过，未出现发散。
4. LongSlow的5 Hz门不能稳定区分真粗差和可用观察。
5. Sensitivity正式11 dB-Hz代表场景中10 Hz门没有发挥作用。
6. Sensitivity 8 dB-Hz压力场景中，门限拒绝了有用纠偏，去门后精度改善。
7. 当前唯一支持保留门限的测试，是人工注入单次25 Hz跳变并断言必须拒绝的旧行为测试；它不等于真实IQ必要性证据。

### 当前假设

观察器的物理有效性、鉴频器无模糊范围、FLL比例增益和趋势创新限制已经足以吸收偶发有界误差。状态机应根据持续的均值、RMS和动态证据换档，而不是在环路入口按5 Hz或10 Hz静默丢弃观察。

### 尚待确认

1. 正负1 Hz/s、TCXO和高动态真实IQ多种子下，全旁路是否仍不退化；
2. 极弱长时保持中，连续同向但仍被观察器判有效的错误频率簇是否会造成NCO长期游走；
3. 若确有这类失败，应优先收紧观察器物理有效性或限制单次环路影响，而不是恢复状态专属残差门。

## 8. 下一步建议

下一步应保持参数不变，只比较基线与no-all：

- L1CA、B1I、G1各3个种子的静态弱信号；
- 正负1 Hz/s；
- TCXO；
- 一个真实短时频率阶跃；
- 强入口高动态。

验收顺序为：失步/重捕、NCO最大偏移、Doppler RMS/P95、码P95、恢复时间，最后才是使用率。

如果no-all在上述矩阵中不比基线差，建议删除FreqGuard在正常闭环中的控制权限，使`freq_used`等同于“观察器物理有效且被FLL消费”。如果只有人工或真实的连续粗差簇失败，应修正观察器或FLL影响限制，不再增加5 Hz、3 Hz、三次确认和禁止重定心等第二套判断状态机。
