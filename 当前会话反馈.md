# 当前会话反馈

不是。芯片源码中码环的最短更新周期是20 ms，不是160 ms。

芯片的DLL系数表按状态给出了以下周期：

| 状态 | DLL周期 |
|---|---:|
| ShortFast | 20 ms |
| ShortNormal | 20 ms |
| ShortSlow | 20 ms |
| LongFast | 160 ms |
| LongNormal | 160 ms |
| LongSlow | 320 ms |
| Sensitivity | 320 ms |
| BitAiding | 600 ms |
| OutdoorBitAiding | 300 ms |
| MidDynamic | 320 ms |
| HighDynamic | 320 ms |

源码中的实际处理路径也与这张表一致。强信号进入ShortFast后，系统把短环路总周期配置为20 ms。位同步前可以每1 ms形成一次码鉴别结果，但并不每1 ms改写码NCO；程序将这些1 ms码误差积累或平均到20 ms，然后调用二阶DLL滤波器更新一次码率。LongFast和LongNormal才使用160 ms码观察与DLL更新。

因此必须区分三个时间概念：

1. 硬件相关结果仍然每1 ms产生一次；
2. 强入口可以每1 ms计算一次Early/Late码误差；
3. DLL滤波器和码NCO控制量在收满20 ms后才更新一次。

这与当前软件方案第四章一致：强入口采用“1 ms码鉴别，连续20次平均，20 ms更新DLL”；普通中弱入口采用160 ms码观察和160 ms更新。第四章不需要因为这个问题修改。
