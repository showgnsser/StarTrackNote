# 当前会话反馈

## 对上一轮结论的修正

用户指出“atan鉴频器理论性能应当强于asin”，这个质疑成立。上一轮测试把三项变化混在了一起：

1. 把逐对鉴频后等权平均改成了原始dot/cross整窗求和，改变了复积幅度权重；
2. 把B1I的平方Prompt一并取消，改变了NH符号消除方式；
3. 将强入口方案错误地推广到了中弱捕获门限。

因此上一轮只能证明两个具体候选实现不合格，不能证明atan鉴频器本身弱于asin。生产代码仍未修改。

## 两种鉴频器的真实关系

相邻Prompt复积写为：

$$
Z=P_{k-1}^{*}P_k=dot+j\,cross.
$$

在符号已知、$dot>0$且相邻相位变化满足$|\Delta\phi|<\pi/2$时：

$$
\operatorname{atan2}(cross,dot)
=\operatorname{asin}\left(\frac{cross}{|P_{k-1}||P_k|}\right)
=\Delta\phi.
$$

所以无噪声主线性区内，两者给出的频率完全相同。atan的优势主要是：使用dot和cross恢复完整角度，在线性区和象限正确时通常具有更好的相位估计性能与更宽的明确工作范围。

但位同步前符号未知。两象限atan通常写成`atan(cross/dot)`，等价于当dot为负时同时翻转dot和cross。此时负dot既可能由bit、NH或meander翻转造成，也可能只是低SNR噪声造成。逐对按dot正负折叠是一项硬判决；进入鉴频门限区后可能出现分支跳变和错误反馈。GNSS FLL文献也明确指出atan/atan2响应、斜率和线性区随相干SNR变化，鉴频器不能脱离数据调制、积分时长和环路参数单独比较。

## 修正后的严格A/B

重新增加第四个候选：

- 保留20对逐对鉴频后等权平均；
- 保留B1I平方Prompt和二倍角尺度；
- 只把每对`asin(sin Δφ)`换成两象限`atan(cross/dot)`；
- 观察长度、更新周期、载波环、码环、位同步和状态机均不变。

候选二进制SHA-256：`766d7aeca3d13d93d5e0aaf64ec1a9b6df5298959a60ad535428331fce204e22`。

### 强信号结论

L1CA和G1上一轮的逐对atan候选本来就是严格的逐对A/B。40 dB-Hz下全部案例均同步：

- L1CA：asin与atan首个20 ms窗口结果基本相同，闭环首次同步差异不超过20 ms；
- G1：首个20 ms窗口的频率RMSE由asin约21.74 Hz降到atan约1.74 Hz，说明atan正确消除了高SNR meander翻转；
- B1I：保留平方后，atan在8例40 dB-Hz中通过7例，失败例是`-125 Hz`边界。平方使相位增量加倍，`±125 Hz`恰好落在两象限atan的模糊边界，不能直接采用。

所以“强信号下atan更强”这一判断对L1CA/G1基本成立，尤其G1收益明显；上一轮把这个结论掩盖了。

### 中低信号结论

32 dB-Hz下，逐对atan仍出现门限效应：

- L1CA：当前asin 4/4同步，中位3240 ms；atan 4/4同步，中位7460 ms；
- G1：两者均4/4同步，中位分别3320和3400 ms，但atan最慢案例达到7180 ms；
- B1I：当前平方asin 8/8同步；平方atan只有3/8同步。

在各信号牵引门限下，逐对atan也没有闭环优势。但这些低C/N0场景本来应该走中弱捕获入口，不能用来否定“强捕获入口使用atan”。

## 为什么asin在低SNR闭环中看起来更稳

当前L1CA/G1方案不是通用意义上的最优asin鉴频器，而是利用了一个具体先验：20个相邻间隔中，真正的数据bit或meander边界只占很少一部分。当前算法宁可让边界处一对产生反号，也不对其余每一对执行低SNR符号硬判决。

因此它的优势来自“稀疏边界假设＋有界逐对输出＋20次平均”，不是asin函数天然优于atan。强信号下dot符号可靠，atan优势恢复；低SNR下dot符号不可靠，硬折叠会进入门限区。

B1I又不同：NH逐毫秒变化，不能依赖“边界稀疏”。当前平方Prompt是在代数上消除正负号；若平方后使用两象限atan，物理频率范围会被二倍角压到`(-125,+125) Hz`，正好与正式边界冲突。若B1I强入口使用未平方的两象限atan，范围可恢复到`(-250,+250) Hz`，已有40 dB-Hz和32 dB-Hz结果均能同步，但这属于“鉴频器＋符号消除结构”共同变化，需要独立冻结。

## 当前正确结论

1. 不能再说“atan整体不如asin”；上一轮该表述撤回。
2. L1CA、G1的强捕获入口使用逐对两象限atan具有物理依据；G1高SNR收益已经在同一真实IQ首窗中出现。
3. 中弱捕获入口不能直接复用该方案，现有弱入口DFT/FMS和当前稳健牵引策略应保持不变。
4. B1I不能简单采用“平方Prompt＋两象限atan”，因为正式`±125 Hz`位于模糊边界；应单独比较未平方atan与当前平方asin。
5. 在完成强入口实际C/N0下的更多seed、符号边界和动态测试前，不修改生产代码。

## 证据位置

- 更新后的机器摘要：`/tmp/startrack-prompt-fll-atan-ab/summary.json`
- 摘要SHA-256：`3b12ce3dbe43a25c519c6c43a4e48237cd14d1baad411fa0f06af0c3adabb5fa`
- 当前基线二进制SHA-256：`b22edc5fbd4e24625490ee18da6ae7c0bbaa9da2737ba4ae712ea2aa4906e6ef`
- StarGen SHA-256：`c7b91c678959b705a3b4adf82b6ab49d42df4715d539a53f07d13a981fe06aa7`

## 参考

- Curran、Lachapelle和Murphy，《Improving the Design of Frequency Lock Loops for GNSS Receivers》，IEEE TAES 2012，系统比较四类GNSS FLL鉴频器并指出鉴频器选择可造成约10 dB性能差异。
- Muthuraman，《Tracking Techniques for GNSS Data/Pilot Signals》，说明数据符号未知时可使用dot符号对cross作判决反馈，同时强调这种结构的工作范围和噪声性能与完整atan2不同。
