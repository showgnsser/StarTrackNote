# 数据型信号atan与atan2组合鉴频方案

## 结论

可以，而且这比“全程asin”或“位同步前直接四象限atan2”都更合理：

- 位同步前使用二象限`atan`，消除未知导航bit、B1I NH码和G1 meander造成的$180^\circ$符号翻转；
- 位同步后擦除公开模板，只在同一个导航bit内部形成差分对，再使用四象限`atan2`；
- 两种模式共用点积、叉积累加器和同一个四象限反正切运算，只需要切换象限折叠开关。

## 位同步前的二象限atan

相邻Prompt形成点积$dot$和叉积$cross$。数学上可以计算`atan(cross/dot)`，因为未知符号翻转会让$dot$与$cross$同时反号，比值保持不变。

实际实现不做除法：若某一对的$dot<0$，同时取反$dot$和$cross$，把差分向量折叠到右半平面；随后累加20对折叠结果，最后调用一次`atan2(Σcross,Σdot)`。由于点积总和非负，这次`atan2`只输出二象限结果，与`atan`等价。

三种数据型信号可以共用这套流程，B1I同步前不再需要平方Prompt。唯一鉴频范围为$(-250,+250)$ Hz，覆盖当前捕获移交范围。

## 位同步后的四象限atan2

位同步后，B1I擦除NH20，G1擦除meander，L1 C/A无需擦除公开模板。若导航bit内容仍未知，则一个20 ms bit内部的20条PDI只能形成19个相邻差分对，bit边界处必须重建观察，不能让四象限`atan2`跨越未知bit翻转。

同bit内部不再折叠点积、叉积，直接累加后计算`atan2(Σcross,Σdot)`，唯一范围为$(-500,+500)$ Hz。若导航bit内容也已知并擦除，才可以继续使用跨bit边界的差分对。

## 与芯片源码的关系

重建芯片源码的二象限`arctan_cordic`在I分量为负时会同时翻转I/Q，物理上就是上述右半平面折叠；稳定路径另有四象限`arctan2_cordic`。因此“同步前atan、同步后atan2”比当前逐对`asin`更接近源码中两类反正切器的职责划分。

## 本次文档修改

- 软件方案更新到V0.14；
- 三种数据型信号的同步前强入口统一为二象限atan；
- 同步后明确切换四象限atan2，并禁止跨未知导航bit边界；
- 删除B1I强入口平方Prompt要求；普通入口5点功率DFT仍保留平方Prompt；
- 同步更新数据型信号流程图并完成实际渲染检查；
- 当前生产代码仍为逐对`asin`，本次没有修改生产代码，也没有改动用户的`.obsidian`配置。

## 下一步

对当前逐对`asin`与目标组合方案做同输入A/B，至少覆盖三种信号的正负移交频差、20种bit边界、B1I NH翻转、G1 meander翻转、低载噪比和动态场景。通过后再修改生产观察器。
