# 当前会话反馈

## 结论修正

28 dB-Hz确实属于G1的中弱捕获移交范围，不应强制进入只服务强捕获结果的1 ms短周期鉴频支路。前一轮把所有G1场景都写成`entry=strong`，等于拿越职责压力场景否决强入口算法，验收口径不正确。

正确入口分工为：

| 捕获等级 | 典型C/N0 | 跟踪入口 | 载波牵引方法 |
|---|---:|---|---|
| 强捕获 | 32、40 dB-Hz | strong | 相邻1 ms单位复积整窗求和atan2 |
| 中弱捕获 | 28 dB-Hz及牵引边界 | normal | 5点功率DFT，短相干块之间非相干积累 |

因此，G1的atan2强入口只需在强捕获实际覆盖范围内验证。28 dB-Hz下ASIN比atan2更稳仍是有效的压力诊断，但该场景不会运行强入口鉴频器，不能决定生产入口选择。

## G1强入口候选

相邻1 ms Prompt形成复积：

$$
Z_n=P_{n-1}^{*}P_n.
$$

归一化为单位相量：

$$
u_n=\frac{Z_n}{|Z_n|}.
$$

20 ms窗口内直接求和：

$$
U=\sum_{n=1}^{N}u_n.
$$

最后只做一次四象限角度计算：

$$
\widehat{\Delta f}
=\frac{\operatorname{atan2}(\Im U,\Re U)}{2\pi T},
\qquad T=1\ \mathrm{ms}.
$$

该方案不逐对判断`dot`符号，不使用窗口中值或主簇重映射，也不对G1相量平方。少量meander边界相量只会抵消部分合向量。

## 补充测试

静态强入口沿用上一轮3个新种子、meander起点`0/5/9 ms`、频差$\pm62.5$ Hz：

- 40 dB-Hz：6/6通过；
- 32 dB-Hz：6/6通过。

本轮新增32 dB-Hz动态矩阵：

- 信号种子`20260902/20260903/20260904`；
- meander起点`0/5/9 ms`；
- 初始频差$\pm62.5$ Hz；
- 频率变化率$\pm5$ Hz/s；
- 共12个物理场景、24条ASIN/atan2同输入A/B真实IQ管道。

结果如下：

| G1强入口 | 静态 | 动态 | 合计 |
|---|---:|---:|---:|
| 当前ASIN基线 | 12/12 | 12/12 | 24/24 |
| 单位相量atan2 | 12/12 | 12/12 | 24/24 |

atan2候选动态最差频率绝对误差P95为1.65 Hz，最差码误差P95低于0.2 chip，最晚首次同步为4.74 s；所有案例最后3 s持续同步，且PDI、命令错误均为0。

## 28 dB-Hz结果应如何使用

单位相量atan2在28 dB-Hz越职责压力矩阵为23/26，而ASIN为26/26。该结果说明ASIN有界投影在更低单PDI信噪比下更稳，但不表示G1强入口必须保留ASIN，因为28 dB-Hz实际由normal入口处理。

28 dB-Hz normal入口不能调用上述1 ms atan2。它继续使用5点频率候选、短相干块和块间非相干积累获得处理增益。

## 最终数据型信号强入口方案

1. L1 C/A：单位相邻复积整窗求和，最后一次四象限atan2。
2. G1：与L1 C/A相同，使用单位相邻复积整窗求和atan2。
3. B1I：单位相邻复积平方，在二倍角域处理并按$4\pi T$恢复物理频差。
4. 三种信号的中弱捕获结果都进入normal入口5点功率DFT，不使用强入口鉴频器。
5. G1主簇重映射和二倍角候选继续废弃；它们不是单位相量直接求和atan2的必要组成。

本轮只修正算法方案和测试职责，生产StarTrack源码没有修改。

## 证据

- G1强入口补充脚本：`/tmp/run_g1_vector_strong_holdout.py`；
- 机器摘要：`/tmp/startrack-g1-vector-strong-holdout/summary_full.json`；
- 摘要SHA-256：`e34fdce8d42b22ce13ab47377abce0d4d8183d3cfa873772ada6a0502c575fc7`；
- atan2候选二进制SHA-256：`79d2bf5f742b2aefc67ec5082220bc08b868ee1561a24d8a7c879ee40cbeef5a`。
