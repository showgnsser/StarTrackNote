# 当前会话反馈

## 结论

从理想无噪声鉴频曲线看，`atan`确实比ASIN更完整；G1测试中ASIN更稳，并不是ASIN反函数具有更高的理论精度，而是它在位同步前主动放弃了低信噪比下不可靠的象限信息，并把每一对频率误差限制为有界值。

前一轮失败的主簇atan2并不能代表所有atan方案。补测删除了主簇选择，只将20个单位复积直接求和后做一次`atan2`。该方案明显减少了粗差，但G1 28 dB-Hz静态和动态仍只有23/26通过；相同输入的ASIN基线为26/26。因此当前保留ASIN仍有真实IQ证据，而不是因为形式偏好。

## 理想条件下为什么atan看起来更合理

设相邻1 ms Prompt复积为

$$
Z_n=P_{n-1}^{*}P_n=d_n+jc_n.
$$

若真实相位差为$\delta$，相邻两毫秒的meander和导航bit符号乘积为$a_n\in\{+1,-1\}$，无噪声时可写成

$$
Z_n=a_nA_ne^{j\delta}.
$$

式中，$A_n$为复积幅度。于是

$$
d_n=a_nA_n\cos\delta,
\qquad
c_n=a_nA_n\sin\delta.
$$

二象限atan使用

$$
\operatorname{atan}\!\left(\frac{c_n}{d_n}\right)
=\operatorname{atan}(\tan\delta).
$$

$a_n$会在分子、分母中同时消失，所以理想条件下meander翻转不影响结果。这个判断是正确的。

当前ASIN使用

$$
\operatorname{asin}\!\left(
\frac{c_n}{\sqrt{d_n^2+c_n^2}}
\right)
=\operatorname{asin}(a_n\sin\delta).
$$

在$|\delta|<\pi/2$时，未翻转相邻对输出$+\delta$，翻转对输出$-\delta$。单看一对结果，ASIN确实不如atan完整。

## 为什么真实G1低信噪比下ASIN反而更稳

### 1. atan必须信任噪声很大的点积

`atan(cross/dot)`把$d_n$放在分母。28 dB-Hz复合G1的单个1 ms Prompt仍然很弱，噪声可以让$d_n$接近0或改变符号。此时比值会出现重尾，象限折叠也可能选择错误半周。

ASIN的分母是

$$
\sqrt{d_n^2+c_n^2},
$$

它始终非负，不需要根据$d_n$的正负作象限判决。单对输出被限制在$[-\pi/2,+\pi/2]$，因此一个低功率噪声相邻对不能单独产生任意大的角度。

### 2. G1的反号相邻对很少

G1 meander每10 ms翻转一次。20 ms观察窗中，大多数相邻对仍位于同一个meander半周期内，只有少数相邻对跨越meander或导航bit边界。

若一个窗口有$N$对相邻Prompt，其中$m$对发生符号翻转，无噪声ASIN平均结果约为

$$
\overline{\delta}_{\mathrm{ASIN}}
=\frac{N-2m}{N}\delta.
$$

例如$N=20$、$m=1$或2时，等效比例为0.9或0.8。它主要表现为鉴频增益降低10%～20%，但结果仍保持正确方向。闭环可以通过后续更新继续收敛。

### 3. atan2圆周均值存在低SNR阈值

直接单位相量方案计算

$$
U=\sum_{n=1}^{N}\frac{Z_n}{|Z_n|},
\qquad
\widehat{\delta}=\operatorname{atan2}(\Im U,\Re U).
$$

无噪声且正常相邻对占多数时，$\widehat\delta$可以准确恢复$\delta$。但反号相邻对会把合向量长度从$N$降低到约$N-2m$。低SNR噪声又会把单位相量随机推向不同方向；当$|U|$接近0时，`atan2`角度会突然跳到完全不同的象限。这就是相位型估计器的阈值效应。

ASIN平均只累加每一对的有界正弦投影，不需要先形成一个可能接近原点的整窗合向量。因此它牺牲了一部分理想鉴频增益，换来了更平滑的低SNR反馈。

### 4. 主簇方法会把一次错误变成整窗错误

先前G1候选先取角度中值，再把其它角度映射到该主簇。低SNR下如果中值落入错误半周，随后20对证据会被一起映射错，产生相关粗差。ASIN没有这类数据驱动的分支选择。

## 补充测试

同一个3新种子、3个meander起点、40/32/28 dB-Hz、静态和$\pm5$ Hz/s holdout中：

| G1强入口方案 | 28 dB-Hz通过数 | 结果 |
|---|---:|---|
| 当前逐对ASIN平均 | 26/26 | 全部通过 |
| 主簇重映射atan2 | 23/26 | 出现未同步和22～46 Hz粗差 |
| 单位相量直接求和atan2 | 23/26 | 粗差减少，但仍有晚同步和10.72 Hz P95 |
| 二倍角atan2 | 前4例0/4 | 低SNR直接发散，提前停止 |

单位相量直接求和的机器摘要为`/tmp/startrack-g1-vector-holdout/summary_full.json`，SHA-256为`82e85038ce927e709cb4b7301c8be916e1c67083c964ea99fc039d48447198eb`。

## 应当怎样理解两种鉴频器

- 高SNR、符号已知或已经擦除时，四象限`atan2`通常更合理，范围更完整，也不存在ASIN的等效增益压缩。
- 符号未知但翻转稀疏、单个1 ms相关很弱时，ASIN的有界投影平均可以比需要象限判断的atan更稳。
- B1I的NH码翻转过于频繁，ASIN会严重抵消，因此必须使用已经通过测试的二倍角atan2专用方案。
- G1当前的ASIN优势是特定统计模型和工作点下的鲁棒性，不代表ASIN在一般载波鉴频问题中优于atan。

因此当前算法选择保持：L1 C/A采用整窗单位相量atan2，B1I采用二倍角atan2，G1保留逐对ASIN窗口平均。生产源码本轮没有修改。
