# 当前会话反馈

你没有理解错，是我把“已经运行的代码”和“讨论中的目标方案”混在一起说，导致结论前后变化。

现在只保留下面三个事实：

| 层次 | 当前状态 |
|---|---|
| 正式代码现在实际运行什么 | 强信号入口使用`asin`；B1I还会先平方Prompt |
| 我们讨论后希望改成什么 | 强信号入口统一使用`atan(Pcross/Pdot)` |
| `atan`是否已经写入并验证 | 没有。正式代码尚未修改，也没有完成对应真实IQ回归 |

因此，当前唯一准确的说法是：

> 现有实现是`asin`；`atan`只是已经讨论确定的待实现方案，还不是当前代码。

我前面有两处错误：第一次把当前代码中的`asin`直接写进目标方案，没有说明它与讨论结论冲突；第二次又把文档中的目标`atan`说成了当前已经运行的代码。两次都混淆了“设计”和“实现”。

现在的软件方案文档写的是目标`atan`，正式代码仍是`asin`，两者暂时不一致。下一步只有两个互斥选择：保留现有`asin`并把文档改回去，或者把正式代码改成`atan`并重新测试。在作出这个选择前，不应再声称两者已经一致。

本次不修改算法代码，也不再引入新的鉴频器结论。
