# 当前会话反馈

本轮使用统一11点LongFast配置，对B1I追加了27个短移交案例：

$$
\{-125,0,+125\}\ \mathrm{Hz}
\times
\{-0.5,0,+0.5\}\ \mathrm{chip}
\times
\{0,10,19\}\ \mathrm{ms}.
$$

全部案例固定为PRN6、24 dB-Hz、seed 20260831、静态、无TCXO，每例20秒。结果为14例通过、13例失败：

| 频差 | 码差 | bit起点0 | bit起点10 | bit起点19 |
|---:|---:|---:|---:|---:|
| −125 Hz | −0.5 chip | 通过 | 失败 | 失败 |
| −125 Hz | 0 chip | 通过 | 失败 | 失败 |
| −125 Hz | +0.5 chip | 通过 | 通过 | 通过 |
| 0 Hz | −0.5 chip | 通过 | 失败 | 失败 |
| 0 Hz | 0 chip | 通过 | 通过 | 失败 |
| 0 Hz | +0.5 chip | 失败 | 通过 | 通过 |
| +125 Hz | −0.5 chip | 通过 | 通过 | 失败 |
| +125 Hz | 0 chip | 通过 | 通过 | 失败 |
| +125 Hz | +0.5 chip | 失败 | 失败 | 失败 |

14个通过案例的最晚首次同步为13.958秒；最坏频率RMS/P95为3.130/5.968 Hz，最坏码误差P95为0.0315 chip，均满足短移交精度门。

13个失败案例都在20秒内没有完成位同步。失败案例虽然日志中的频率观察有效率和使用率仍为1，但频率误差RMS已经达到23～853 Hz，部分码误差超过1 chip。这说明观察器把错误粗频结果标成了有效结果并持续送入FLL；失败不是“没有频率观察”，而是“频率观察的物理值错误”。

11点网格解决的是$\pm125$ Hz范围内的频点间隔问题，不能单独解决当前B1I LongFast在24 dB-Hz下的粗频错误接受。bit起点变化同时改变NH起点和所消费的噪声样本，因此本轮单种子结果只能说明存在显著码频/起点耦合，不能把差异全部归因于NH边界。证据保存在`/tmp/b1i-longfast11-mini-matrix/cases`，使用的StarTrack二进制SHA-256为`1cbfc13ee653614d655deaf3e281d5a3b9c92d6f198c911835152549912e6ef8`。
