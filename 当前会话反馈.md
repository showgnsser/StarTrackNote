# 当前会话反馈

## 纠正

你说得对：强信号入口已经确定使用`atan`鉴频器，不是`asin`。上一版方案错误地沿用了临时实现中的旧鉴频公式，现已纠正。

## 当前正确分工

### 强信号入口

L1 C/A、B1I和G1统一直接使用相邻两条原始1 ms Prompt：

$$
\mathrm{Pdot}_n=I_{n-1}I_n+Q_{n-1}Q_n,
$$

$$
\mathrm{Pcross}_n=I_{n-1}Q_n-Q_{n-1}I_n,
$$

$$
\widehat{\Delta f}_n=
\frac{1}{2\pi T}
\operatorname{atan}
\left(
\frac{\mathrm{Pcross}_n}{\mathrm{Pdot}_n}
\right),
\qquad T=1\ \mathrm{ms}.
$$

数据bit、NH码或meander造成Prompt反号时，点积和叉积会同时反号，二者的比值不变。因此不需要平方Prompt。每个相邻1 ms结果先独立鉴频，再在20 ms内平均，三阶FLL每20 ms更新一次。1 ms基线下，二象限`atan`的无模糊范围是$\pm250$ Hz，覆盖当前强信号移交范围。

### 中弱信号入口

- L1 C/A、G1：160 ms功率DFT，11个频点覆盖$\pm62.5$ Hz。
- B1I：160 ms相邻1 ms `asin`鉴频平均，不在牵引阶段枚举20种NH起点。

### 位同步之后

位同步后已经掌握数据边界或NH/meander相位，可以按状态使用更长相干观察，并使用`atan2`或相应的DFT/FMS观察器。

## 文档与代码状态

软件方案中的强信号章节已经改回`atan(Pcross/Pdot)`，删除了强入口的Prompt平方、相位倍数和`asin`公式。本次只修订方案文档，没有修改正式跟踪代码。当前正式代码的强入口仍残留`asin`实现，后续实现对齐时必须单独修改并执行真实IQ回归。
