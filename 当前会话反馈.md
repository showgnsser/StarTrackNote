# L1 C/A牵引与数据位同步章节完成

## 当前判断

L1 C/A、B1I和G1当前的强入口、普通入口与位同步代表路径都已闭合，暂时没有发现新的结构性错误。最新真实IQ中，三种信号在代表性的强/普通入口和约$\pm62.5$ Hz残差下都完成了同步；删除状态专属频率拒绝门后，也没有出现失步、发散或码环失稳。

这个结论的边界是“当前方案没有已知结构性问题”，不是“三种信号的所有位同步场景已经完成资格”。20种bit起点、多种子、动态、TCXO和耐久矩阵仍需后续扩大回归。

## 本次文档修改

- 删除了原第四章“数据符号与导频二次码同步”，不再把八种信号混写在一个章节中；
- 新第四章只写“GPS L1 C/A牵引与数据位同步”；
- 将强入口、普通入口、并行码牵引、位同步开放资格、20种bit边界搜索、独立确认和锁定后的变化按实际处理顺序展开；
- 明确普通入口的11点、$\pm100$ Hz频率网格只属于载波牵引，当前L1 C/A位同步的频率候选固定为0 Hz；
- 明确首次候选可以先对齐20 ms积分，但只有独立确认通过后，监督状态才把位同步视为锁定；
- 新增L1 C/A总流程图，并按原始尺寸完成视觉检查，没有发现文字压线、箭头断开、模块交叉或内容裁切。

## 冻结的L1 C/A方案

1. 强捕获入口使用相邻1 ms Prompt叉点积鉴频，20对结果平均，每20 ms更新FLL；
2. 普通捕获入口使用10 ms短相干、1280 ms功率窗和11点DFT，每160 ms更新FLL；
3. 两条入口都用最近三次绝对频率一致性，以及当前残差和三次极差均不超过15.625 Hz的条件开放位同步；
4. 位同步只比较20种bit边界，采用1/2/4 s自适应搜索，并使用另一批2/4 s数据独立确认；
5. 码牵引与载波牵引并行运行，载波辅助承担主要码率漂移；
6. 本章暂不写B1I的NH20和G1的meander，待本章评审后分别编写后续章节。

## 检查结果

- 最终文档只保留一个第四章，旧八信号同步章节已移除；
- 文档中的66个块公式分隔符成对；
- Obsidian图片链接与SVG文件一致；
- `git diff --check`通过；
- 本次只修改文档和图片，没有重新修改或运行跟踪代码；
- `.obsidian`中的用户配置和主题改动未纳入本次提交。

下一步先由你审核L1 C/A这一章。确认后再按同样结构单独编写B1I牵引与NH20/bit同步章节。
