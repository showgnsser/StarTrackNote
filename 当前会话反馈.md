# 位同步后与弱信号载波观察审查

## 当前实际方案

当前代码在位同步后并不是统一使用`atan2`，而是按状态选择功率DFT或FMS：

| 状态 | 当前鉴频器 | 观察长度 |
|---|---|---:|
| ShortNormal、ShortSlow | 11点功率DFT | 20 ms |
| LongFast、LongNormal | 11点功率DFT | 160 ms |
| LongSlow、MidDynamic、HighDynamic | 11点功率DFT | 320 ms |
| Sensitivity | 三点FMS | 320 ms |
| BitAiding | 三点FMS | 600 ms |
| OutdoorBitAiding | 11点功率DFT | 300 ms |

代码中已经存在点积、叉积观察器：符号未知时执行二象限折叠，符号已可靠擦除时执行四象限`atan2`。但是当前正式状态表几乎没有让它接管同步后的主频率控制。

## 对弱信号性能的影响

如果只把同步前强入口的逐对`asin`改成20对二象限atan，不会直接影响弱状态，因为同步前强入口和同步后DFT/FMS使用不同观察器。

如果把Sensitivity当前320 ms FMS直接换成“单个20 ms bit内部19对atan2”，则很可能明显降低弱信号性能：

- 观察时间从320 ms缩短到20 ms；
- 低载噪比下单bit相位容易越过反正切阈值；
- 当前FMS通过长时间功率累加取得的处理增益会丢失。

所以不能把“位同步后使用atan2”作为所有状态的统一替换。若要评估同步后atan/atan2，必须保持原状态的320或600 ms总观察时间，与当前DFT/FMS做同输入A/B。

## atan与atan2的正确切换条件

真正决定使用二象限还是四象限的不是“是否完成位同步”，而是**导航符号是否已知并完成擦除**：

- 只知道bit边界、不知道bit正负：跨bit观察仍须二象限折叠、平方或功率域处理；
- 导航符号已经可靠擦除：可以使用四象限`atan2`；
- 只在同一个bit内部形成差分对也可以使用`atan2`，但单bit观察不能直接替代弱状态长窗。

## 本次文档修正

- 软件方案更新为V0.15；
- 保留同步前强入口二象限atan目标方案；
- 删除“位同步后统一切换atan2”的错误结论；
- 增加当前同步后各状态DFT/FMS表及弱性能风险说明；
- 流程图改为“同步后按状态使用功率DFT或FMS，atan2仅为待测候选”；
- 当前生产代码未修改，用户`.obsidian`配置未改动。

## 下一步

第一步只A/B同步前`asin`与二象限atan。同步后atan/atan2属于独立优化包，必须保持各状态原观察时长，分别与20/160/320/600 ms DFT或FMS比较，不能和强入口整改一起替换。
