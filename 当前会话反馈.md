# 当前会话反馈

## 结论

已经按芯片可见结构做了真实IQ A/B。结论是：

> 芯片的“位同步前相邻1 ms鉴频、位同步只搜NH20相位”阶段划分在强、中信号下可以工作，但不能把重建源码中的叉积公式直接接到当前原始Prompt上，也不能满足B1I 24 dB-Hz、残差正负125 Hz的普通移交目标。

因此该候选不能直接替换当前正式方案。芯片中的`pst_cord_iq_data`不是当前原始PDI Prompt，或者重建源码缺失了NH符号处理。

## 测试候选

生产工作区未修改。当前WIP被复制到：

```text
/tmp/startrack-b1i-chip-loop-src
```

候选只做三件事：

1. B1I普通未同步入口不再使用11点长DFT；
2. 改用相邻1 ms Prompt频率观察，160 ms非重叠平均和160 ms控制更新；
3. DataBitSync保持源码配置，只用1个中心频点搜索20种NH20相位。

随后分别测试两种输入：

- 候选A：直接使用原始Prompt叉积，最接近重建源码字面表达；
- 候选B：先把Prompt复数平方消除NH正负号，再做同样的1 ms相邻鉴频和160 ms平均。候选B不是芯片源码可直接证明的步骤，只用于定位缺失的符号处理。

## 候选A：原始Prompt叉积

测试范围为复合C/N0等于24、32和40 dB-Hz，残差频率为正负62.5和正负125 Hz，零码差，seed 20260831，共12例，每例15秒。

结果：

| C/N0 | 完成位同步 | 同步后物理精度也合格 |
|---:|---:|---:|
| 24 dB-Hz | 1/4 | 1/4 |
| 32 dB-Hz | 0/4 | 0/4 |
| 40 dB-Hz | 2/4 | 0/4 |
| 合计 | 3/12 | 1/12 |

40 dB-Hz的两个同步案例属于假稳定：

- 真值−125 Hz时，最终载波控制约+382 Hz，10至15秒频率RMS约501 Hz；
- 真值−62.5 Hz时，最终载波控制约+44 Hz，RMS约106 Hz。

只有24 dB-Hz、−62.5 Hz一例同时满足同步和基本频率精度，不能构成可用方案。

## 为什么原始叉积必然有问题

B1I公开NH20为：

```text
+ + + + + - + + - - + - + - + + - - - +
```

相邻毫秒的NH符号乘积在一个循环中恰好有10个+1和10个−1，其总和为0。若原始Prompt仍带NH符号，则真实相位增量对应的叉积也会被交替乘以正负号；跨完整NH周期直接平均会抵消真实频率信息。

这说明源码中的：

```text
pst_cord_iq_data
```

不能简单理解为当前硬件上报的原始Prompt。该数据进入`signal_tracking_1ms()`以前必然还有未重建的处理，或者源码截图重建漏掉了NH符号处理。直接照抄可见叉积公式在物理上不闭合。

## 候选B：平方消除NH符号

平方候选使用同样的1 ms原子、160 ms非重叠观察和单频点NH同步，仅在鉴频前消除正负号。

### 32和40 dB-Hz

残差频率正负62.5和正负125 Hz各4例：

| C/N0 | 完成位同步 | 首次同步范围 | 10至15秒频率RMS/P95 | 码误差P95 |
|---:|---:|---:|---:|---:|
| 32 dB-Hz | 4/4 | 3.52至8.02秒 | 最坏0.582/1.147 Hz | 最坏0.0796 chip |
| 40 dB-Hz | 4/4 | 3.52至3.68秒 | 最坏0.541/1.587 Hz | 最坏0.0488 chip |

强、中信号下阶段结构可以工作，而且同步后的频率和码精度都合格。

### 24 dB-Hz

首先测试两个固定种子、残差正负62.5和正负125 Hz，共8例：

```text
完成位同步：0/8
```

随后补零残差和正负31.25 Hz：

| 残差 | 位同步 | 首次同步 | 频率RMS/P95 | 码P95 |
|---:|---:|---:|---:|---:|
| 0 Hz | 通过 | 7.54秒 | 0.937/1.538 Hz | 0.0889 chip |
| +31.25 Hz | 通过 | 7.54秒 | 1.428/2.766 Hz | 0.1650 chip |
| −31.25 Hz | 失败 | 无 | NCO发散 | 不计 |

因此24 dB-Hz合计只通过2/11，而且存在明显正负不对称；仍不能覆盖捕获移交范围。

## 确定性测试

候选构建成功。定向CTest结果：

```text
test_prompt_fll_obs   PASS
test_data_bit_sync    PASS
test_data_track_engine FAIL
```

原始叉积候选在TrackEngine测试中锁到了错误B1I边界；平方候选则有场景在12秒内不能锁定。这与真实IQ的失败相互印证，不是分析器判据造成的假失败。

## 如何理解芯片方案

芯片初始化有两个入口：

1. 捕获已经给出位同步结果：直接进入同步后的正常跟踪；
2. 捕获没有给出位同步结果：才运行1 ms预同步环路。

所以芯片的1 ms预同步路径很可能主要服务强捕获入口；中弱捕获能够通过更长捕获积累给出NH/bit边界，随后不再承受这条预同步路径的低SNR限制。本次32/40 dB-Hz通过、24 dB-Hz失败的结果与这种职责划分一致。

当前不能把“芯片强入口可用”外推成“24 dB-Hz普通移交也应使用同一方案”。

## 当前决策

- 不修改生产代码；
- 不把芯片重建源码中的原始叉积直接写进正式方案；
- 不继续扩大该候选的码差、20种NH相位和更多种子矩阵，因为24 dB-Hz零码差基本角已经失败；
- 历史11频率×20 NH相位联合搜索仍是当前唯一具有完整540/540真实IQ证据的B1I宽残差方案；
- 若仍要复现芯片路径，下一步必须先找回`pst_cord_iq_data`的生成过程和原始三参数CORDIC函数，确认NH符号在什么位置被处理，不能继续猜测。

## 证据位置

```text
原始叉积结果：/tmp/startrack-b1i-chip-loop-iq
平方160 ms结果：/tmp/startrack-b1i-chip-square160-iq
候选源码：/tmp/startrack-b1i-chip-loop-src
候选构建：/tmp/startrack-b1i-chip-loop-make
```

二进制SHA-256：

```text
原始叉积StarTrack：0b9f2d5ea72cfd0452f9cf2e250da6cf799c88f96792ca3b3e2ece19fc8559e1
平方候选StarTrack：f925c90694aa52f78d3db591a3a7fdf5129ef3d991ca9796f4e26e12a16f2bac
StarGen：c7b91c678959b705a3b4adf82b6ab49d42df4715d539a53f07d13a981fe06aa7
```
