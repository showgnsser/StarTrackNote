# 当前会话反馈

## 结论

当前问题不是11点频率网格造成的，而是11点网格所在的处理阶段发生了变化。历史提交`a75172f`已经完成B1I完整540例接受移交测试，结果为540/540通过；当前11点LongFast小矩阵只有14/27通过。因此不能用“以前没有覆盖这些角”解释本次回归，当前方案确实丢失了旧方案的保护结构。

## 之前的方案

旧方案把B1I的11个频率候选放在位同步器中：

1. B1I的NH20相位尚未确定时，LongFast保持捕获移交的载波NCO，不允许预同步粗频观察持续修改FLL；
2. 位同步器同时枚举11个频率候选和20种NH/bit边界；B1I频点间隔为25 Hz，覆盖$-125$ Hz至$+125$ Hz；
3. 每个边界假设先按公开NH20符号擦除，再形成完整20 ms相干功率；
4. 首次搜索使用1秒数据，随后使用独立的2秒或4秒数据确认；
5. 只有搜索和独立确认通过后，残余频率才一次性吸收到载波NCO。

旧方案的关键不是“频点更多”，而是“长时间联合判断后只校正一次”。错误的短时粗频峰不能连续推动NCO，所以不会形成频率正反馈。历史540例的最慢同步为11.097秒，最差频率RMS/P95为2.233/3.820 Hz，最差码误差P95为0.02298 chip。

## 当前方案

当前方案把频率估计从位同步器前移到LongFast闭环：

1. 位同步器只保留0 Hz一个中心频率候选，负责搜索20种NH/bit边界；
2. B1I的1 ms Prompt先复数平方，消除NH20和导航bit正负号；
3. 平方Prompt每10 ms相干，连续256个短块形成2560 ms功率窗；
4. 2560 ms窗口每160 ms滑动发布一次11点DFT结果；相邻两次结果共享2400 ms数据；
5. 每条被观察器标为有效的结果立即进入FLL，之后才用连续频率条件决定是否开放位同步。

当前观察有效条件实际非常宽松：`min_peak_ratio=1.0`、`min_norm_power=0.0`并允许边缘频点。最大功率与次峰之比按定义不会小于1，归一化功率不会小于0，$\pm125$ Hz边缘峰也允许发布，所以热噪声选出的错误峰仍会被标为有效。

当前还删除了原有`FreqGuard`。旧LongFast并不是使用5 Hz直接门限，而是允许78.125 Hz以内的变化直接通过，更大的跳变要求连续三次候选一致且至少间隔一个完整观察跨度。更重要的是，旧B1I在位同步前本来就保持NCO，因此预同步粗频结果不会进入FLL；`FreqGuard`只是第二层保护。

## 为什么会失控

当前处理顺序是：错误DFT峰发布有效结果，FLL据此改变NCO，下一次2560 ms重叠窗口又同时包含多个旧NCO和新NCO时段，再次选出新的错误峰。由于每160 ms都直接反馈，错误会沿着NCO变化继续放大。失败日志中可以看到粗频结果在$-125$、$-100$、$+100$和$+125$ Hz等频点之间跳变，但`freq_valid`和`freq_used`始终为1，最终频率RMS达到23～853 Hz。

连续三次频率资格只控制位同步何时开放，不能阻止前面的错误观察进入FLL，所以它不是载波控制安全门。

## 两套方案的本质区别

| 项目 | 历史540/540方案 | 当前14/27方案 |
|---|---|---|
| 11点候选位置 | 位同步阶段 | LongFast载波牵引阶段 |
| NH处理 | 每个边界候选擦除NH20 | Prompt复数平方 |
| 频率统计 | 1/2/4秒搜索与独立确认 | 2560 ms滑窗、160 ms更新 |
| 位同步前NCO | 保持捕获移交值 | 每次有效DFT结果都更新 |
| 校频方式 | 确认后一次性校正 | 连续FLL反馈 |
| 粗差隔离 | NCO保持，另有FreqGuard | 两层保护均已取消 |

因此真正的回归点是“把频率候选从位同步联合搜索移到预同步连续FLL”，不是“5点改成11点”。若恢复旧B1I路径，应恢复位同步阶段的11点频率与20种NH边界联合搜索，并在其确认前保持载波NCO；L1 C/A和G1是否继续采用当前LongFast方案可以单独决定，不应为了配置表统一而强迫三种信号使用完全相同的牵引阶段。
