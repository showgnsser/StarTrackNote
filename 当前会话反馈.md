# 当前会话反馈

不能笼统地说“B1I根本没有使用DFT”，必须区分阶段。

## 位同步前的牵引阶段

B1I没有使用多频点DFT搜索。芯片配置为：

```text
track_type   = TRACK_1MS_LOOP
freq_bin_num = 1
```

载波环使用相邻两条1 ms Prompt的叉积形成频率误差：

$$
P_{cross}=I_{k-1}Q_k-Q_{k-1}I_k
$$

然后在当前控制周期内平均这些逐毫秒误差并送入载波环滤波器。这个阶段没有11点DFT，也没有平方Prompt长滑窗。

## 位同步阶段

B1I位同步器的通用代码支持“NH相位×频率候选”二维搜索，但B1I配置的`freq_bin_num=1`，实际只有0 Hz中心候选。其主要工作是枚举20种NH20相位/边界并比较相干功率，不执行多频点DFT频率搜索。

因此，对“B1I捕获移交后的牵引和位同步”而言，可以简化为：

```text
1 ms叉积鉴频闭环
    -> 频率先收敛
    -> 单频点NH20相位搜索
    -> 位同步确认
```

## 位同步后的正常跟踪

位同步以后不能再说完全没有DFT。正常跟踪公共路径会根据状态形成较长相干结果和频率候选功率，并由相应载波鉴频器更新环路。B1I会复用这套普通跟踪基础设施，其中存在DFT频率观察。

所以准确结论是：

> 芯片B1I在位同步前牵引和位同步搜索中不使用多频点DFT；位同步后的正常跟踪状态仍可能使用公共DFT载波观察。

这与当前实验方案“位同步前使用11点、2560 ms重叠DFT持续驱动FLL”有本质区别。
