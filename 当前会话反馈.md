# 当前会话反馈

重新逐项对照后，结论需要分开说。

## DLL是否与当前实现一致

第四章现在描述的DLL与L1 C/A、B1I和G1“数据位同步前牵引路径”一致：

- ShortFast每1 ms对五抽头取功率，使用五抽头宽峰与Early–Late码鉴相，20条鉴相结果固定平均后每20 ms推进一次PI；
- LongFast的L1 C/A和G1先形成10 ms五抽头复相干幅值，再把16个短块非相干累加到160 ms；
- LongFast的B1I把160条1 ms五抽头功率非相干累加，避免NH20符号进入线性相干；
- LongFast固定以Prompt为鉴别中心；ShortFast才允许五抽头宽峰选择；
- 正码误差提高DLL残余码率，负码误差降低码率；无效观察保持上一条PI输出；
- 最终码率等于标称码率、载波辅助码率和DLL残余码率之和。

但这不等于“全状态、全信号DLL”都只使用本章公式。同步后的普通状态可以使用五抽头双差，Sensitivity可保留95%的旧积分量，E1/B1C使用ASPeCT码鉴别。文档已补上这一适用范围。

## L1 C/A与G1的DFT范围

这里发现了真实的不一致：上一版文档写5点DFT，但当前工作区实际仍是11点。

| 项目 | 当前实际配置 |
|---|---|
| L1 C/A LongFast | 11点，$-100:20:+100$ Hz |
| G1 LongFast | 11点，$-100:20:+100$ Hz |
| B1I LongFast | 11点，$-125:25:+125$ Hz |
| 数据位同步 | 只保留0 Hz，不再重复枚举频率 |

此前验证的$\{-50,-25,0,+25,+50\}$ Hz网格属于数据位同步实验。它使用20 ms bit相干并联合搜索bit边界，不是LongFast使用10 ms或5 ms短块的载波DFT；后来频率搜索已经从位同步中移除。因此，那次结果不能直接证明LongFast采用$\pm50$ Hz已经通过。

另外还做过LongFast“5点但仍覆盖$\pm100$ Hz”的A/B，即L1 C/A和G1使用$\{-100,-50,0,+50,+100\}$ Hz。L1 C/A的$\pm62.5$ Hz两角通过；G1的$-62$ Hz通过，$+62$ Hz最终同步但首次同步为12.679 s，15 s短窗同步率只有0.78，因此没有形成完整通过结论。它也不是$\pm50$ Hz方案。

从物理上看，L1 C/A/G1改成$\{-50,-25,0,+25,+50\}$ Hz是合理候选：正式$\pm62.5$ Hz移交落在边缘外12.5 Hz，边缘频点允许进入FLL；12.5 Hz残差对10 ms相干的理论损失约0.224 dB，对5 ms相干约0.056 dB。但这只是物理可行性判断，尚缺LongFast真实IQ的正负频差、多种子和闭环收敛A/B，不能直接写成已经验证的生产参数。

文档已恢复为当前真实11点配置，并明确把5点或$\pm50$ Hz列为待验证简化方案。本轮没有修改生产跟踪代码，也没有提交用户的Obsidian配置修改。
