# 当前会话反馈

## 上一版1.28秒/2.56秒长窗DFT怎样工作

上一版的“长窗DFT”并不是把1.28 s或2.56 s数据一次性做线性相干积分。它仍然从连续1 ms Prompt复相关开始，先形成许多5 ms或10 ms短相干块，再把短块功率放进1.28 s或2.56 s滑动窗内做非相干累积。

“长窗”指的是总功率统计窗，不是单个相干积分长度。

## 1. 输入

每条输入仍是硬件已经完成相关的1 ms Prompt：

\[
P[k]=I[k]+jQ[k].
\]

观察器连续保存这些1 ms结果。

## 2. 五个频率候选

最后测试的长窗版本同样使用：

\[
f_m\in\{-62.5,-31.25,0,+31.25,+62.5\}\text{ Hz}.
\]

对每个候选频率，逐条旋转1 ms Prompt：

\[
\widetilde P_m[k]
=P[k]e^{-j2\pi f_m t_k}.
\]

如果窗口期间载波NCO控制字发生变化，上一版还会积分每条PDI记录的NCO历史，把所有Prompt换算到窗口平均NCO参考。这个步骤只消除软件自己换载波控制字造成的分段相位，不能消除真实信号动态。

## 3. L1 C/A的1.28秒统计

L1 C/A每10条旋转后的1 ms Prompt形成一个短相干块：

\[
S_{m,r}
=\sum_{n=0}^{9}\widetilde P_m[10r+n].
\]

然后累加128个短块功率：

\[
J_m
=\sum_{r=0}^{127}|S_{m,r}|^2.
\]

因此：

\[
10\text{ ms}\times128=1.28\text{ s}.
\]

未知导航bit可能破坏跨bit边界的10 ms块，但不会让不同短块在复数域互相抵消。观察器不枚举bit边界，只依靠短块功率累加降低bit翻转影响。

## 4. G1的1.28秒统计

G1使用更短的5 ms相干块：

\[
S_{m,r}
=\sum_{n=0}^{4}\widetilde P_m[5r+n],
\]

\[
J_m
=\sum_{r=0}^{255}|S_{m,r}|^2.
\]

所以：

\[
5\text{ ms}\times256=1.28\text{ s}.
\]

这里同样不搜索meander相位，只靠缩短相干块降低符号翻转造成的损失。

## 5. B1I的2.56秒统计

B1I的NH码可以逐毫秒翻转。上一版先把每条Prompt做复数平方：

\[
P_2[k]=P[k]^2.
\]

因为

\[
(-P[k])^2=P[k]^2,
\]

平方能够消除NH码和未知数据符号的正负号。但平方也会把载波相位和频率变成两倍，所以候选旋转必须使用二倍角：

\[
\widetilde P_{2,m}[k]
=P_2[k]e^{-j4\pi f_m t_k}.
\]

之后每10 ms形成一个平方域短相干块：

\[
S_{m,r}
=\sum_{n=0}^{9}\widetilde P_{2,m}[10r+n],
\]

并累加256个块的功率：

\[
J_m
=\sum_{r=0}^{255}|S_{m,r}|^2.
\]

因此：

\[
10\text{ ms}\times256=2.56\text{ s}.
\]

B1I使用2.56 s而不是1.28 s，是因为复数平方虽然消除了NH符号，却在低信噪比下产生更差的非线性噪声统计，需要更多短块功率才能区分频点。

## 6. 怎样选择频率

得到五个功率

\[
\{J_0,J_1,J_2,J_3,J_4\}
\]

后，观察器选择最大功率频点。内部频点再使用左右相邻功率做三点抛物线插值；正负62.5 Hz边缘频点保留粗结果。

输出频率相对于长窗平均NCO表示。

## 7. 为什么叫滑动长窗

虽然总统计窗是1.28 s或2.56 s，但观察器每20 ms发布一次结果：

- 1.28 s窗的相邻两次输出共享1260 ms数据，重叠率约98.44%；
- 2.56 s窗的相邻两次输出共享2540 ms数据，重叠率约99.22%。

也就是说，第一次结果需要等待1.28 s或2.56 s成熟；成熟以后，每来20条新PDI，就删除最旧20条，再用几乎相同的整窗重新计算五个频点。

## 8. 输出怎样进入载波环

每个有效长窗结果都会按观察中心时刻重定时到当前控制边界，然后进入FLL：

\[
f_{\mathrm{control,new}}
=\operatorname{FLL}\left(\widehat{\Delta f}_{\mathrm{long}}\right).
\]

最近三次长窗结果还用于开放位同步：

1. 三次结果都有效；
2. 当前残差绝对值不超过15.625 Hz；
3. 三次绝对频率最大值与最小值之差不超过15.625 Hz。

满足后才允许位同步器开始搜索。

但是相邻三次结果只跨越40 ms新数据，绝大部分1.28/2.56 s历史完全相同，因此它们不是三份独立频率证据。

## 9. 后面为什么又有一次频率搜索

长窗DFT只搜索频率，不搜索bit、NH或meander边界。长窗资格成立后，位同步器还会重新执行：

\[
5\text{个频率}\times20\text{个边界}
\]

的联合搜索。

所以旧数据链为：

    1 ms Prompt
      → 1.28/2.56 s短块功率长窗DFT
      → 每20 ms重复更新FLL
      → 三次高度重叠结果形成资格
      → 位同步器再次搜索5个频率和20个边界
      → 再次校正NCO
      → 独立确认边界

这正是它被废弃的主要原因：同一阶段存在两套频率搜索和两次频率反馈。

## 10. 实测问题

严格的“两层5点搜索”实测结果为：

- B1I代表矩阵26/27通过；
- G1最坏边界52/60通过。

失败并不是没有处理增益，而是前级长窗可能在未知符号、平方噪声或特定边界下选择错误频点。错误结果又每20 ms通过高度重叠的窗口反复写入FLL，后面的位同步搜索已经来不及恢复。

因此当前不再把1.28 s/2.56 s长窗接入数据型普通入口。
