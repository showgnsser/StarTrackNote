# 当前会话反馈

## 本次问题

验证三种数据型信号在位同步前把现有鉴频器改为两象限 `atan/atan2` 后，牵引和位同步效果是否更好。

## 测试结论

**不建议修改生产方案。** 两象限 `atan/atan2` 在很强信号下只有一个软件服务节拍量级的微小收益，进入中等和弱信号后反而明显降低频率牵引与位同步成功率。当前鉴频器应继续保留：L1CA、G1使用逐对归一化 `asin` 后平均；B1I使用平方Prompt消除逐毫秒NH符号后再鉴频。

本次没有修改生产仓库，所有候选代码和构建均位于 `/tmp`。

## 对比方案

1. **当前方案（base）**：逐对计算归一化叉积并使用 `asin`，20对频率结果平均；B1I使用平方Prompt。
2. **整窗两象限方案（atan）**：每对点积为负时同时翻转点积和叉积，20对复积先求和，最后只执行一次 `atan2(Σcross, Σdot)`；三种信号不再使用B1I平方Prompt。
3. **逐对两象限方案（pair）**：每对先按点积符号折叠，再执行 `atan2(cross,dot)`，最后平均20个频率结果；三种信号不再使用B1I平方Prompt。

位同步后原有DFT/FMS观察器和各状态环路均未改动。

## 已验证事实

### 确定性测试

两种atan候选都能在无噪声条件下正确处理：

- 窗口内任意一次数据bit翻转；
- 每毫秒交替的最坏符号序列；
- 残差频率 `±62.5 Hz` 和 `±249 Hz`；
- 强入口闭环的 `166 Hz/s` 动态测试。

当前 `asin` 方案在20对窗口中恰有一对跨bit翻转时，会得到真值的90%；例如 `62.5 Hz` 输出 `56.25 Hz`。但该偏差只污染一个窗口，不足以证明闭环会更差。

### 真实IQ A/B矩阵

测试信号为L1CA、B1I、G1，均从强捕获入口进入；码相位误差为0，初始频率控制字为0。每例15秒、两个信号种子，使用同一StarGen配置分别驱动三个StarTrack二进制。

- L1CA：残差 `±62.5 Hz`；C/N0为40、32、22 dB-Hz。
- B1I：残差 `±62.5 Hz`、`±125 Hz`；C/N0为40、32、24 dB-Hz。
- G1：残差 `±62.5 Hz`；C/N0为40、32、28 dB-Hz。

总计48个物理场景、144条真实IQ闭环运行，全部无PDI错误和命令错误。

| 信号强度 | 当前方案 | 整窗atan2 | 逐对atan |
|---|---:|---:|---:|
| 40 dB-Hz | 16/16同步 | 16/16同步 | 16/16同步 |
| 32 dB-Hz | 16/16同步 | 16/16同步 | 16/16同步 |
| 各信号牵引门限 | 9/16同步 | 3/16同步 | 1/16同步 |

40 dB-Hz下，整窗atan2的首次同步中位数相对当前方案只提前约10～30 ms：L1CA为3160对3170 ms，B1I为3200对3210 ms，G1为3170对3200 ms。该差异最多约一个20 ms服务节拍，不构成实质收益。

32 dB-Hz下已经出现退化：

- L1CA首次同步中位数：当前3240 ms，整窗atan2为4710 ms，逐对atan为7460 ms；
- B1I首次同步中位数：当前3180 ms，整窗atan2为3580 ms，逐对atan为4320 ms；
- G1整窗atan2中位数略快110 ms，但不同种子和频差方向的结果不一致，逐对atan仍变慢。

牵引门限下退化最明显：

- L1CA 22 dB-Hz：当前2/4同步，两个atan候选均0/4；
- B1I 24 dB-Hz：当前3/8同步，整窗atan2为0/8，逐对atan为1/8；
- G1 28 dB-Hz：当前4/4同步，整窗atan2为3/4，逐对atan为0/4。

部分atan候选在低C/N0下把载波环推离真值数百至数千赫兹，不能作为可接受的随机波动。

## 原因解释

无噪声时，负点积确实可以解释为数据bit、NH码或meander造成的半周翻转，因此折叠后使用atan2是正确的。但在低C/N0下，负点积也经常只是噪声造成的。位同步前没有额外证据区分这两种来源，每一对都按点积符号折叠，相当于对噪声作硬判决，会削弱真正频差的统计方向。

整窗atan2还会按复积幅度隐式加权，少量大幅噪声复积能够支配整个20 ms观察。逐对atan避免了这种幅度支配，却仍然保留“逐对硬判符号”的问题。

当前方案的取舍更稳健：L1CA和G1允许真正的符号边界只污染20对中的一对，以换取其余19对不被噪声硬折叠；B1I逐毫秒NH翻转过于频繁，因此使用复数平方在代数上消除符号，不依赖低SNR下不可靠的点积正负判决。

## 最终决定

- 保留当前位同步前鉴频器，不采用整窗 `atan2(Σcross,Σdot)`。
- 不采用逐对折叠 `atan2(cross,dot)` 统一替换三种信号。
- 不取消B1I的平方Prompt鉴频。
- 位同步后的DFT/FMS及弱信号状态保持不变。
- 若后续希望修复L1CA/G1单个bit边界造成的10%瞬时偏差，应研究只识别“窗口中唯一边界”的软处理，不能对全部相邻对逐一按dot符号硬折叠。

## 证据位置

- 完整机器摘要：`/tmp/startrack-prompt-fll-atan-ab/summary.json`
- 摘要SHA-256：`5f276806219da6d0a870837fdce2d59f741910deaf8559ee2448285258632394`
- 基线StarTrack SHA-256：`b22edc5fbd4e24625490ee18da6ae7c0bbaa9da2737ba4ae712ea2aa4906e6ef`
- 整窗atan2 StarTrack SHA-256：`d9a0b93a5f38ff4755c5eda500928672df0e4fe839c3cbe30daf3a59b9d4ce4e`
- 逐对atan StarTrack SHA-256：`fb2d3eb46734d0c6f0acc04257cd7bb67194b7a68fef75338720d80fe7fdb9b9`
- StarGen SHA-256：`c7b91c678959b705a3b4adf82b6ab49d42df4715d539a53f07d13a981fe06aa7`
