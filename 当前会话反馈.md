# 当前会话反馈

## 冻结结论

三种数据型信号的牵引算法结构可以冻结，但这表示“设计方案不再继续分叉”，不表示三种信号的完整资格已经完成。

冻结后的方案如下：

- 强入口统一使用相邻1 ms Prompt鉴频，20 ms平均，三阶FLL每20 ms更新；B1I先对Prompt平方以消除NH码和导航bit反号；
- 中弱入口统一使用160 ms非重叠观察窗和160 ms FLL更新周期；
- L1 C/A使用10 ms短相干块、16次功率累加和11点DFT；
- G1使用5 ms短相干块、32次功率累加和11点DFT；
- L1 C/A与G1的11个频率候选间隔12.5 Hz，总范围为±62.5 Hz；
- B1I不在载波牵引中枚举NH或bit边界，不使用320 ms滑动窗；它对159个相邻1 ms鉴频结果做160 ms平均，再更新三阶FLL；
- 三种信号的数据位同步均使用11个频率候选和20种边界；B1I只在位同步度量中按候选边界擦除NH20；
- 三种信号使用同一套五抽头、Early–Late、PI、Kca和码NCO闭环。强弱入口只改变码观察的积累方式和更新周期。

## 文档整改

已重写软件方案第四章，结构改为：

1. 强信号载波牵引；
2. 中弱信号载波牵引；
3. 码环牵引；
4. 数据位同步。

码环不再挤在强信号章节中。新的码环章节统一说明五抽头观测、宽峰选择、Early–Late鉴别、PI递推、Kca换算和码NCO码率合成，并用表格单独列出强入口、L1 C/A与G1中弱入口、B1I中弱入口如何形成码观察。

数据型信号总览图已经删除旧的1.28 s、2.56 s滑动窗以及“B1I牵引阶段枚举NH边界”等内容，改为三条互不交叉的并行支路：载波牵引、数据位同步和码环牵引。

DLL图已重画：

- Early–Late公式改为可直接阅读的上下分式；
- 用三个独立框说明居中、提高码率和降低码率；
- Kca三角形只保留`Kca`，不再放置越界文字；
- 标称码率和Kca多普勒项先合成为基础码率，再与DLL残余码率相加，连线不再重叠。

## 验证依据与边界

已有简化B1I方案的真实IQ开发矩阵结果：

- 三信号30 s矩阵最终同步并稳定47/48；
- B1I全部NH起始相位矩阵最终同步并稳定38/40；
- 已确认的失败集中在B1I特定NH相位、边界频差方向和动态组合；
- 用户已接受该边界失败风险，以换取不枚举NH边界、不使用320 ms滑动窗的低计算量结构。

因此当前可以冻结算法结构，但仍不能宣称B1I或三种数据型信号已经完成全场景资格。

## 本次修改文件

- `10-逐模块软件设计/19-跟踪软件设计方案.md`
- `90-附件/图STSW-03_数据型信号牵引与位同步_v01.svg`
- `90-附件/图STSW-04_DLL码鉴相与PI码环_v01.svg`
- `当前会话反馈.md`

本次只修改算法方案和配图，没有修改跟踪代码。
