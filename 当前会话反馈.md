# 强入口鉴频器重新审查

## 结论

你的质疑成立。当前“逐对取`asin`再平均”的路径没有足够理由继续作为目标方案。更合理的强入口是：

1. 连续形成20对相邻Prompt；
2. 分别累加20对点积和叉积；
3. 最后只执行一次`atan2(叉积总和, 点积总和)`；
4. 将所得相位变化除以1 ms，形成频率结果。

当前代码已经在累加`dot_sum`和`cross_sum`，但它们只被用于质量统计；频率仍由20个逐对`asin`结果平均得到。这一取舍不合理。

## 为什么不能直接平均20个逐对atan2

L1 C/A的未知导航bit翻转，以及G1的meander翻转，会让跨边界那一对Prompt的点积和叉积同时反号。若先对这一对单独执行完整四象限`atan2`，会得到接近$\pm\pi$的相位跳变，对应接近$\pm500$ Hz的假大频差。

正确处理不是退回`asin`，而是先累加点积和叉积。跨边界差分只会作为一个反向向量从总和中减掉：L1 C/A的20对中最多影响1对，G1最多影响2对，其余向量仍决定总方向。

## 三种信号的目标方案

- L1 C/A、G1：直接对Prompt形成20对点积和叉积，再计算`atan2(Σcross, Σdot)`，唯一鉴频范围为$(-500,+500)$ Hz。
- B1I：先平方Prompt消除NH码和导航bit正负号，再累加平方Prompt的点积和叉积，最后计算`atan2`并除以二倍角尺度，物理唯一范围为$(-250,+250)$ Hz。

这种方法比逐对角度平均还有两个优势：它天然按相关幅度给差分向量加权；低功率噪声PDI不会与强Prompt获得完全相同的权重。

## 芯片源码边界

重建芯片源码的`signal_tracking_1ms()`只明确形成了叉积量，且其反正切调用存在实参数量与函数定义不一致的缺损。源码注释还明确写着GPS可以改用`atan2`。稳定/GEO路径本身也使用点积和叉积的四象限`atan2`。因此没有证据证明当前`asin`是必须保留的芯片物理合同。

## 本次文档修改

- 软件方案更新为V0.13；
- 强入口目标方案改为20对差分复积累加后执行一次`atan2`；
- 分别写明L1 C/A、B1I和G1的符号翻转处理与无模糊范围；
- 明确记录当前生产代码仍为逐对`asin`，切换前需要同输入A/B；
- 本次没有修改生产代码，也没有改动用户的`.obsidian`配置。

## 下一步

先用相同PDI对“逐对`asin`平均”和“20对累加后`atan2`”进行离线及闭环A/B，覆盖正负频差、bit/meander边界、B1I NH翻转、低载噪比和动态场景；通过后再修改生产观察器。
