# 当前会话反馈

## 本次问题

1. 芯片重建源码实际使用什么鉴频器？
2. 刚才的测试中，当前asin方案是否也不能让所有场景成功位同步？

## 芯片重建源码的实际证据

芯片证据文件为`/home/showskills/SN770/PowerPoint/track_rebuilt.c`，其内容由源码截图重建，并非可直接编译的完整原工程。

### 位同步前强入口

`signal_tracking_1ms()`执行以下步骤：

1. 读取相邻两个1 ms Prompt；
2. 形成叉积：

$$
cross=I_{k-1}Q_k-Q_{k-1}I_k;
$$

3. 对每一对形成频率误差；
4. 累加并平均20个1 ms鉴频结果。

可见重建代码没有形成dot。调用被重建为：

```c
arctan_cordic(cross, VECTOR_GAIN_SHIFT + 2, 15)
```

但同一文件中可见的`arctan_cordic`定义只有两个参数。因此可以确认“相邻1 ms、cross鉴频、20次平均”的总体结构，不能从现有缺损资料唯一证明它是标准`atan(cross/dot)`、标准`asin`，也不能证明其确切定点缩放。

源码注释还写明“GPS在20 ms内没有bit变化时可以使用atan2”，说明当前这条可见路径本身并不是标准四象限atan2。

当前StarTrack的逐对归一化cross＋asin，是根据可见cross结构恢复出的工程实现，不是已完成逐bit证明的芯片原公式。

### 其他阶段

- 芯片确实实现了二象限`arctan_cordic(I,Q)`：当I为负时同时翻转I/Q，返回`(-π/2,+π/2)`；可见调用主要用于相位鉴别。
- 芯片也实现了四象限`arctan2_cordic(I,Q)`；可见用于GEO专用dot/cross鉴频路径。
- 一般信号位同步后的普通状态使用功率DFT选峰和频率插值。
- 弱状态使用双支路FMS功率鉴频。

因此不能笼统地说“芯片所有状态都使用atan”。不同入口和状态使用不同观察器；位同步前1 ms路径的精确反函数又受重建源码缺损限制。

## asin基线是否所有场景都能位同步

### 强制走强入口的结果

是的，如果把各信号牵引门限案例全部强制送入`entry=strong`，当前asin基线并没有全部同步：

- L1CA 22 dB-Hz：2/4同步；
- B1I 24 dB-Hz：3/8同步；
- G1 28 dB-Hz：4/4同步；
- 合计9/16同步。

但这不能解释为“当前正式位同步失败”，因为强入口本来只对应强捕获结果。L1CA 22、B1I 24和G1 28 dB-Hz属于牵引门限，应使用中弱捕获对应的normal入口。

### 使用正确normal入口复测

使用同一个当前基线二进制、同一批StarGen场景和相同初始频差，将上述16例改为`entry=normal`后：

- L1CA 22 dB-Hz、`±62.5 Hz`、2种子：4/4同步；
- B1I 24 dB-Hz、`±62.5/±125 Hz`、2种子：8/8同步；
- G1 28 dB-Hz、`±62.5 Hz`、2种子：4/4同步；
- 合计16/16同步，首次同步时间4.64～10.74秒，15秒末16/16仍保持同步。

normal入口不使用1 ms asin强牵引路径，而是使用更长观察和功率频率搜索。因此这组结果说明：上一轮9/16失败的直接原因是入口选择错误，不是当前完整位同步方案在这些场景下失败。

### 当前证据边界

本次矩阵只覆盖两个种子、零初始码差和静态频差，不等于完整资格。它可以证明当前入口职责：

- 40和32 dB-Hz强入口测试均为16/16同步；
- 牵引门限应走normal入口，本次为16/16同步；
- 不能要求一个强入口鉴频器同时承担中弱入口职责。

## 当前结论

1. 芯片位同步前强入口可以确认是cross型相邻1 ms鉴频和20次平均，但现有重建资料不足以把它精确命名为标准atan或asin。
2. 芯片的二象限atan、四象限atan2、DFT和FMS都存在，但用于不同阶段。
3. 当前asin强入口确实不能覆盖所有牵引门限案例；这不是其设计职责。
4. 按捕获强弱选择正确入口后，本次48个物理场景中：40 dB-Hz强入口16/16、32 dB-Hz强入口16/16、牵引门限normal入口16/16均完成位同步。
5. 生产代码未修改；后续若优化atan，应只针对强入口按L1CA、B1I、G1分别设计，不能改变normal入口。

## 证据位置

- 芯片对齐报告：`/home/showskills/SN770/Project/StarTrack/docs/reports/chip_source_alignment_audit_20260814.md`
- 芯片对齐矩阵：`/home/showskills/SN770/Project/StarTrack/docs/knowledge/source_alignment_matrix.md`
- A/B与入口复测摘要：`/tmp/startrack-prompt-fll-atan-ab/summary.json`
- 最新摘要SHA-256：`3c6ea1e92ba813df313562f56437371a394481b7408eaebddcab2c7a34b168b4`
- 当前基线二进制SHA-256：`b22edc5fbd4e24625490ee18da6ae7c0bbaa9da2737ba4ae712ea2aa4906e6ef`
