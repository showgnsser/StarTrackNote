# 当前会话反馈

## 更正结论

前一轮出现的6个B1I牵引失败不是旧方案突然退化，而是测试对象已经不是旧方案。我把“11点DFT”从位同步阶段搬到了位同步之前，并让长窗平方Prompt DFT持续驱动FLL；这同时改变了DFT所在阶段、载波NCO更新方式、位同步开放条件和码频反馈关系，不能称为只修改频率范围。

恢复正确架构后，当前WIP在同一27个真实IQ角点中全部通过：

```text
27/27 PASS
```

之前集中在负0.5 chip的6个失败全部消失。

## 两套方案的本质区别

### 已验证的联合位同步方案

B1I普通入口按以下顺序工作：

1. 捕获多普勒先装入载波NCO；
2. 位同步前不使用长窗DFT反复修改载波NCO；
3. 160 ms功率DLL持续修正捕获移交码偏差；
4. 位同步器联合搜索11个频率候选和20个NH20相位；
5. 搜索结果通过后，一次性把残余频率写入载波控制量；
6. 后续独立确认使用新的NCO并锁定同一NH20边界。

这里的11点DFT属于位同步器的一部分，输出是一组频率与NH相位的联合判决。

### 产生21/27结果的错误候选

错误候选按以下顺序工作：

1. 位同步前先形成2.56 s平方Prompt DFT观察；
2. 观察器每160 ms滑动发布结果并持续驱动FLL；
3. 连续频率观察通过资格后才开放位同步；
4. 位同步器只搜索一个中心频点和20个NH20相位。

负0.5 chip时，长窗Prompt频谱会受到码相关损失和码环变化影响。错误频率观察被FLL持续采纳后，载波NCO离开正确中心，最终形成码频耦合发散。这就是前一轮6个失败突然出现的原因。

## 当前冻结候选

B1I普通入口恢复联合位同步方案：

- 频率候选数：11；
- 相邻频点间隔：12.5 Hz；
- 覆盖范围：正负62.5 Hz；
- 频率候选：`-62.5, -50, -37.5, -25, -12.5, 0, +12.5, +25, +37.5, +50, +62.5 Hz`；
- NH20相位候选数：20；
- 不使用额外9点局部频率细化；
- B1I普通入口位同步前不运行2.56 s平方Prompt FLL；
- 160 ms功率DLL保持工作；
- B1I强入口仍保留短周期预同步FLL，不受普通入口改动影响。

## 当前WIP真实IQ验证

统一条件：

- BDS B1I，PRN 6；
- 复合C/N0为24 dB-Hz；
- 频差为负62.5、0和正62.5 Hz；
- 码差为负0.5、0和正0.5 chip；
- NH20起始位置为0、10和19 ms；
- signal seed为20260831；
- 每例20秒，评分区间为15至20秒。

结果：

| 指标 | 结果 |
|---|---:|
| 案例通过数 | 27/27 |
| 最晚首次同步 | 7.078 s |
| 最坏频率RMS | 1.557 Hz |
| 最坏频率P95 | 4.144 Hz |
| 最坏码误差P95 | 0.0145 chip |
| 最低同步比例 | 1.0 |
| 最低频率观察有效率 | 1.0 |
| 最低频率观察使用率 | 1.0 |

这说明正负62.5 Hz范围下不需要B1I局部细化，也不需要在位同步前增加2.56 s平方DFT反馈。

## 代码修改

当前生产WIP只修改三处：

1. `src/sync/signal_sync.cpp`
   - B1I普通入口使用11个频点；
   - 频点间隔固定为12.5 Hz。
2. `src/tracking/track_engine.cpp`
   - B1I LongFast普通入口立即开放联合位同步；
   - 同步搜索期间保持捕获载波NCO；
   - 不再形成或使用位同步前2.56 s平方Prompt DFT。
3. `tests/tracking/test_data_track_engine.cpp`
   - B1I普通入口冻结“联合频率与NH搜索后一次性校频”的时序；
   - G1和强入口继续冻结“先频率牵引、后位同步”的原时序。

没有修改G1、L1CA、载波环系数、DLL系数、状态机或C/N0门限。

## 验证

当前生产WIP重新构建成功。定向测试：

```text
test_data_bit_sync      PASS
test_data_track_engine  PASS
git diff --check        PASS
```

当前Release二进制SHA-256：

```text
4298b2c716b08dadc9bf7a7bfcae1533e745226d8948d976b1a221498db1f38f
```

证据位置：

```text
当前WIP构建：/tmp/startrack-b1i-jointsync-actual-build
27例结果：/tmp/b1i-current-jointsync62p5-matrix
临时候选源码：/tmp/startrack-b1i-grid62p5-src
测试清单：/tmp/b1i-grid62p5-manifest.json
```

## 当前边界

- 27例是一个信号种子和三个代表NH相位的小矩阵，足以否定前一轮21/27的错误架构，并验证当前设计方向；它不是三种子、20个NH相位的完整180例资格。
- 正式campaign清单和runner的小数案例ID格式尚未写回生产WIP；算法先保持当前已验证状态。
- 当前StarTrack工作区原本已有大量未提交改动，本轮只保留上述三个文件的最小修改，不提交整个WIP。
