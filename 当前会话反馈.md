# 当前会话反馈

## 芯片源码结论

芯片对B1I的主设计不是“11个频率候选驱动长滑窗FLL”，而是把载波牵引和NH20相位搜索拆开：捕获若已经给出位同步结果，就直接进入位同步后的正常跟踪；捕获若没有给出位同步结果，B1I选择`TRACK_1MS_LOOP`，先用相邻1 ms Prompt做载波和码闭环，并把位同步频率候选数设为1。载波粗差由前级1 ms环路消化，位同步阶段主要搜索20种NH20相位/边界。

## 1. 两种捕获移交入口

源码初始化首先检查捕获是否已经给出位同步结果：

- 已给出：直接置`uc_bit_sync=1`，位同步频率候选数为1，然后按信号强度进入ShortFast或LongFast；
- 未给出：再选择“位同步前先闭环”还是“位同步器同时搜索频率”。

在未给出位同步结果时，B1I对应的`CHANNEL_BD_MEO_TYPE`被明确分到`TRACK_1MS_LOOP`，同时执行：

```text
track_type   = TRACK_1MS_LOOP
freq_bin_num = 1
```

同一段源码把GPS/GLO等另一类入口分到`TRACK_SHORTER_FMS_LOOP`，并给位同步器配置多频点。源码紧邻注释也明确表达了设计原则：只要位同步前已经运行跟踪环，位同步频点数就设为1。

因此芯片并不是所有数据型信号统一使用“11频点位同步”，而是存在两条策略：

1. 位同步前先用环路收敛频率，位同步只搜边界；
2. 位同步前不依靠该环路，位同步同时搜索边界和频率。

B1I被放在第一条策略中。

## 2. B1I位同步前的载波环

`signal_tracking_1ms()`明确写着“tracking loop before bit sync, use 1ms coherent int”。处理过程是：

1. 保存上一条1 ms Prompt复相关；
2. 读取当前1 ms Prompt复相关；
3. 形成相邻两条Prompt的叉积：

$$
P_{cross}=I_{k-1}Q_k-Q_{k-1}I_k
$$

4. 将叉积送入二象限CORDIC反正切近似，得到1 ms频率误差；
5. 同一毫秒计算各码抽头幅度并执行DLL码鉴别；
6. 在当前状态规定的控制周期内，对逐毫秒频率误差和码误差求平均；
7. 平均结果分别进入载波环滤波器和码环滤波器，再更新载波NCO与码NCO。

这里的基本相干原子始终是1 ms。ShortFast采用短控制周期，LongFast采用长控制周期；长周期只是累计多条1 ms鉴频/鉴码结果，不是先拼出一个2560 ms复相关，也不是每160 ms重算一个高度重叠的长DFT窗。

源码中的载波鉴频只使用$P_{cross}$，没有使用$P_{dot}$，因此它不是完整的`atan2(Pcross,Pdot)`鉴频器，更接近叉积归一化后反正弦/小角度反正切一类实现。源码还留有注释：GPS在20 ms内bit不翻转时可以考虑使用`atan2`。这说明该实现是按未知符号环境做的工程折中，而不是认为叉积鉴频器在所有场景都优于`atan2`。

## 3. B1I位同步如何处理NH20

位同步器仍然有“边界×频率”二维数组的通用实现，但B1I入口把频率候选数设为1，所以B1I实际只使用中心频点。对于每一个候选NH相位，源码用公开NH20序列对1 ms Prompt变号：

$$
c_{NH}(h,k)=NH[(19+k-h)\bmod20]
$$

然后对擦除后的$I/Q$分别累加并在候选边界闭合时形成功率。搜索结束后比较峰值与相隔半个周期的谷值；B1I跳过GPS使用的峰周围三角形限制。首次搜索通过后进入确认，确认阶段同样只使用中心频点。

通用位同步函数确实支持多个频率候选，也能在搜索成功后把候选频差一次性写入NCO；但只有`freq_bin_num>1`时才执行这一步。B1I初始化为1，因此这段多频率补偿不是B1I主路径。

## 4. 源码中的阶段顺序

芯片意图可以概括为：

```text
捕获已给位同步
    -> 直接进入位同步后的正常跟踪

捕获未给位同步，B1I
    -> 相邻1 ms Prompt鉴频 + 逐毫秒DLL
    -> 按ShortFast/LongFast周期更新载波与码NCO
    -> 快速牵引阶段完成
    -> 只在中心频点搜索20种NH20相位/边界
    -> 独立确认
    -> 位同步后的正常跟踪
```

状态切换函数允许ShortFast在尚未位同步时完成一次状态推进，随后位同步处理才开放；其他快速状态原则上要等待位同步完成才能继续向后切换。

## 5. 与当前StarTrack实验方案的区别

当前失败方案采用的是：B1I Prompt复数平方、10 ms短块、2560 ms观察窗、160 ms滑动更新、11个频率候选，每条有效结果立即进入FLL。相邻观察共享绝大多数数据，错误频率峰会在反馈后继续污染下一窗口。

芯片源码没有这条结构。它的B1I路径是“1 ms相邻鉴频闭环 + 单频点NH位同步”，没有平方Prompt的2560 ms滑动DFT，也没有11个长窗候选连续驱动FLL。

历史StarTrack的540/540方案则是另一种已经实测成功的工程方案：位同步阶段联合搜索11个频率候选和20种NH相位，确认后一次性校频。它和芯片B1I主路径也不相同，但至少有完整真实IQ证据。不能再把“历史11×20联合搜索”说成芯片B1I原样实现。

## 6. 当前可以下的结论

已验证事实：

- 芯片B1I无位同步移交时选择1 ms预同步环路；
- 芯片把B1I位同步频率候选数设为1；
- 预同步载波鉴频使用相邻1 ms Prompt的叉积，码环也在同一路径工作；
- 位同步枚举NH20相位，通用多频率代码在B1I主配置下不启用；
- 芯片没有当前2560 ms高重叠DFT反馈结构。

待确认问题：

- 现有文件是从图片重建并经过修订的源码。B1I“未提供位同步且非强捕获”的初始状态在可见调度条件中存在边界矛盾：初始化可能把状态改为LongFast，而位同步开放条件又排除了LongFast。独立算法可以确认，但这一特殊入口不能只凭该重建文件逐行证明；
- 芯片的叉积鉴频在StarTrack 24 dB-Hz、频差$\pm125$ Hz和全部NH相位下是否满足当前指标，仍需用同一真实IQ矩阵验证，不能仅因芯片使用就直接认定适合当前前端和目标。

因此下一步若按芯片思路整改，最小A/B不是继续调整11点长滑窗，而是建立“1 ms叉积鉴频 + 单频点NH位同步”候选，与历史540/540联合搜索方案在同一批B1I真实IQ上比较。验证前不修改正式算法结论。

## 源码位置

- 初始化与入口选择：`/home/showskills/SN770/GNSS/Receiver/track_full_rebuilt.c:519`
- ShortFast未同步时可推进一次：同文件`3852`
- 1 ms预同步载波/码环：同文件`4793`
- NH20相位与通用频率候选：同文件`4935`
- 位同步成功后的可选频率补偿：同文件`5649`
- 位同步开放条件：同文件`6019`
- 位同步前跟踪调度：同文件`7581`
- 本次核对源码SHA-256：`f97b37fd2d0af5c2bfcbaf20e832ace079c5b9c3eba41484c306a30bd9e7426e`
