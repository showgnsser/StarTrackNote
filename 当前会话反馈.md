# 当前会话反馈

你感觉没错：我刚才直接跳进了“实现并测试候选方案”，没有先把你提出的7个问题逐条回答清楚，交流顺序错了。

当前没有任何StarGen、StarTrack或CTest后台进程在运行，也没有另一个任务偷偷占用本会话。刚才只运行了一组不到1秒的定向CTest，执行完便结束了。

我刚才实际做的是：根据你提出的“去掉前置长窗DFT后，位同步前仍必须能独立锁住载波环”的要求，在`/tmp/startrack-data-presync-fll-src`建立了一个临时候选，而不是直接改正式工作区。候选保留位同步前160 ms载波FLL和DLL，只删除1.28/2.56 s长窗DFT；五频点仍只用于位同步搜索。

定向测试发现，直接复用现有逐对`asin`再平均的160 ms Prompt FLL并不可靠：一个L1 C/A确定性移交角在20秒时仍残留约5.66 Hz，因此该候选尚未进入真实IQ大批量测试，也没有冻结或提交。这个结果恰好支持了你的担心：不能简单删除长窗DFT后让位同步前没有可靠载波反馈。

当前正式源码工作区原本就有大量未提交修改；本轮没有把临时候选合入正式工作区，也没有启动长时间后台任务。

接下来正确的交流顺序应是：先逐条回答“载波NCO和码NCO在位同步前究竟由谁更新、三种信号能否统一、需要验证哪些场景”；你确认方案后，我再继续实现和测试。当前先暂停实验。
