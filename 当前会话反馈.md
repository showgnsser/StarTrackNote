可以相加，关键是**先统一成37 bit有符号数，再相加**。[第6.5节](/home/showskills/SN770/Note/StarTrackNote/09-逐模块定点化/18-跟踪逻辑定点化设计方案.md:875)规定了37 bit中间量，但没有展开补位步骤。

| 操作数 | 相加前的处理 |
|---|---|
| 无符号的 $K_{code,0}$ | 原36 bit前补一个0，变成37 bit有符号正数 |
| 有符号的 $\Delta K_{code}$ | 符号扩展到37 bit：正数高位补0，负数高位补1 |

随后使用普通二进制加法器相加。负改正量采用二补码表示，相加自然实现减小码步进。例如 $200+(-3)=197$，两种扩展都不改变原数值。

相加后检查：

$$
0<K_{code,init}<2^{36}
$$

满足范围时，取低36 bit装入uQ0.36码步进；不满足则不能装载。

这里的 $\Delta K_{code}$ 已经是码步进原始整数的改正量，与 $K_{code,0}$ 使用相同的LSB，**不需要再移位对齐**。37 bit仅是加法中间宽度，不增加码NCO的存储位宽或精度。
