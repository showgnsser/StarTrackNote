---
title: StarTrack跟踪算法完整设计方案
aliases:
  - StarTrack跟踪算法方案
  - StarTrack跟踪算法教材
tags:
  - GNSS
  - 跟踪环路
  - BOC
  - 状态机
  - 算法设计
status: 当前实现
version: 2.0
date: 2026-08-25
---

# StarTrack跟踪算法完整设计方案

> [!abstract] 这份文档是写给谁的
> 本文假定读者只知道“GNSS接收机用卫星信号定位”，不假定读者懂相关器、NCO、
> FLL、PLL、DLL、BOC、ASPeCT、slew或FineCheck。阅读本文不需要先看代码。
> 文中第一次出现的术语都会先解释“它是什么、为什么需要、输入是什么、输出是什么”，
> 然后才说明StarTrack如何实现。

> [!important] 本文的两个层次
> 第1章至第14章是一套完整的StarTrack跟踪算法教程；第15章以后用真实随机场景
> 暴露的三个问题及整改方案。不要跳过前面的概念直接阅读整改章节，否则很容易把
> “同步标志”“跟踪稳定”“状态名称”和“输入动态”混为一谈。

> [!note] 实现来源与声明边界
> 本书依据 `/home/showskills/SN770/Project/StarTrack` 的
> `codex/eight-signal-evolution` 分支在2026-08-25的当前实现编写。它解释算法原理、模块
> 职责和实现逻辑，不把设计说明自动写成八信号资格已经完成。

---

## 0. 阅读地图：先知道自己正在看哪一层

整个跟踪器可以先想成一个人在黑暗中追着一辆车跑：

1. **相关器**像眼睛，每1 ms看一次“车大概在左边、正中还是右边”；
2. **观察器**把许多次模糊的观察合起来，估计频率误差、相位误差或码误差；
3. **环路控制器**决定下一步把本地载波和本地码调快还是调慢；
4. **同步器**确认导航bit或导频二次码的边界；
5. **状态机**根据强弱、动态、同步和健康证据，选择用短观察还是长观察；
6. **runner与analyzer**负责构造实验和判分，它们不是跟踪算法本身。

本文按这个顺序由浅入深展开：

| 阅读阶段 | 解决的问题 | 对应章节 |
|---|---|---|
| 建立直觉 | 跟踪为什么是闭环，而不是重复捕获 | 第1章 |
| 学会语言 | PDI、NCO、P/E/L、ready/valid/used分别是什么 | 第2章 |
| 看懂全局 | 一批I/Q数据如何变成下一条硬件命令 | 第3～4章 |
| 看懂控制 | FLL、PLL、DLL如何估误差和调NCO | 第5～8章 |
| 看懂BOC | 为什么B1C/E1需要ASPeCT、slew和FineCheck | 第9～11章 |
| 看懂策略 | 11个状态为什么存在，谁负责什么 | 第12～14章 |
| 看懂整改 | outage、B1C弱动态、状态职责为什么出错 | 第15～19章 |

![[90-附件/图RT-01_阅读地图.svg]]

---

## 1. 跟踪究竟在做什么

### 1.1 捕获只给出“附近”，跟踪负责一直对准

卫星信号到达接收机时，接收机并不知道两个最重要的量：

- 载波频率因为卫星与接收机相对运动产生了多普勒偏移；
- 卫星伪随机码在本地时间轴上的起始位置未知。

捕获模块会在一个二维网格上搜索“频率格”和“码相位格”，得到一个粗略结果。例如：

- 捕获说载波大约在 `+4500 Hz`；
- 捕获说本地码大约应放在 `0.35 chip`；
- 但真实值可能还差几十赫兹和零点几chip。

捕获不能只做一次后就结束，因为卫星、接收机振荡器和传播环境都在变化。跟踪器必须在
每个新时刻重新回答两个问题：

1. 本地载波现在比信号快还是慢？
2. 本地码现在比信号早还是晚？

然后通过反馈不断修改本地NCO，使误差维持在零附近。

### 1.2 两条主闭环

StarTrack内部有两条主控制链：

#### 载波链

$$
\text{相关结果}
\rightarrow
\text{频率/相位观察}
\rightarrow
\text{FLL/PLL}
\rightarrow
\text{载波NCO}
$$

它的目标是让本地载波的频率和相位跟随接收信号。

#### 码链

$$
\text{Early/Prompt/Late相关结果}
\rightarrow
\text{码误差观察}
\rightarrow
\text{DLL}
\rightarrow
\text{码NCO}
$$

它的目标是让本地伪随机码的Prompt抽头停在正确相关峰中心。

两条链不是完全独立。载波多普勒也能预测码率变化，因此StarTrack先用载波多普勒承担
主要码率漂移，DLL只修正剩余码误差。这叫**载波辅助码环**。

### 1.3 闭环稳定不等于某一列日志为true

“跟踪稳定”至少同时包含：

- 频率误差没有持续发散；
- 码误差仍围绕正确主峰，而不是错误旁峰；
- PDI时间轴连续，硬件确实执行了软件上一条命令；
- 同步边界没有丢失；
- 状态切换合法，没有锁后重新掉入入口牵引；
- 在要求精度时，Doppler和码误差满足数值门限。

因此 `sync=1` 只表示同步器保存着一个已确认的边界，不能单独证明当前仍跟着正确峰。
本轮B1C问题的关键正是：同步标志还在，但频率和码已经严重偏离。

---

## 2. 先把所有基础术语讲清楚

### 2.1 I/Q复信号

接收机把射频信号下变频后表示成同相分量I和正交分量Q：

$$
z = I + jQ
$$

其中 $j$ 是虚数单位。I/Q让接收机同时保存幅度和相位。后面的载波频率观察、相位观察、
BOC/PRN相关都建立在复数相关结果上。

### 2.2 数据分量和导频分量

现代GNSS信号常把总功率分给两路：

- **数据分量**携带导航电文，符号可能未知并发生翻转；
- **导频分量**不承载导航数据，但会叠加一段公开二次码，便于弱信号长时间积累。

StarTrack对L5、B2A、E5A、E1和B1C默认使用导频驱动闭环，称为 **pilot-only，单导频
闭环**。这不表示硬件没有相关数据分量，而是数据相关只做诊断或解调，不参与FLL/PLL/DLL
控制。L1CA、B1I和G1没有独立导频，默认由数据分量闭环。

### 2.3 chip、码相位和码率

GNSS伪随机码由一串 $+1/-1$ 码片组成，一个码片称为一个 **chip**。

- **码相位**：本地码当前位于一个主码周期的什么位置，单位chip；
- **码率**：每秒推进多少chip，单位chip/s；
- **码误差**：本地码相位与真实信号码相位之差，单位chip。

例如B1C主码长度为10230 chip，周期10 ms，标称码率为：

$$
R_c = \frac{10230}{0.010}=1{,}023{,}000\ \mathrm{chip/s}
$$

码相位是周期量。`-0.3515625 chip`在命令接口中不能直接写成负数，而应回绕为：

$$
10230-0.3515625=10229.6484375\ \mathrm{chip}
$$

### 2.4 NCO是什么

NCO是 **Numerically Controlled Oscillator，数控振荡器**。它不是一个只会产生正弦波的
抽象名词，而是跟踪闭环真正写给硬件的“下一步怎么走”：

- 载波NCO决定本地载波每个样点转多少相位；
- 码NCO决定本地码每个样点推进多少码相位。

StarTrack的软件环路不直接改历史样本，也不突然改变已经发生的相位。它在处理完当前
PDI后生成一条新命令，该命令从下一条1 ms边界生效。

### 2.5 PDI是什么

PDI在本项目里可理解为 **每1 ms发布一次的相关积分记录**。硬件层完成：

1. 用载波NCO把输入信号下变频；
2. 用本地码NCO生成本地码；
3. 对多个码抽头做1 ms复相关；
4. 把相关值、样点位置、命令号、码段号等写入 `HwPdiRecord`。

软件看不到原始I/Q，只能消费PDI。这是明确的软硬件边界，不是性能优化细节。

### 2.6 Prompt、Early、Late、Very Early、Very Late

相关器不会只看一个本地码位置，而会同时看五个位置：

| 缩写 | 中文 | 相对Prompt的位置 | 用途 |
|---|---|---:|---|
| VE | Very Early | 更早 | 判断较大的码偏差 |
| E | Early | 早 | 形成局部码鉴别器 |
| P | Prompt | 中心 | 载波、同步和功率主输入 |
| L | Late | 晚 | 形成局部码鉴别器 |
| VL | Very Late | 更晚 | 判断较大的码偏差 |

如果Prompt落在正确主峰中心，Early和Late应大致对称；如果一侧明显更强，说明本地码偏离
中心。图中横轴不是时间，而是本地码相对于接收信号的码相位偏差。

![[90-附件/图RT-02_五抽头相关器.svg]]

### 2.7 观察器、环路和状态机不是一回事

这是理解代码最重要的分界：

| 层次 | 它回答的问题 | 例子 | 能否直接改NCO |
|---|---|---|---|
| 观察器 | “我测到了多少误差？” | `FreqObs`、`CodeObs` | 不能 |
| 环路 | “根据误差，下一步调多少？” | FLL、PLL、DLL | 能，产生控制量 |
| 监测器 | “最近一段时间是否稳定？” | `DllMonitor`、频率监测 | 不能 |
| 状态机 | “现在该用哪套观察和环路参数？” | 11状态FSM | 间接，通过切策略 |

观察器可以有结果但环路不采用；状态机也可以读取监测证据，却不能自己重新计算鉴别公式。

### 2.8 `ready`、`valid`和`used`

这三个词经常被混淆：

- `ready=true`：这个观察窗已经积满，到了应该给出结果的时刻；
- `valid=true`：结果还通过了有限值、功率、峰值唯一性等质量检查；
- `used=true`：环路在这个历元确实把该观察用于更新控制器。

一个典型过程是：

```text
窗口未满       ready=0 valid=0 used=0
窗口满但噪声大 ready=1 valid=0 used=0
窗口满且可信   ready=1 valid=1 used=1
可信但策略保持 ready=1 valid=1 used=0
```

所谓**频率观察使用率**，就是计分窗口内 `freq_used=true` 的历元占应更新历元的比例。它
检查的是“控制器是否持续获得并采用新频率证据”，不是芯片算法里某个独立物理量。

日志和门限里还常见两个统计量：

- **RMS**是均方根，先把误差平方、求平均、再开方；它对持续波动和大误差都敏感；
- **P95**是绝对误差的95百分位，表示95%的计分样本不超过这个数，用来观察尾部误差。

### 2.9 FLL、PLL和DLL

- **FLL**：Frequency Locked Loop，频率锁定环；根据相邻相关块的相位变化估频差；
- **PLL**：Phase Locked Loop，相位锁定环；根据Prompt相位估相位差；
- **DLL**：Delay Locked Loop，延迟锁定环；根据Early/Late不平衡估码相位差。

弱信号时相位容易随机翻转，PLL比FLL脆弱。因此StarTrack的弱、深弱和极弱辅助状态只
允许频率控制，PLL观察即使存在也不能注入载波控制。

FLL和DLL内部都包含比例或积分记忆。**PI**是Proportional-Integral，比例-积分控制：比例项
对当前误差快速响应，积分项记住持续偏差并消除稳态残差。只有物理语义正确、位于局部
单调域的误差才能进入PI。

### 2.10 DFT和FMS是什么

**DFT**是Discrete Fourier Transform，离散傅里叶变换。跟踪器在若干候选频率上计算相关
功率，哪个频点最强，就把它当成粗频率候选。它适合“在一段范围内找峰”。

**FMS**是项目和芯片代码沿用的固定频率支路鉴频名称。本文不依赖缩写展开，只按实际算法
解释：在当前NCO左、右两个固定频率假设上计算功率，通过两侧功率差反演局部残余频差。
它适合“已经知道峰在中心附近后做细修”，不是全带搜索。

两者的区别可以类比为：DFT先在一排停车位里找车，FMS只在当前停车位左、右各看一眼，
判断车是否停正。车已经跑到别的停车位时，局部FMS可能仍输出一个小量，因此必须限定
无模糊范围和使用阶段。

### 2.11 什么是 `slew`

`slew`在本文中译作**有限码相位斜坡**。它不是“立刻把码相位跳0.1 chip”，而是：

1. 先确认码应该向左还是向右移动；
2. 在一段固定时间内给码率增加一个临时偏置；
3. 积分后总共移动指定chip；
4. 时间到后发送零偏置停止命令；
5. 只有硬件PDI确认停止命令已经生效，才允许开始下一轮观察。

例如B1C一次要移动 $0.1$ chip，持续 $160$ ms，则额外码率为：

$$
R_{\mathrm{slew}}
=\frac{0.1}{0.160}
=0.625\ \mathrm{chip/s}
$$

持续160 ms后总位移正好是：

$$
0.625\times0.160=0.1\ \mathrm{chip}
$$

这样做比直接装载码相位安全，因为相关器、样点计数和命令时间轴保持连续。

### 2.12 什么是 `FineCheck`

`FineCheck`译作**细码复核阶段**。它发生在粗牵引认为“已经靠近中心”之后、允许导频同步
之前。它的职责不是继续粗拉，也不是立即让DLL闭环，而是回答：

> 刚才的粗方向判断真的把Prompt送进中央主峰了吗，还是只是停在一个看起来像中心的
> BOC旁峰附近？

以B1C为例：

1. 粗牵引完成三次同方向的0.1 chip有限移动；
2. 从停止命令确认后的下一个完整10 ms主码开始，切换到局部ASPeCT细码观察；
3. 累积三次160 ms细码观察，`DllMonitor`覆盖约480 ms；
4. 只有细码误差均值和RMS都不超过0.10 chip，才宣布“码已居中”；
5. 在此之前，DLL比例积分控制不接管，码NCO只保留载波辅助和已经完成的有限移动。

因此FineCheck本质上是一个**安全门**，不是一种新的环路。

![[90-附件/图RT-03_slew与FineCheck.svg]]

---

## 3. StarTrack的总数据流

### 3.1 从I/Q到下一条命令

![[90-附件/图RT-04_总体数据流.svg]]

一次完整处理按以下顺序发生：

1. `StarGen`生成测试I/Q，或真实前端提供量化I/Q；
2. `TrackRunner`丢弃捕获移交点以前的样点，然后启动跟踪闭环；
3. `HardwareChannel`用当前命令完成混频、本地码生成和1 ms相关；
4. `HwRegisterBank`发布 `HwPdiRecord`；
5. `PdiDecoder`把定点相关值解码为软件使用的 `PdiData`；
6. `LoopPdiSelector`选择当前跟踪分量，`PilotWiper`在已同步后擦除导频二次码；
7. 各观察器形成频率、相位、码、同步和C/N0证据；
8. `CarrierLoop`和`CodeLoop`更新控制量；
9. `TrackFsm`选择是否需要更换状态策略；
10. `TrackEngine::makeCommand()`生成下一1 ms边界使用的硬件命令；
11. logger写出track、pdi和events三类证据，analyzer离线判分。

### 3.2 1 ms硬件PDI与20/160/320 ms软件窗口

硬件永远每1 ms交付一次PDI；软件可以按不同状态积累多个PDI：

| 名称 | 含义 | 典型值 |
|---|---|---|
| `coherent_ms` | 复数相关先连续相加多久 | 1、20、100、120 ms |
| `carrier_obs_ms` | 一个频率观察覆盖多久 | 20、160、300、320、600 ms |
| `carrier_update_ms` | 多久允许载波环更新一次 | 20～600 ms |
| `code_obs_ms` | 一个码观察覆盖多久 | 20～600 ms |
| `code_update_ms` | 多久允许DLL更新一次 | 不超过600 ms |

日志仍可每20 ms输出一次，但日志周期不能反推软硬件交互周期。例如320 ms观察成熟前会有
许多20 ms日志行，这些行只是“窗口还没积满”，不是环路丢失了16次更新。

### 3.3 `processPdi()`的六个生产阶段

当前 `TrackEngine::processPdi()`严格按六步处理：

1. **确认时间轴**：检查epoch、gap、overflow、late和上一条命令号；
2. **解码并选分量**：把硬件s24（有符号24位）相关值转换成软件PDI，选择loop component；
3. **形成观察**：频率、码、相位、同步、C/N0各自积窗；
4. **更新环路**：观察重定时后更新载波环和载波辅助码环；
5. **更新状态机**：只用独立监测证据选择新策略；
6. **生成下一命令**：命令的 `apply_epoch` 是当前epoch加1。

这个顺序解释了为什么“当前PDI发现应当slew”时，slew不能在当前PDI已经发生；它最早从
下一条1 ms边界开始。

### 3.4 八种信号怎样进入同一套跟踪核心

八种信号共用 `TrackEngine`、载波环、码环和FSM，但物理目录不同。目录负责告诉公共算法：

- 主码多长、多少个1 ms segment；
- 默认闭环使用数据还是导频；
- 是否有公开二次码；
- 是BPSK还是BOC族调制；
- 哪些分量功率真正经过固定前端。

| 信号 | 默认闭环分量 | 主码周期 | 公开覆盖码/二次码 | 码观察特点 |
|---|---|---:|---|---|
| GPS L1CA | 数据 | 1 ms | 无固定二次码，20 ms数据bit | 普通BPSK Early-Late |
| BDS B1I D1 | 数据 | 1 ms | NH20 | 位前覆盖码处理后普通DLL |
| GLONASS G1 | 数据 | 1 ms | 20 ms meander | 普通DLL，载频另含FDMA频道 |
| GPS L5 | L5Q导频 | 1 ms | 20 chip导频二次码 | 单导频闭环 |
| BDS B2A | B2a-P导频 | 1 ms | 100 chip导频二次码 | 单导频闭环 |
| Galileo E5A | E5a-Q导频 | 1 ms | 100 chip导频二次码 | 单导频闭环 |
| Galileo E1 | E1C导频 | 4 ms | 25 chip导频二次码 | BOC/PRN双副本ASPeCT |
| BDS B1C | B1C-P导频 | 10 ms | 1800 chip导频二次码 | BOC/PRN双副本、wide FLL、ASPeCT |

“单导频闭环”表示载波环和码环只由导频分量驱动；数据分量可以做只读诊断或后续解调，
但不能在没有独立资格证据时偷偷加入控制。

NH20和meander都是叠加在主码上的公开或固定 $+1/-1$ 覆盖序列。它们会翻转相关符号，
所以软件必须在正确边界处理，不能把符号翻转误判成载波相位突然跳变。

B1C-P占复合发射功率的 $3/4$，固定窄带前端保留该导频分量的 $29/33$，所以相对复合输入
的可用功率比例为：

$$
\frac{3}{4}\times\frac{29}{33}
=\frac{29}{44}
$$

这个比例只用于物理功率和测试真值口径，不能在环路里再乘一次补偿。

---

## 4. 捕获移交与时间轴

### 4.1 什么叫捕获移交

捕获模块交给跟踪器的最小物理量是：

- 信号和PRN；
- `start_sample`：从输入流哪个样点开始跟踪；
- `doppler_hz`：该起点的物理载波多普勒估计；
- `code_phase_chips`：该起点的本地码绝对相位。

跟踪器不应读取捕获内部的真值、参考样点或搜索阶段临时字段来“修正”移交。

### 4.2 为什么非零 `start_sample` 容易写错

TrackRunner会丢弃 `start_sample` 之前的输入，但跟踪闭环没有在这段时间偷偷运行。因此
移交量必须描述**首个真正被跟踪的样点**，而不是场景时间0。

令：

- $F_s$ 为采样率；
- $S_0$ 为 `start_sample`；
- $T_0=S_0/F_s$ 为跟踪入口的绝对时间；
- $f_0$ 为场景初始频率；
- $d(t)$ 为线性rate、jerk、正弦动态和TCXO合成的动态项；
- $e_f$ 为希望注入的捕获残余频差。

则移交频率应满足：

$$
f_{\mathrm{handoff}}
=\operatorname{round}\!\left(f_0+d(T_0)+e_f\right)
$$

场景初始频率反解为：

$$
f_0=f_{\mathrm{handoff}}-e_f-d(T_0)
$$

码相位也必须积分从0到 $T_0$ 的码率变化，不能只修频率。

这里的rate是频率一阶变化率，单位Hz/s；jerk在本测试体系中表示rate继续随时间变化的项；
正弦动态模拟周期性运动；TCXO是温补晶振误差模型，模拟接收机本振缓慢漂移和随机变化。

![[90-附件/图RT-05_绝对时间与移交.svg]]

### 4.3 为什么码相位要用Q16整数闭环

主码相位接近周期末端时，十进制格式化可能把合法值舍入成恰好等于码长。例如：

```text
真实合法值  10229.99999999 chip
保留8位后  10230.00000000 chip
CLI判断     phase >= code_length，拒绝
```

正确做法是先在整数Q16域回绕：

$$
P_{16}=N_{\mathrm{code}}\times65536
$$

$$
\phi_{16}
=\operatorname{round}(\phi_{\mathrm{chip}}\times65536)
\bmod P_{16}
$$

最后才转换成十进制文本，并反算检查仍为同一个Q16字。

---

## 5. 相关器：跟踪器每1 ms真正看到了什么

### 5.1 相关的直觉

把接收码和本地码相乘后积分：

- 两者对齐时，大部分chip同号，积分绝对值大；
- 两者错开时，正负项互相抵消，积分绝对值小。

相关结果仍是复数，因为本地载波也可能有残余相位差：

$$
R_k=I_k+jQ_k
$$

其中下标 $k$ 可以是VE、E、P、L或VL。

### 5.2 BPSK与BOC的区别

BPSK类信号的自相关主峰近似单峰，Prompt附近Early/Late差值可以直接判断方向。

BOC信号在一个chip内部还乘了副载波，相关曲线出现多个峰和零点。捕获使用BPSK-like
近似可以快速找到附近位置，但跟踪不能把BPSK-like峰直接当成唯一BOC主峰。否则可能在
旁峰上得到“局部误差接近零”的假象。

### 5.3 为什么E1和B1C保留两套副本

硬件对E1/B1C同时形成：

- **BOC副本**：包含实际BOC(1,1)副载波；
- **PRN-only副本**：只保留PRN码，不乘BOC副载波。

两套副本都包含P/E/L/VE/VL五抽头。ASPeCT利用它们的不同旁峰结构，构造修改相关功率，
抵消BOC旁峰。

![[90-附件/图RT-06_BOC双副本.svg]]

### 5.4 完整主码边界为什么重要

E1主码由4个1 ms段组成，B1C主码由10个1 ms段组成。跨段相干前必须验证：

- segment顺序连续；
- code_period没有跳变；
- PDI epoch连续；
- 命令号是预期值；
- 没有gap、overflow或late。

任一条件不满足，观察窗必须清空。否则一个“看起来积满160 ms”的结果可能实际拼接了
两个不连续的本地码或两套不同NCO命令。

---

## 6. 载波观察：怎样从相关值估频差

### 6.1 最基本的相邻块鉴频

设相邻两个复相关块为 $Z_{k-1}$ 和 $Z_k$，常用共轭积为：

$$
C_k=Z_{k-1}^{*}Z_k
$$

它的相角就是两个相关块之间的相位增量。若块中心间隔为 $T$：

$$
\widehat{\Delta f}
=\frac{\operatorname{atan2}(\Im C_k,\Re C_k)}{2\pi T}
$$

未知数据bit可能让相关值翻号。二倍角或平方FLL先把相关值平方，频率换算分母相应多一个2：

$$
\widehat{\Delta f}_{\mathrm{sq}}
=\frac{\operatorname{atan2}(\Im C_k,\Re C_k)}{4\pi T}
$$

### 6.2 为什么还需要多频点观察

当残余频差超过单个鉴频器的无模糊范围时，只看相邻块相角会把真实频率绕回错误区间。
因此不同状态会使用：

- `CrossDot`：快速相邻PDI叉点积；
- `BinPower`：围绕当前NCO建立多个频率假设，比较同频点积累功率；
- `FMS`：在已稳定NCO附近做较窄的局部频率搜索；
- `DFT/FMS`：入口先粗搜，再在主频点内精化；
- `CodePeriodFll`：现代长主码导频按完整主码块鉴频。

### 6.3 B1C预同步为什么使用五抽头宽FLL

B1C捕获码误差可能达到约 $\pm90/256=\pm0.3515625$ chip。这个位置的Prompt相关可能很弱，
只读Prompt会让载波观察失效。因此预同步FLL同时检查BOC副本的VE/E/P/L/VL，从有信号
能量的固定抽头统计中形成一个频率观察。

关键限制是：

- 五抽头只是提高码未居中时“看见载波”的机会；
- 最终仍只输出一个频率误差，控制同一载波NCO；
- 码slew期间暂停并清窗，不能跨码率换字拼接频率窗口；
- B1C锁定前保持wide source，码中心成立本身不切回Prompt；
- 真正同步锁定的边界才清wide窗口并进入正常状态观察。

当前正式开发配置使用10个相邻10 ms主码对，也就是11个完整主码块：

$$
T_{\mathrm{span}}=(10+1)\times10\ \mathrm{ms}=110\ \mathrm{ms}
$$

窗口每20 ms滑动发布一次。平方FLL的相位基线仍是相邻主码之间的10 ms，不是110 ms，
所以物理无模糊半范围为：

$$
f_{\mathrm{amb}}
=\frac{1}{4\times0.010}
=25\ \mathrm{Hz}
$$

最终观察要求五抽头联合平方复积集中度不低于0.65、存在性不低于0.14，并以
`source_tap=3`标识“五抽头组合”，不能在日志里冒充Prompt。110 ms是输入支持跨度，
20 ms是发布节拍，10 ms是鉴频相位基线；三种时间不能混成一个“积分周期”。

### 6.4 `physical_hz`与NCO参考

观察器输出的 `physical_hz` 是相对某个观察中心NCO的残余。若窗口内NCO发生正常换字，
不能把它误认为输入信号突然跳频。观察器必须保存或重定时每个块的NCO参考，使：

$$
f_{\mathrm{absolute}}
=f_{\mathrm{nco,ref}}+f_{\mathrm{physical}}
$$

在正常命令换字前后保持一致。

---

## 7. 载波环：FLL、PLL和预测怎样共同工作

### 7.1 FLL解决什么

FLL输入频率残差 $e_f$，输出对载波NCO频率的修正。二阶FLL不仅看当前误差，还维护频率
变化趋势，因此能跟随一定的Hz/s动态。

直觉上：

- 比例项快速纠正当前偏差；
- 趋势项估计偏差正在以什么速度变化；
- 无新观察时只做预测，不把“没测到”当成“误差为零”。

当前三阶FLL把内部状态写成频率修正 $f$ 和一个更新周期内的趋势步长 $d$。设更新时间为
$T$，则物理变化率为：

$$
r=\frac{d}{T}\quad(\mathrm{Hz/s})
$$

在新的控制时刻到来时，先按真实经过时间 $\Delta t$ 预测：

$$
f\leftarrow f+r\Delta t
$$

收到有效频差 $e_f$ 后，再按“先趋势、后频率”的顺序校正。没有显式芯片系数时：

$$
\omega_n=\frac{B_{\mathrm{FLL}}}{0.53}
$$

$$
K_1=1.414\,\omega_nT
$$

$$
K_2=(\omega_nT)^2
$$

趋势支路允许更新时：

$$
d_{k}=d_{\mathrm{ref}}
+\lambda(d_{k-1}-d_{\mathrm{ref}})
+K_2e_f
$$

然后更新频率：

$$
f_k=f_{\mathrm{pred}}+(d_k-d_{k-1})+K_1e_f
$$

$\lambda$ 是趋势保留比例。某些局部FMS观察可以修当前频率，却没有资格更新高阶趋势；这由
`physical_valid`和状态的 `fms_trend`权限共同决定。

### 7.2 PLL解决什么

PLL输入Prompt相位误差，负责强信号下更精细的相位跟踪。但相位观察对噪声、bit翻转和
二次码擦除错误更敏感，所以只有 `ShortSlow` 的FLL辅助PLL策略允许相位控制注入。

弱信号状态即使日志里还能形成相位诊断，也必须由状态策略和控制器两层共同阻断PLL。

PLL输入相位的单位是cycle，不是弧度。令相位误差为 $e_\phi$，自然频率为：

$$
\omega_{n,\mathrm{PLL}}
=\frac{B_{\mathrm{PLL}}}{0.7845}
$$

当前三阶PLL修正的递推为：

$$
r_{\phi,k}
=r_{\phi,k-1}
+0.25T\omega_{n,\mathrm{PLL}}^3e_\phi
$$

$$
i_{\phi,k}
=i_{\phi,k-1}
+T\left(r_{\phi,k}
+0.55\omega_{n,\mathrm{PLL}}^2e_\phi\right)
$$

$$
f_{\mathrm{PLL}}
=i_{\phi,k}
+2.4\omega_{n,\mathrm{PLL}}e_\phi
$$

最终载波控制量为：

$$
f_{\mathrm{control}}
=f_{\mathrm{FLL\ base}}
+f_{\mathrm{PLL}}
$$

PLL显式失效而FLL仍有效时，当前PLL修正会被吸收到FLL基频并清除PLL积分；单纯“本历元
没有相位更新”只保持PLL状态，不等同失效。

### 7.3 为什么状态切换不应重置NCO

状态改变的是“以后怎样观察、环路带宽多大”，不是把物理载波重新放回0。切换时应保留：

- 当前载波NCO频率和相位；
- 当前码NCO相位和码率；
- 与新策略兼容的环路状态。

只清除因观察周期、抽头几何或符号假设改变而不兼容的窗口。否则状态切换本身就会制造
一次人为频率或码相位跳变。

---

## 8. 码观察与DLL

### 8.1 普通Early-Late鉴别

在单峰相关函数附近，可用Early和Late的功率差形成码误差：

$$
D_{EL}
=\frac{|E|^2-|L|^2}{|E|^2+|L|^2}
$$

具体实现还要结合抽头间距和归一化增益，把无量纲差值换成chip误差。它只在主峰附近
单调，离开吸引域后不能保证方向仍正确。

### 8.2 DLL输出不是完整码率

StarTrack的码率由三项组成：

$$
R_{\mathrm{code}}
=R_{\mathrm{nominal}}
+R_{\mathrm{carrier\ aid}}
+R_{\mathrm{DLL}}
$$

载波辅助项为：

$$
R_{\mathrm{carrier\ aid}}
=f_D\frac{R_c}{f_L}
$$

其中：

- $f_D$：当前物理载波多普勒；
- $R_c$：标称码率；
- $f_L$：标称载波频率。

DLL只估剩余误差，避免把卫星运动造成的主要码率变化全部交给噪声更大的Early/Late差值。

对一个有效细码误差 $e_c$，当前二阶增量PI为：

$$
I_k=\lambda_I I_{k-1}+K_Ie_c
$$

$$
R_{\mathrm{DLL},k}=I_k+K_Pe_c
$$

$\lambda_I$ 是旧积分保留比例，普通状态为1；极弱状态可以小于1以抑制旧积分噪声随机
游走。观察未成熟或无效时，代码保持完整旧DLL输出，不注入“零误差”，也不把积分随意
衰减。

### 8.3 为什么粗码方向不能进入DLL PI

`CodeObs`带有 `kind`：

- `kCoarse`只表示“应该向左或向右移动一小步”；
- `kFine`才表示“这是局部单调域内、可作为连续误差的chip数”。

`CodeLoop::update()`只有收到 `kFine && ready && valid` 才更新比例积分状态。粗方向如果进入
PI，控制器会把一次“移动0.1 chip”的命令误解为持续的码率误差，缺测时继续保持，最终
产生多chip漂移。

### 8.4 DLL监测器为什么同时看均值和RMS

只看平均误差可能把正负大幅抖动抵消成0。`DllMonitor`因此在连续窗口内计算：

$$
\overline e_c
=\frac{\sum_i e_iT_i}{\sum_iT_i}
$$

$$
e_{\mathrm{RMS}}
=\sqrt{\frac{\sum_i e_i^2T_i}{\sum_iT_i}}
$$

其中 $e_i$ 是一次码观察，$T_i$ 是该观察覆盖的毫秒数。只有累计时长达到400 ms才发布
成熟状态。FineCheck要求均值和RMS同时位于中央吸引域。

---

## 9. ASPeCT：BOC为什么需要两套相关值

ASPeCT是本项目沿用的BOC自相关旁峰消除算法名称。初学者不需要先记住缩写，只要先记住
它的核心动作：**同一输入同时做BOC相关和PRN-only相关，再按固定公式组合两者，使正确
主峰保留、旁峰被削弱。** 它不是新的调制，也不是第三个环路。

### 9.1 BOC旁峰问题

BOC副载波把一个普通PRN主峰分裂成更复杂的相关形状。局部Early/Late鉴别器只能在正确
零点附近工作。如果捕获移交落在旁峰附近，它可能仍输出一个局部零误差，从而把旁峰误当
主峰。

### 9.2 式(13)：修改相关功率

StarTrack使用BOC副本和PRN-only副本构造：

$$
M(\tau)
=|R_{\mathrm{BOC}}(\tau)|^2
-\beta|R_{\mathrm{PRN}}(\tau)|^2
$$

其中：

- $\tau$：码相位偏差；
- $R_{\mathrm{BOC}}$：带BOC副载波的复相关；
- $R_{\mathrm{PRN}}$：PRN-only复相关；
- $\beta=2$：当前冻结系数；
- $M(\tau)$：用于压制旁峰的有符号修改功率。

“有符号”很重要：结果可以为负，不能取绝对值把负旁峰重新变成正峰。

### 9.3 式(14)：局部细码误差

先分别形成BOC和PRN-only的点积差：

$$
D_{\mathrm{BOC}}
=\Re\left\{(E_{\mathrm{BOC}}-L_{\mathrm{BOC}})P_{\mathrm{BOC}}^*\right\}
$$

$$
D_{\mathrm{PRN}}
=\Re\left\{(E_{\mathrm{PRN}}-L_{\mathrm{PRN}})P_{\mathrm{PRN}}^*\right\}
$$

再组合为局部码误差：

$$
\widehat e_c
=\frac{D_{\mathrm{BOC}}-\beta D_{\mathrm{PRN}}}
{(6+\beta d)|P_{\mathrm{BOC}}|^2}
$$

其中 $d$ 是完整Early-Late间距，输出单位chip。该式适合中央主峰附近的连续DLL误差，
不能代替主峰搜索。

上式写的是 `AspectDisc` 内部鉴别量。生产 `AspectCodeObserver` 在形成公共 `CodeObs` 时会
按项目统一的“本地码相位修正”约定取一次负号：正的 `CodeObs.code_chips` 表示应增加本地
码相位，负值表示应减小。这样普通Early-Late、ASPeCT细观察和 `CodeLoop` 使用同一控制
符号；不能只照搬论文公式的正负号后直接写NCO。

### 9.4 粗牵引和细跟踪必须分开

| 阶段 | 使用的证据 | 输出语义 | 能否进入DLL |
|---|---|---|---|
| 宽峰粗牵引 | 五抽头方向组修改功率 | 左/中/右 | 否 |
| FineCheck | 局部ASPeCT式(14) | 连续chip误差 | 暂不控制，只监测 |
| 稳定细跟踪 | 局部ASPeCT式(14) | 连续chip误差 | 是 |

把这三个阶段混在一起，会出现两类典型错误：用局部鉴别器寻找远处主峰，或者把离散粗方向
塞进PI控制器。

---

## 10. BOC牵引：从捕获附近走到可同步主峰

### 10.1 为什么不能一上来就开DLL和PilotSync

对E1/B1C，捕获使用BPSK-like近似，移交码误差可能仍有零点几chip。此时有两个风险：

1. Prompt可能靠近BOC相关零点，载波观察只看Prompt会失效；
2. 局部ASPeCT式(14)可能已经离开单调域，误差符号不再代表真实方向。

如果此时直接让DLL积分，错误方向可能被积成持续码率；如果直接让PilotSync搜索，旁峰上也
可能积出看似稳定的二次码相位。正确顺序必须是“先把载波和码送入可信区域，再同步”。

### 10.2 B1C预同步的九个阶段

![[90-附件/图RT-07_B1C预同步流程.svg]]

下面逐阶段解释。图中的“允许”不是一个抽象状态名，而是对后续模块输入的硬门控。

#### 阶段A：从捕获移交开始

入口携带捕获得到的Doppler和码相位。硬件按B1C的10段主码、BOC/PRN-only双副本和五抽头
开始产生1 ms PDI。

此时：

- `PilotSync`关闭；
- 普通Prompt FLL不是首选；
- DLL PI清零，只保留载波辅助码率；
- AspectPull可以后台积累，但在宽FLL资格成立前不能发布控制事件。

#### 阶段B：五抽头wide FLL先证明载波可观测

`CodePeriodFllObs`跨完整10 ms主码形成B1C预同步频率观察。当前代码要求最终聚合观察：

- `ready=true`；
- `valid=true`；
- `physical_valid=true`；
- `source_tap=3`，表示五抽头组合源；
- 观察末历元恰好是当前PDI。

当前 `kB1cWideConfirms=1`，一次满足条件的最终聚合就锁存wide资格。资格成立只说明“载波
观察有物理可信度，可以开始码牵引”，不说明码已经居中。

资格成立的同一个历元会清除资格前积累的码窗，本历元旧粗码结果不能被消费。下一PDI才
开始一个全新的码牵引事务。

#### 阶段C：AspectPull形成粗方向

B1C每个粗观察覆盖160 ms，也就是16个完整10 ms主码。每个主码内部先复数相干，主码间
在修改功率域相加。

五抽头先压缩为三组：

$$
G_{\mathrm{left}}=\frac{M_{VE}+M_E}{2}
$$

$$
G_{\mathrm{center}}=M_P
$$

$$
G_{\mathrm{right}}=\frac{M_L+M_{VL}}{2}
$$

取最大组 $G_b$ 和次大组 $G_s$，计算方向对比度：

$$
\rho
=\frac{G_b-G_s}{|G_b|+|G_s|}
$$

当前B1C要求 $\rho\ge0.10$，并且两个互不重叠的160 ms decision给出同一方向，才发布一个
`kCoarse`观察。因此最早粗方向需要：

$$
2\times160=320\ \mathrm{ms}
$$

左组发布 $-0.1$ chip，中组发布0，右组发布 $+0.1$ chip。这个数只表示“一次有限移动的
方向和幅度”，不是连续DLL误差。

#### 阶段D：启动一次有限slew

如果粗结果不是0，`startCodeSlew()`：

1. 把幅度限制在内抽头间距 $\pm0.1$ chip；
2. 计算160 ms内应叠加的临时码率；
3. 从当前epoch加1开始生效；
4. 记录绝对结束epoch；
5. 清除粗码、细码、DLL监测和PilotSync历史；
6. 暂停wide FLL新窗，避免跨码率变化拼接。

载波环的已确认NCO和相位不重置。码率命令仍包含实时载波辅助。

#### 阶段E：按绝对时间停止，并等硬件确认

160 ms到期后，软件生成不再包含slew偏置的码命令。但“软件已经生成停止命令”还不等于
“硬件已经执行”。只有后续PDI中的 `cmd_id` 等于停止命令ID，事务才真正结束。

若停止确认PDI恰好有gap或命令不匹配，软件会继续发布新的零偏置命令并更新待确认ID，
不能永远等待一个已经错过的旧命令号。

#### 阶段F：三次同方向slew后才进入FineCheck

B1C一次0.1 chip移动可能不足以从 $0.35$ chip移交误差进入中心。当前代码记录每次已经完整
执行且确认停止的方向：

- 同方向计数加1；
- 反方向重新从1计数；
- 达到三次同方向后，才请求FineCheck；
- 第三次停止确认的当前PDI不入FineCheck，从下一历元、下一个完整segment 0起步。

这里的“三次”不是说不看中间新证据。每次slew后都会重新积累两个独立粗decision；只有
新证据仍指向同一方向，才会发生下一次移动。

#### 阶段G：FineCheck复核中央主峰

FineCheck改用 `AspectCodeObserver` 的式(14)细码误差，不再使用左/中/右离散方向。每次细码
观察覆盖160 ms，`DllMonitor`需要覆盖至少400 ms，因此通常三次观察后得到约480 ms成熟
证据。

通过条件是：

$$
|\overline e_c|\le0.10\ \mathrm{chip}
$$

且

$$
e_{\mathrm{RMS}}\le0.10\ \mathrm{chip}
$$

FineCheck期间若粗观察再次可靠地复现外峰，优先相信外峰证据并回到粗牵引，避免一个局部
小误差掩盖仍未进入主峰的事实。

#### 阶段H：打开码居中门，从下一PDI开始PilotSync

FineCheck通过时设置 `pilot_code_centered=true`，重置旧同步历史。由于本PDI进入
`SignalSync`时门还是关闭的，所以当前PDI不会偷跑进同步窗；下一PDI才开始新搜索。

#### 阶段I：PilotSync搜索并独立确认

B1C-P公开二次码长1800个主码周期，每周期10 ms，总长18 s。同步器必须：

1. 用完整18 s搜索频率与二次码相位；
2. 再用独立18 s窗口确认；
3. 锁定后由 `PilotWiper`擦除二次码；
4. 进入普通细码ASPeCT和状态载波观察；
5. 不再回退到预同步粗包络，除非发生明确失锁或重建。

这解释了为什么B1C首次同步的物理下限约为36 s。20 s测试不可能证明B1C完成了完整搜索与
独立确认。

### 10.3 E1与B1C的差异

两者都需要BOC双副本和ASPeCT，但不能把参数互相复制：

| 项目 | E1 ShortFast | E1 LongFast | B1C Fast入口 |
|---|---:|---:|---:|
| 主码周期 | 4 ms | 4 ms | 10 ms |
| 粗几何inner/outer | 0.25/0.50 chip | 0.25/0.50 chip | 0.10/0.20 chip |
| 单子窗 | 20 ms | 160 ms | 160 ms |
| 每decision子窗数 | 3 | 8 | 1 |
| 同方向decision数 | 2 | 2 | 2 |
| 最早粗事件 | 120 ms | 2560 ms | 320 ms |
| 对比度门 | 0.35 | 0.10 | 0.10 |
| 粗功率 | BOC包络 | BOC包络 | ASPeCT修改功率 |
| FineCheck中心门 | 0.20 chip | 0.20 chip | 0.10 chip |

E1在粗中心后还要把硬件抽头从0.25/0.50切换到稳定0.10/0.20，并等待硬件命令确认，然后
才建立FineCheck。E1码居中后还会冻结当前载波频率趋势，为PilotSync提供固定NCO参考。

---

## 11. 同步器：确认“符号边界在哪里”

### 11.1 数据bit同步与导频二次码同步

GNSS信号可能包含：

- 每20 ms等周期翻转的数据bit；
- NH码或公开二次码；
- 没有数据bit、但有长公开二次码的导频。

同步器要找的是这些符号边界。只有边界和符号已知，才能跨翻转点继续做长相干积分。

### 11.2 为什么“搜索”和“确认”必须独立

在噪声里搜索很多候选，总会有一个候选看起来最好。如果立刻把这个最大值当锁定，搜索
本身造成的选择偏差会产生假同步。StarTrack采用：

1. 搜索窗找候选；
2. 清除搜索统计；
3. 用不重叠的新窗确认同一候选；
4. 只有确认也通过才发布locked。

### 11.3 码未居中时为什么禁止PilotSync

码偏离主峰会改变Prompt幅度和相位关系。若同步器先在错误码位置锁定，`PilotWiper`会按
错误相位擦二次码，后续相干积分比未擦更坏。因此预同步粗码可以后台观察，但只有
FineCheck证明中央主峰后，PilotSync才获得输入。

### 11.4 PilotWiper做什么

锁定后，`PilotWiper`根据公开二次码相位给导频loop component的所有抽头和两个副本乘同一
$+1/-1$ 符号：

- BOC和PRN-only必须同时擦；
- P/E/L/VE/VL必须同时擦；
- 数据分量不能被误擦；
- 时间轴中断必须清除不能继续复用的相干窗。

擦除后才允许跨二次码chip进行真正的复数相干积累。

---

## 12. C/N0与“环路质量”必须分层

### 12.1 C/N0是什么

$C/N_0$是载波功率与噪声功率谱密度之比，通常用dB-Hz表示。它描述信号强弱，但不是跟踪
是否收敛的唯一证据。

### 12.2 状态控制量与物理显示量不是同一个东西

StarTrack当前有两类量：

- `LoopCn0`：为FSM和控制服务的芯片式环路质量量，强调稳定、及时和bit-exact；
- `physical/display C/N0`：尽量按物理功率定义估计，供报告和显示，不反馈控制。

在极弱B1C里，LoopCn0可能触发8/10 dB的有效哨兵，而物理估计可能因低SNR功率差出现长
负尾。前者不能冒充无偏物理测量，后者失败也不自动证明闭环跟踪失败。

### 12.3 FSM怎样使用C/N0

FSM把C/N0作为“应使用短窗还是长窗、是否需要辅助”的证据之一，同时还检查：

- PDI和命令是否健康；
- 同步是否锁定；
- 频率观察是否新鲜、稳定；
- DLL是否位于可交接范围；
- 辅助bit是否有效；
- 动态监测是否成熟。

不能用一个C/N0阈值替代所有这些证据。

---

## 13. 十一个状态：不要把状态名当成场景标签

### 13.1 为什么需要多个状态

观察时间存在基本权衡：

- 窗口短：响应快、动态能力强，但噪声大；
- 窗口长：积累能量多、适合弱信号，但响应慢、容易受动态相位弯曲影响。

状态机的任务就是在“快”和“稳”之间选择一套固定策略。状态名称描述**接收机此刻采用的
职责**，不是输入场景文件上的 `low/medium/high dynamic` 标签。

### 13.2 十一个状态逐一解释

| 状态 | 主要时标 | 载波方式 | 它真正负责什么 | 不负责什么 |
|---|---:|---|---|---|
| ShortFast | 20 ms | FLL | 强入口，快速把粗频和粗码拉近 | 长时间弱保持 |
| ShortNormal | 20 ms | FLL | 强入口初步收敛、等待同步 | 作为中动态最终态 |
| ShortSlow | 20 ms | FLL辅助PLL | 已同步强信号精跟踪 | 极弱信号能量积累 |
| LongFast | 160 ms | FLL | 普通移交牵引、同步前长统计入口 | 最终低动态稳态 |
| LongNormal | 160 ms | FLL | 同步后中弱信号过渡收敛 | 直接代表“normal场景” |
| LongSlow | 320 ms | FLL | 低动态、中弱信号自主稳态 | 最高动态补救 |
| Sensitivity | 320 ms | FLL-only | 自主极弱保持 | 用PLL追相位、高动态搜索 |
| BitAiding | 600 ms | FLL-only | 严格已知bit辅助的室内极弱跟踪 | 自主无辅助跟踪 |
| OutdoorBitAiding | 300 ms | FLL-only | 显式室外辅助入口及辅助失效降级 | 由动态标签自动进入 |
| MidDynamic | 320 ms | FLL | HighDynamic退出时的恢复台阶 | 普通“中动态”目标态 |
| HighDynamic | 320 ms | 较宽FLL | LongSlow残余动态超限后的补救 | 所有高动态输入必经状态 |

下面给出当前 `kTrackPolicies` 的完整主要时标和带宽。`相干×块数`说明先形成多长复相关，
再组合多少块；它与CSV日志周期无关。

| 状态 | 相干ms×块数 | 载波观察/更新ms | 码观察/更新ms | FLL带宽Hz | DLL带宽Hz | 载波模式 |
|---|---:|---:|---:|---:|---:|---|
| ShortFast | 1×20 | 20/20 | 20/20 | 3.0 | 2.0 | FLL |
| ShortNormal | 20×1 | 20/20 | 20/20 | 2.0 | 0.8 | FLL |
| ShortSlow | 20×1 | 20/20 | 20/20 | 1.0 | 0.1 | FLL+PLL |
| LongFast | 20×8 | 160/160 | 160/160 | 1.6 | 1.0 | FLL |
| LongNormal | 20×8 | 160/160 | 160/160 | 0.8 | 0.5 | FLL |
| LongSlow | 20×16 | 320/320 | 320/320 | 0.35 | 0.1 | FLL |
| Sensitivity | 20×16 | 320/320 | 600/320 | 0.2 | 0.15 | FLL-only |
| BitAiding | 120×5 | 600/600 | 600/600 | 0.05 | 0.025 | FLL-only，需辅助 |
| OutdoorBitAiding | 100×3 | 300/300 | 300/300 | 0.4 | 0.1 | FLL-only，需辅助 |
| MidDynamic | 20×16 | 320/320 | 320/320 | 0.5 | 0.1 | FLL |
| HighDynamic | 20×16 | 320/320 | 320/320 | 0.7 | 0.1 | FLL |

表中的带宽不是“越大越先进”。大带宽响应快但噪声大，小带宽降噪但动态能力低。状态机
不是寻找单一最优值，而是在不同物理职责之间选择已经冻结的一套折中。

四种PDI组织模式也要分清：

| 模式 | 作用 |
|---|---|
| `OneMs` | 直接使用连续1 ms PDI，适合入口快速牵引 |
| `Normal` | 按已确认bit/二次码边界形成正常相干块 |
| `HalfBit` | 把相干段限制在半个bit以内，降低未知翻转影响 |
| `ShortFms` | 用有限频率假设做粗频居中和局部精化 |

### 13.3 普通强弱链

![[90-附件/图RT-08_普通强弱状态链.svg]]

典型普通入口：

```text
LongFast → LongNormal → LongSlow → Sensitivity
```

典型强入口：

```text
ShortFast → ShortNormal → ShortSlow
```

信号变弱时，ShortSlow可转LongSlow，LongSlow经过独立低C/N0计数进入Sensitivity；信号恢复
后沿相反方向逐级恢复。状态切换需要候选连续确认和最小驻留，不因单次波动跳变。

### 13.4 动态补救链

![[90-附件/图RT-09_动态补救状态链.svg]]

动态分类的物理源职责是LongSlow。当前工程的入口条件为以下任一项：

$$
\frac{|\overline f|}{f_{\mathrm{RMS}}}>0.8
\quad\land\quad
f_{\mathrm{RMS}}>4.6875\ \mathrm{Hz}
$$

或者可信频率变化率在一个320 ms周期内超过4.6875 Hz：

$$
|\dot f|>
\frac{4.6875}{0.320}
=14.6484375\ \mathrm{Hz/s}
$$

进入HighDynamic后，残余动态下降时先到MidDynamic，再回LongSlow。MidDynamic因此是恢复台阶，
不是看到场景标签“medium”就应进入的状态。

实现里还有一条不改变物理职责的提交压缩：ShortSlow的低SNR证据已经要求交给LongSlow时，
如果同一批成熟动态证据又要求HighDynamic，软件可把
`ShortSlow → LongSlow → HighDynamic`折叠成一次pending交接。这样避免先切LongSlow、清窗后
丢失已经成熟的动态证据；它不表示ShortSlow直接读取场景真值选择HighDynamic。

### 13.5 显式辅助链

![[90-附件/图RT-10_辅助状态链.svg]]

BitAiding和OutdoorBitAiding必须有可信、逐PDI对齐的外部bit符号。没有外部bit的现代导频
单分量跟踪不应为了“看起来更灵敏”进入这两个状态。公开导频二次码自主同步不算外部辅助。

### 13.6 为什么之前50例终态不符不能直接判算法错

此前自然场景把输入 `dynamic=medium/high` 直接翻译成最终必须驻留MidDynamic或
HighDynamic。这个判据违背状态职责：

- 普通状态已经把残余误差压得很小时，不需要进入补救状态；
- 8个高动态耐久案例物理精度全部通过，说明普通状态已完成跟踪；
- 没有非法跳转或错误入口，只是最终状态名与场景标签不同。

正确测试应分别判断“物理是否稳定”和“状态职责是否正确”，而不是匹配字符串。

---

## 14. 一次完整跟踪历元的故事

为了把前面所有模块串起来，下面用一个普通BPSK导频历元说明：

1. 硬件用上一条命令处理新的1 ms输入，生成P/E/L等相关值；
2. PDI携带当前epoch和实际执行的cmd_id；
3. 软件先验证epoch连续、没有gap/overflow/late、cmd_id符合预期；
4. 选择导频分量；若二次码已锁定，擦除本PDI的公开符号；
5. 相干积分器把这1 ms加入当前20/160/320 ms窗口；
6. 若窗口未满，观察器返回ready=false，环路只保持或预测；
7. 若窗口已满且质量门通过，形成FreqObs/CodeObs；
8. 载波环把频差变成新的control_hz；
9. 码环用载波多普勒先算载波辅助，再按细码观察更新DLL残差；
10. 监测器更新最近频率RMS、变化率、DLL均值/RMS和C/N0；
11. FSM若看到持续证据，先建立候选状态，不立即切换；
12. 候选满足驻留和确认后，生成携带新策略的硬件命令；
13. 下一PDI回报该命令号后，状态切换才正式提交；
14. logger记录本历元事实，analyzer离线比较真值并判分。

这个故事也是后续定位问题的顺序：先查输入和命令时间轴，再查观察，再查环路，最后查状态。
如果一开始只看最终state或sync，很容易把脚本错误误诊成算法错误。

---

## 15. 测试系统：场景、runner、日志和判分

### 15.1 四个角色

| 角色 | 做什么 | 不能做什么 |
|---|---|---|
| StarGen | 根据场景生成I/Q和真值曲线 | 不替跟踪器修NCO |
| campaign runner | 展开case、写场景、启动管道、保存SHA | 不实现跟踪算法 |
| StarTrack | 只根据PDI闭环跟踪 | 不读取场景真值控制 |
| analyzer | 按同窗真值计算误差和门限 | 不反过来修改跟踪 |

### 15.2 三份核心日志

- `track.csv`：状态、观察、环路结果、同步、C/N0和命令摘要；
- `pdi.csv`：PDI时间轴、抽头、命令确认和底层诊断；
- `events.csv`：状态切换、同步边界、B1C wide/coarse/slew/FineCheck事件。

真实结论必须同时绑定场景、命令、原始CSV、result和summary的SHA。只保存“PASS”文本不能
证明使用了哪套代码、哪个输入或哪个分析器。

### 15.3 两类通过结论

本轮随机活动要求分开报告：

#### 稳定通过

- 管道正常结束；
- PDI和命令连续；
- 没有锁后重入入口牵引；
- 没有同步丢失；
- 没有持续发散或错误峰假稳定。

#### 精度通过

在指定计分窗内：

$$
\mathrm{Doppler\ RMS}\le5\ \mathrm{Hz}
$$

$$
\mathrm{Doppler\ P95}\le10\ \mathrm{Hz}
$$

$$
\mathrm{code\ P95}\le0.2\ \mathrm{chip}
$$

正式经典跟踪还要求同步、频率观察有效率和频率观察使用率不低于0.95，以及目标弱状态
驻留等场景专属门。

### 15.4 状态职责通过

状态职责门只检查：

- 入口状态是否符合移交类型；
- 跳转是否沿合法邻接关系；
- 强弱与辅助职责是否相容；
- 显式HighDynamic探针能否触发并按High→Mid→LongSlow恢复。

自然场景不得再要求最终状态名等于输入动态标签。

---

## 16. 问题一：outage场景不是算法先坏，而是时间轴先写错

### 16.1 outage是什么

outage是人为制造一段信号功率降到零或极低的遮挡，用来检查：

1. 遮挡开始时接收机是否保持合理预测；
2. 遮挡期间是否发生PDI或命令时间轴错误；
3. 信号恢复后能否在规定时间内重新稳定；
4. 是否错误地一直宣称自己仍锁在正确峰上。

### 16.2 为什么必须按20 ms对齐

当前campaign的功率调度以20 ms为一个控制帧。所有调度点必须满足：

$$
\frac{t_k}{0.020}\in\mathbb Z
$$

如果下降沿、零功率平台、恢复沿分别占20 ms、$N\times20$ ms、20 ms，则完整调度应是：

```text
正常功率 ──20 ms下降沿──> 零功率 ──N×20 ms平台──> 零功率 ──20 ms恢复沿──> 正常
```

![[90-附件/图RT-11_outage时间轴.svg]]

原脚本把边沿包含进了用户请求的outage长度，导致真正的零功率平台少20 ms。算法看到的
不是清单声称的场景，结果再精确也不能当作该场景证据。

### 16.3 非零pilot offset又引入了第二个脚本问题

现代导频案例可能从二次码的非零代表相位开始，因此 `start_sample` 不为0。若仍把
时间0的Doppler和码相位直接交给TrackRunner，实际入口就不是预定残差。

修正到入口时刻后，一些码相位落在主码周期末端。旧脚本直接格式化浮点值，又把
`10229.99999999`舍入成非法的`10230.00000000`，导致CLI在算法启动前拒绝。

### 16.4 当前已验证事实

活动目录：`/tmp/startrack-outage-aligned-final-20260824`

活动摘要SHA256：
`44e099b3bda931528ddfe296a440011e0263ec64f1211ce269fcc954059f0e4f`

Release binary SHA256：
`c7c6625a0dea5d0bffa8ae8da9e09a099f4769b968cac3865d3c58d057b7074e`

24例中，19例完成真实IQ并通过：

| 指标 | 19例中的最坏值 | 案例 |
|---|---:|---|
| outage恢复时间 | 4.459 s | `e5a_outage_14` |
| Doppler RMS | 1.611 Hz | `g1_outage_14` |
| Doppler P95 | 3.206 Hz | `g1_outage_14` |
| 码误差P95 | 0.0122 chip | `l5_outage_13` |

其余5例是CLI量化边界错误，没有进入跟踪算法：

- `l5_outage_14`；
- `b2a_outage_12`、`b2a_outage_13`、`b2a_outage_14`；
- `e5a_outage_12`。

StarGen在下游提前退出后收到SIGPIPE，`gen_rc=-13`。SIGPIPE表示管道下游已经关闭，
上游继续写数据时被操作系统终止；这不能写成“跟踪失败”。

### 16.5 整改方案A

#### 实现原则

1. outage请求长度只表示完整零功率平台，不包含边沿；
2. 所有schedule点必须在20 ms网格，生成时fail closed；
3. 非零offset先在绝对输入时间 $T_0$ 重定向Doppler和码相位；
4. 码相位先在Q16整数域回绕，再输出十进制；
5. CLI文本必须反算回同一个Q16字，否则启动前拒绝；
6. 旧19个通过案例保留为旧活动证据，5个缺失案例重跑后再形成24/24新summary，不能直接
   修改旧占位为PASS。

#### 验收门

- 24例每个schedule点都是20 ms整数倍；
- 零功率平台时长逐例等于请求值；
- `-1 LSB`得到 `code_length-1/65536`，周期值回绕为0；
- 五个旧CLI拒绝案例管道正常；
- 每例恢复不超过35 s；
- 场景、命令、CSV、result、summary重新绑定SHA。

---

## 17. 问题二：B1C弱信号高动态下出现“同步标志假稳定”

### 17.1 先看三个原失败案例

| 案例 | 首次同步 | Doppler RMS/P95 | 码P95 | 当前结论 |
|---|---:|---:|---:|---|
| `b1c_dynamic_20` | 36.918 s | 1.639/3.116 Hz | 0.0291 chip | 修正移交时间轴后通过 |
| `b1c_transition_08` | 36.919 s | 17.308/22.226 Hz | 1.552 chip | 同步仍为1，但已离开正确码峰 |
| `b1c_transition_11` | 36.918 s | 38.782/47.938 Hz | 0.239 chip | 同步及时，弱动态精度仍失败 |

对应result SHA256：

- `dynamic_20`：`3326e30258d59ea2fe484026549dcf80417a91f29e5bff1daead3282f2a0a753`；
- `transition_08`：`6d7d7be1810fa437d39679738d8b21d62eccb156ae350f2794c8048de5d8a45c`；
- `transition_11`：`339eb2a893a2253b819642929eb1f1bb2ca391109b4f801131a007a5d0d570f4`。

### 17.2 这些场景比经典1 Hz/s更难在哪里

`transition_08/11`不是已经资格化的经典1 Hz/s缓变：

- 线性rate约为 $+1.263/-1.243\ \mathrm{Hz/s}$；
- 叠加约5 Hz正弦调制；
- 还有jerk和TCXO；
- 复合功率约11.77/12.24 dB-Hz；
- 弱动态保持约40 s。

因此它们是随机压力角，不应反向改写经典资格目标。但“同步标志在错误码峰上长期保持”是
接收机完整性问题，不能用“场景更难”回避。

### 17.3 什么叫同步标志假稳定

假设PilotSync在时刻 $t_0$ 正确锁定。之后载波或码逐渐越出当前观察器的无模糊范围，但：

- 同步器没有立即看到一个明确的相位冲突；
- 局部FMS在错误局部峰附近仍给出可用小残差；
- 局部DLL也可能在另一个BOC零点附近输出小量；
- FSM看到PDI和命令都正常，于是继续保持状态。

日志就会出现：

```text
sync = 1
freq_used = 1
没有PDI/cmd错误
但Doppler误差数十Hz，码误差超过1 chip
```

这不是“只是精度差一点”，而是状态标志与物理事实冲突。

![[90-附件/图RT-12_B1C假稳定机理.svg]]

### 17.4 当前能够确认和不能确认的内容

#### 已验证事实

- `dynamic_20`的首要问题是非零offset移交没有重定向；修正后算法通过；
- `transition_08/11`修正移交后仍失败，不能再归因于runner时间轴；
- 两例没有PDI、命令或overflow错误；
- `transition_08`的sync保持与1.552 chip误差同时存在，不能把sync当稳定充分条件；
- 当前生产 `Sensitivity` 是320 ms、3频点、5 Hz间隔的局部FMS，职责是自主极弱低动态保持。

#### 当前假设

弱信号、正弦动态、jerk和TCXO共同把真实峰推离局部FMS/局部码鉴别器的可信区域，而局部
观察仍围绕错误零点输出小量，造成错误峰保持。

#### 待确认问题

- 第一个能够区分“正常极弱保持”和“错误峰保持”的内部证据是什么；
- 正确峰仍在现有五抽头/宽频接受域内时，能否受限重新牵引；
- 正确峰已离开接受域时，何时应明确撤销同步并请求外部重捕获；
- 是否应阻止某些动态组合过早进入Sensitivity，而不是进入后再恢复。

### 17.5 整改方案B：先建立可解释诊断，再选择控制动作

#### 第一步：只读诊断

同一历元记录并对齐：

1. 当前局部FMS残差、峰值和无模糊边界；
2. 载波环预测值与独立频率监测值；
3. 宽频目标观察的主峰、次峰和物理频率；
4. ASPeCT修改功率五抽头与局部式(14)码误差；
5. DllMonitor均值、RMS、成熟度；
6. 同步相位比、确认状态和失效计数；
7. NCO命令实际确认时刻。

诊断字段只能写日志，不能被FSM、FLL、DLL或同步器读取。先用已通过B1C -158/-160静态和
1 Hz/s+TCXO作为正常分布，再用 `transition_08/11` 找无交集证据。阈值选型seed与held-out
seed分离。

#### 第二步：按证据选择三类结果之一

##### B1：提前阻止越域

如果宽频观察在真实峰仍可见时已经持续证明局部FMS不再适用，则保持或回到LongSlow，
不要进入更窄的Sensitivity观察。

##### B2：受限重新牵引

如果宽频与ASPeCT证据证明码仍在已冻结接受域内，可复用现有粗方向、有限slew和FineCheck。
不能读取真值直接装载码相位，也不能把任意远距离搜索伪装成跟踪。

##### B3：明确撤销同步并请求重捕获

如果正确峰证据已经丢失、或宽频与局部观察持续冲突，必须清除sync并报告需要重捕获。
“正确地承认丢失”优于“在错误峰上持续输出sync=1”。

#### 第三步：闭环验收

`transition_08/11`必须出现以下二者之一：

1. 恢复并保持在5/10 Hz、0.2 chip门内；
2. 在错误峰长期保持前明确撤销同步并发出重捕获需求。

禁止出现第三种结果：`sync=1`但码误差越过0.5 chip或误差持续发散。

### 17.6 B1C回归门

任何生产修改都必须保持：

- B1C经典-158/-160静态三种子不新增失锁；
- B1C经典1 Hz/s+TCXO三种子不新增状态抖动；
- `dynamic_20`首次同步不晚于42 s且满足5/10 Hz、0.2 chip；
- 预同步wide FLL、三次有限slew、FineCheck、18+18 s同步时序不被悄悄缩短；
- E1和其他导频路径逐字段不变；
- 无PDI、cmd、overflow错误；
- 完整Release CTest、style和dependencies通过。

---

## 18. 问题三：测试把“输入动态”错当成“最终状态职责”

### 18.1 原判据为什么不合理

自然场景脚本知道自己注入了 `low/medium/high` 动态，于是要求最终状态分别进入
LongSlow、MidDynamic或HighDynamic。这个逻辑忽略了闭环的目的：状态机看的是**剩余误差**，
不是场景真值标签。

如果ShortSlow或LongSlow已经把一个高动态输入压到5/10 Hz内，说明当前策略有足够裕量。
强迫它再进入HighDynamic反而会增加带宽和噪声，不是更“符合预期”。

### 18.2 当前证据

- 50例只有最终状态不符合脚本预期；
- 没有非法跳转；
- 没有错误入口；
- 特别是8个高动态耐久案例物理精度全部通过；
- 当前芯片职责的首次HighDynamic入口只从LongSlow残余动态证据产生；
- MidDynamic是HighDynamic退出后的恢复台阶。

因此这50例首先是测试职责门错误，不是50个算法失败。

### 18.3 整改方案C：三种结论独立输出

runner应输出：

1. `stability_passed`：时间轴、同步和闭环完整性是否健康；
2. `precision_passed`：Doppler和码误差是否满足门限；
3. `state_role_passed`：入口、合法跳转、强弱/辅助职责是否正确。

三者不能互相覆盖。例如：

| 情况 | 稳定 | 精度 | 状态职责 | 最终解释 |
|---|---|---|---|---|
| 普通状态跟住高动态 | 通过 | 通过 | 通过 | 无需HighDynamic |
| sync保持但错峰 | 失败 | 失败 | 可正常 | 算法完整性失败 |
| 精度稍超门但不发散 | 通过 | 失败 | 通过 | 稳定但未达精度 |
| 物理通过但非法跳转 | 通过 | 通过 | 失败 | FSM实现问题 |

### 18.4 HighDynamic必须用显式探针验证

另建专用状态探针，直接构造满足生产入口条件的**残余证据**，而不是只把场景rate命名为
high：

1. 先从LongSlow建立合法稳定入口；
2. 让连续独立频率批次满足RMS与均值比门，或可信rate超过14.648 Hz/s；
3. 要求进入HighDynamic；
4. 降低残余动态；
5. 要求按HighDynamic → MidDynamic → LongSlow恢复；
6. 全程命令从下一PDI生效，状态提交等待命令确认。

---

## 19. 分阶段实施计划

### 19.1 工作包A：修runner，不改跟踪算法

目标：关闭outage 20 ms平台、非零offset移交和Q16文本边界。

允许修改：

- `tools/run_l1ca_campaign.py`及共享schedule/Q16辅助；
- `tools/run_data_signal_campaign.py`；
- `tools/run_pilot_signal_campaign.py`；
- 对应Python测试与活动报告。

禁止修改：FLL、PLL、DLL、FSM、ASPeCT和Profile。

完成定义：5个CLI拒绝案例重跑通过，并与19例组合形成可审计24/24新活动。

### 19.2 工作包B：改状态职责判分，不改状态机参数

目标：自然场景不再按输入标签强迫最终动态状态；增加显式High/Mid恢复探针。

允许修改：随机活动runner、analyzer、manifest、Python测试和报告。

禁止修改：为了让旧脚本通过而降低HighDynamic入口门或缩短驻留。

完成定义：自然场景按稳定/精度/职责三层输出；显式探针准确触发并恢复。

### 19.3 工作包C：B1C只读诊断

目标：找到正常极弱保持与错误峰保持之间不重叠的内部证据。

允许修改：只读诊断结构、logger、离线分析、定向隔离测试和报告。

禁止修改：任何控制公式、阈值、状态或同步行为。

完成定义：诊断对固定输入逐历元可复现；排除诊断列后控制轨迹与基线一致；形成训练/held-out
分离的证据报告。

### 19.4 工作包D：B1C完整性闭环

只有工作包C证明证据可分后才能实施。根据结果选择提前阻止越域、受限重牵引或明确失锁，
一次只实现一个可解释机制。

禁止把runner修复、诊断和生产阈值改动混进同一提交。

---

## 20. 代码导航：读者想看源码时从哪里开始

| 概念 | 文件 | 关键接口/函数 |
|---|---|---|
| 总编排 | `src/tracking/track_engine.cpp` | `TrackEngine::processPdi()` |
| 状态策略 | `src/tracking/track_fsm.cpp` | `kTrackPolicies`、`desiredDynamic()` |
| 状态公共模型 | `include/model/track_data.h` | `TrackState`、`TrackMode`、观察结果结构 |
| 1 ms硬件相关 | `src/hardware/hw_channel.cpp` | `HardwareChannel`处理链 |
| PDI解码 | `src/integrator/pdi_decoder.cpp` | `PdiDecoder::decode()` |
| 导频擦码 | `src/integrator/pilot_wiper.cpp` | `PilotWiper::wipe()` |
| 普通频率观察 | `src/carrier_observer/freq_observer.cpp` | `FreqObserver` |
| B1C完整主码FLL | `src/carrier_observer/code_period_fll_obs.cpp` | `CodePeriodFllObs::addWide()` |
| 载波控制 | `src/carrier_loop/carrier_loop.cpp` | `CarrierLoop::correct()` |
| ASPeCT公式 | `src/code_observer/aspect_disc.cpp` | `aspectPower()`、`AspectDisc::combine()` |
| BOC粗牵引 | `src/code_observer/aspect_pull_obs.cpp` | `formCoarse()`、`combineWindow()` |
| DLL | `src/code_loop/code_loop.cpp` | `CodeLoop::update()` |
| 码稳定监测 | `src/monitor/dll_monitor.cpp` | `DllMonitor::add()` |
| 同步总编排 | `src/sync/signal_sync.cpp` | `SignalSync::add()` |
| 导频同步 | `src/sync/pilot_sync.cpp` | 搜索与独立确认 |
| 日志 | `src/logger/tracking_logger.cpp` | track/pdi/events输出 |
| 分析器 | `tools/analyze_tracking.py` | 同窗真值和通过门 |

建议源码阅读顺序与表格一致，不要从某个观察器中间跳到FSM，再回头猜PDI结构。

---

## 21. 已验证不可行或明确废弃的方案

### 21.1 用单个wide目标频差超限强制离开Sensitivity

结果：弱静态场景会因噪声wide观察误退出。原因是单窗异常没有足够连续证据，且wide目标
观察的职责不是直接替代稳定FMS控制。禁止重新采用。

### 21.2 增加到16个20 ms同方向观察就期待B1C恢复

结果：定向测试能通过，但 `transition_08` 真实IQ不触发，RMS、P95和码误差没有改善。
原因是错误峰问题不一定表现为连续同方向粗证据，单纯延长确认只会更慢。禁止把它当修复。

### 21.3 看到LoopCn0为8/10 dB就宣布失锁

结果：已通过的B1C -158/-160极弱跟踪中也会合法触发该控制量哨兵。原因是它是环路质量
下限，不是无偏物理C/N0，也不是错误峰独立证据。禁止单独作为失锁门。

### 21.4 放宽5/10 Hz或0.2 chip门

这只改变报告，不改变接收机。`transition_08`已经达到1.552 chip，远超“统计边缘误差”。
禁止通过放宽门限掩盖假稳定。

### 21.5 用 `sync==1` 定义稳定

同步只证明某个历史边界曾经确认。它不验证当前载波和码仍在正确峰。必须与频率、码误差、
独立锁定证据和时间轴共同判断。

### 21.6 把场景标签直接映射到状态名

HighDynamic是残余动态补救状态，MidDynamic是恢复台阶。输入真值很高但普通状态已经跟住时，
不进入动态状态是正确结果。禁止恢复旧的终态字符串匹配。

---

## 22. 本轮验收总表

### 22.1 runner修复

| 验收项 | 门限/要求 |
|---|---|
| outage调度 | 全部20 ms对齐 |
| 零功率平台 | 精确等于请求时长 |
| 非零offset | Doppler和码相位都重定向到入口时刻 |
| Q16 | 文本反算同一Q16字，周期边界正确回绕 |
| 旧缺失案例 | 5/5真正启动并完成跟踪 |
| 完整活动 | 新证据链24/24，不篡改旧summary |

### 22.2 状态职责

| 验收项 | 要求 |
|---|---|
| 自然场景 | 稳定、精度、职责三层独立输出 |
| 最终状态 | 不再等同输入dynamic标签 |
| 状态路径 | 无非法邻接、提交等待命令确认 |
| 显式动态探针 | HighDynamic进入，随后MidDynamic、LongSlow恢复 |
| 普通高动态耐久 | 物理通过即可职责通过，不强迫动态状态 |

### 22.3 B1C完整性

| 验收项 | 要求 |
|---|---|
| 正常回归 | -158/-160静态与1 Hz/s+TCXO不退化 |
| `dynamic_20` | 首sync≤42 s，RMS/P95≤5/10 Hz，码P95≤0.2 chip |
| `transition_08/11` | 恢复精度，或在错峰长期保持前明确失锁 |
| 假稳定 | 禁止sync=1且码误差>0.5 chip持续存在 |
| 时间轴 | PDI/cmd/overflow错误为0 |
| 跨信号 | E1及其他导频控制字段不变 |

### 22.4 通用工程门

```bash
cmake -S . -B build-release -G Ninja -DCMAKE_BUILD_TYPE=Release
cmake --build build-release --parallel 1
ctest --test-dir build-release --output-on-failure -j 1
python3 skills/startrack-cpp-style/scripts/check_startrack_style.py .
python3 scripts/validate_fixed_catalog.py
python3 scripts/validate_signal_campaign.py
git diff --check
```

长真实IQ测试必须串行，固定StarTrack、StarGen、runner、analyzer和manifest SHA。不同版本的
结果不能拼成一次资格。

---

## 23. 一页总结：新手最终应该记住什么

1. 捕获只给附近位置，跟踪通过载波环和码环持续对准；
2. 硬件每1 ms只交相关PDI，软件看不到原始I/Q；
3. 观察器测误差，环路改NCO，监测器看稳定，FSM只选策略；
4. `ready`是窗口满，`valid`是结果可信，`used`是环路真用了；
5. FLL追频率，PLL追相位，DLL追码；弱信号主要依赖FLL；
6. 码率等于标称码率、载波辅助和DLL残差之和；
7. BOC有旁峰，E1/B1C必须用BOC与PRN-only双副本的ASPeCT；
8. slew是有起止时间的有限码率偏置，不是码相位瞬移；
9. FineCheck是粗牵引后、同步前的细码安全复核，不是一个新环路；
10. B1C必须先wide FLL、再粗码、再slew、再FineCheck，最后才做18+18 s同步；
11. 状态名称描述接收机职责，不等于场景标签；MidDynamic是恢复台阶；
12. sync=1不等于物理稳定，错误峰也可能保留历史同步标志；
13. runner错误必须先修输入时间轴，不能让算法替脚本背锅；
14. B1C弱动态问题必须用独立内部证据识别，不能靠放宽门限；
15. 任何整改都必须保持经典灵敏度、跨信号隔离和命令确认时序。

---

## 24. 术语速查表

| 术语 | 最短解释 |
|---|---|
| Acquisition | 捕获；在频率×码相位网格上找到粗位置 |
| Handoff | 移交；把捕获结果交给跟踪器 |
| I/Q | 保存幅度和相位的复信号两个分量 |
| chip | 伪随机码的一个码片 |
| NCO | 数控振荡器；决定本地载波/码每样点推进量 |
| PDI | 硬件每1 ms发布的相关积分记录 |
| P/E/L | Prompt/Early/Late，中/早/晚码抽头 |
| VE/VL | 更早/更晚抽头，用于较宽码偏差判断 |
| coherent | 复数相关直接相加，保留相位 |
| noncoherent | 功率相加，不要求跨块相位一致 |
| FLL | 根据相位随时间变化追频率 |
| PLL | 根据Prompt相位追载波相位 |
| DLL | 根据码相关两侧不平衡追码相位 |
| carrier aid | 用载波多普勒预测主要码率变化 |
| BOC | 带副载波、相关函数有旁峰的调制 |
| BPSK-like | 捕获中忽略BOC副载波的近似表示 |
| ASPeCT | 用BOC和PRN-only双副本抑制旁峰的算法 |
| coarse | 只表示左/中/右的粗方向观察 |
| fine | 中央单调域内可进入DLL的连续chip误差 |
| slew | 固定时间内施加临时码率，完成有限码相位移动 |
| FineCheck | 粗牵引后用细码连续证据复核中央主峰的阶段 |
| sync | 数据bit或导频二次码边界已确认 |
| PilotWiper | 按已知二次码符号统一擦除导频相关值 |
| ready | 观察窗口已经积满 |
| valid | 成熟观察通过物理与质量门 |
| used | 本次观察确实被环路采用 |
| FSM | 根据证据选择观察周期和环路策略的状态机 |
| outage | 人为制造的一段信号遮挡或零功率平台 |
| TCXO | 温补晶振误差模型，会产生连续频率扰动 |
| RMS/P95 | 均方根误差/绝对误差95百分位 |
| LSB | Least Significant Bit，定点字中的最低有效位 |

---

## 25. 修改记录

| 版本 | 日期 | 修改内容 |
|---|---|---|
| 1.0 | 2026-08-25 | 建立面向审计的三问题整改清单 |
| 2.0 | 2026-08-25 | 按教程体例重写：从基础概念、数据流、环路、BOC牵引和状态职责逐层讲解，并将整改放到完整算法背景之后 |
